# KK_Crab 会议纪要 `R01`

- 会议形式：异步轮次会议
- 轮次：`R01`
- 时间：`2026-03-16 14:50`
- 主持：总控窗口

## 本轮输入

1. 前端、后台、后端三条线已分别完成第一轮工程骨架。
2. 当前需要从“可开发”转为“可协同、可联调、可收口”。

## 各线进展

### 前端

- 已完成小程序工程骨架、主包/分包、主 Tab、共享层和首批内页骨架。
- 当前保持 mock 联调模式。
- 主要外部依赖：CloudBase 环境信息、`bootstrap` 真接口、城市筛选数据源。

### 后台

- 已完成后台 Web 工程骨架、登录与布局、核心路由守卫、审核/举报/处罚/日志页面骨架。
- 当前可本地运行，业务流程主要由 mock 驱动。
- 主要外部依赖：`review-admin` / `governance-admin` 字段级 DTO、`availableActions`、`permissionSummary`。

### 后端

- 已完成 CloudBase 后端底座、`shared`、`user-bff.bootstrap`、`admin-auth`、数据库初始化脚手架。
- 当前具备最小联调基础。
- 主要待补：后台管理端 DTO 明细、CloudBase 规则下发口径、环境说明。

## 风险与阻塞

1. 后台管理接口 contract 目前只锁定了返回容器，字段级结构未完全锁定，后台无法安全替换 mock。
2. 前端没有 CloudBase 环境信息，无法切换真实联调。
3. “城市筛选数据源”与 `preferredView` 默认值仍未正式拍板。

## 本轮已拍板决策

1. 使用“会议制 + 收件箱/回报箱”作为长期协作方式。
2. 前端继续保留 mock，不自创业务规则、权限规则、联系方式释放逻辑。
3. 后台优先接入真实 `admin-auth`，其余后台业务接口继续 mock。
4. 后端下一轮优先补后台管理接口 DTO 与样例，不优先扩展 `mine/profile`。

## 下一轮任务分配

### 分配给前端

1. 继续细化四个主 Tab 和首批内页。
2. 保持 mock，但准备 `bootstrap` 的真实联调切换点。
3. 城市筛选继续保留占位，不内置城市枚举。
4. 条件允许时补做 TypeScript 类型检查。

### 分配给后台

1. 接入真实 `admin-auth`。
2. 收口 `review-admin` / `governance-admin` 的 service/model。
3. 保持页面骨架稳定，等待后端 DTO 后整体替换。

### 分配给后端

1. 输出 `review-admin` / `governance-admin` 的字段级 DTO。
2. 输出 `availableActions` 枚举与 `permissionSummary` 结构。
3. 输出小程序联调环境说明与 CloudBase 规则落地方案。

## 会议后动作

1. 总控窗口更新各线收件箱。
2. 各线读取收件箱并继续。
3. 下一轮会议在新回报或新阻塞出现后召开。
