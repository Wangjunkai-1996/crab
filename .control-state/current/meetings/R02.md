# KK_Crab 会议纪要 R02

- 会议形式：异步轮次会议
- 轮次：R02
- 时间：2026-03-16 15:47
- 主持：总控窗口

## 本轮输入

1. 前端、后台、后端均已提交第二轮回报。
2. 本轮重点从“骨架是否存在”转向“真实联调是否可启动、共享契约是否已收口”。

## 各线进展

### 前端

- 已完成 `bootstrap` 真联调切换准备，抽出运行时适配层，可在拿到 CloudBase `envId` 后切真实链路。
- 已将首批主 Tab 与内页接入 bootstrap 守卫，失败时统一走页面级 error state。
- 已补齐城市筛选留位与 TypeScript 静态编译检查。

### 后台

- 已接入真实 `admin-auth` 调用通道，支持真实云函数与 mock 回退双模式。
- 已进一步收口权限 helper、路由守卫、工作台入口和页面只读态。
- 当前主要等待真实环境 smoke 与采用新的 `permissionSummary` / `availableActions` 结构。

### 后端

- 已输出后台管理端 DTO 契约层、`availableActions` 枚举、`permissionSummary` 结构与 JSON 样例。
- 已补 CloudBase 接入说明、`preferredView = null` 兼容方案与可执行初始化脚本。
- 下一步重点转向真实 handler 与 `dev` 环境实跑。

## 风险与阻塞

1. CloudBase `dev/test` 环境 ID、已部署函数与管理员账号尚未提供，前端和后台都无法完成真实 smoke。
2. 城市筛选数据源与默认城市来源仍未拍板。
3. `preferredView` 默认值仍未拍板，但技术上已约定可返回 `null`。
4. 举报/处罚状态码与部分后台 DTO 字段尚未正式回填共享文档。

## 本轮已拍板决策

1. 当前电脑继续使用仓库外运行态 `KK_Crab-control`；跨电脑续接以仓库内 `.control-state/current/` 为准。
2. R02 起，后台采用后端输出的 `permissionSummary = pageAccess + actionAccess` 结构。
3. R02 起，后台采用后端输出的 `availableActions` 最终 action key 枚举。
4. R02 起，`review-admin` / `governance-admin` 的实现口径以后端 `contracts/admin/*.ts` 与 `scripts/api-samples/*.json` 为准，正式文档回填可后置。
5. 在默认值未拍板前，`preferredView` 允许为 `null`，前端必须兼容，不得自行固化默认值。

## 下一轮任务分配

### 分配给前端

1. 继续保留 mock，但按后端接入说明准备真实 `bootstrap` 切换和 BFF 类型收口。
2. 细化 `packages/creator/apply` 与 `packages/publish/application-manage` 的状态页与表单体验。
3. 在城市源未拍板前，不做真实城市候选源逻辑，只保留占位。

### 分配给后台

1. 采用新的 `permissionSummary` 和 `availableActions` 结构，替换旧的 mock 权限假设。
2. 将 `review-admin` / `governance-admin` 的 service/model 统一切到后端样例口径，页面层不分散改字段。
3. 继续保留业务 mock，待环境信息到位后完成 `admin-auth` 真实 smoke。

### 分配给后端

1. 将 `taskList`、`reportList`、`accountActionList` 绑定到真实 handler，打通最小查询闭环。
2. 实现 `taskDetail`、`reportDetail` 聚合查询与 `availableActions` 动态生成。
3. 输出 `dev/test` 环境 handoff 清单；如环境侧配合到位，完成初始化脚本实跑验证。
4. 补前端首批 BFF 返回样例，优先 `notice-bff`、`application-bff`、`message-bff`、`creator-bff`、`publisher-bff`。

## 会议后动作

1. 总控窗口更新 `board.md`、`decisions.md`、`parking-lot.md`。
2. 总控窗口刷新三条线收件箱为 R02 任务。
3. 下一轮会议在新回报、环境到位或重大阻塞出现后召开。
