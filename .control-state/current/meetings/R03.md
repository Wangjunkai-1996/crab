# KK_Crab 会议纪要 R03

- 会议形式：异步轮次会议
- 轮次：R03
- 时间：2026-03-16 16:55
- 主持：总控窗口

## 本轮输入

1. 前端、后台、后端均已提交 R02 回报。
2. 本轮重点从“契约收口”推进到“联调前最后一轮适配准备”和“真实 handler 继续扩面”。
3. 当前仍未拿到 CloudBase `dev/test` 环境 ID、已部署函数清单、管理员账号与必要密钥，因此本轮仍以“联调前准备”与“真实 handler 扩面”为主。

## 各线进展

### 前端

- 已完成 `preferredView = null` 全链路兼容。
- 已把 `packages/creator/apply`、`packages/publish/application-manage` 从占位页补成可联调状态页。
- 已收口首批小程序 BFF 类型，并升级 mock adapter 支撑更多状态流转。

### 后台

- 已完成 `permissionSummary = pageAccess + actionAccess` 收口。
- 已将 `review-admin` / `governance-admin` 页面层依赖集中到 `service + model + normalizer`。
- 已完成主要业务页对 `availableActions` 与权限摘要的适配。

### 后端

- 已将 `review-admin.taskList/taskDetail`、`governance-admin.reportList/reportDetail/accountActionList` 绑定到真实 handler。
- 已补齐前端首批 BFF DTO 与 JSON 样例。
- 已输出环境 handoff 文档，但真实环境信息仍未到位。

## 风险与阻塞

1. CloudBase `dev/test` 环境 ID、已部署函数清单、管理员账号、必要密钥仍未提供，前端/后台/后端都无法完成真实 smoke。
2. 城市筛选数据源与默认城市来源仍未拍板，前端继续只保留占位与缓存位。
3. `preferredView` 最终默认值仍未拍板，但当前允许返回 `null` 的技术口径继续有效。
4. 后台动作请求枚举 `resolveReport.resultAction`、`accountAction.restrictionType`、`resolveTask.nextQueueType` 仍未正式锁定。
5. `priorityItems[]`、`reportDetail.targetSnapshot.summary`、举报/处罚状态码、`dm_configs` 命名差异仍待后续拍板或文档回填。

## 本轮已确认口径

1. R03 继续沿用“会议制 + 收件箱/回报箱”模式。
2. 在环境未到位前，前端与后台继续保持“真实契约收口 + mock 可运行”，不假装已具备真实联调条件。
3. 后台动作请求枚举在正式拍板前，只能作为“待确认字段”集中收口，不得在页面层分散固化。
4. 前端若缺少从“我的报名”直跳“报名详情”的正式 `applicationId` 聚合字段，不得自行脑补聚合返回。

## 下一轮任务分配

### 分配给前端

1. 继续按后端最新 sample 收口 `notice-bff`、`message-bff`、`application-bff` 页面使用字段，减少页面层散落 DTO。
2. 把真实请求切换点进一步集中到 service/request 层，确保环境到位后可最小改动切通 `bootstrap` 与首批 BFF。
3. 针对“我的报名 -> 报名详情”缺少正式 `applicationId` 聚合字段的情况，维持安全降级路径，不自行扩展 contract。
4. 城市筛选继续只做字段位、缓存位、回显位，不实现真实候选源逻辑。

### 分配给后台

1. 继续用后端 sample 收口 `review-admin.taskList/taskDetail`、`governance-admin.reportList/reportDetail/accountActionList` 的集中 normalizer 和页面读模型。
2. 将 `resolveReport.resultAction`、`accountAction.restrictionType`、`resolveTask.nextQueueType` 收敛到单点常量/映射层，标记为待确认，不在页面层扩散。
3. 保持 `admin-auth` 真接口通道不回退；若环境信息到位，优先完成登录 / 恢复 / 登出 / 改密 smoke。
4. 在后端继续补 real handler 前，保持业务页 mock 可运行，但 mock 形状必须以后端 contract/sample 为准。

### 分配给后端

1. 继续将 `governance-admin.dashboard`、`operationLogList` 从 contract/sample 推进到真实 handler。
2. 输出后台动作请求枚举和 payload 样例建议，优先覆盖 `resolveReport.resultAction`、`accountAction.restrictionType`、`resolveTask.nextQueueType`，如未拍板需明确“临时口径”。
3. 继续补管理端高频动作 handler，优先 `claim/release/resolve` 系列闭环。
4. 继续补小程序高频真实 handler，优先 `notice-bff` / `application-bff` 核心闭环；如环境未到位，可先补 sample / contract / handler 骨架。

## 会议后动作

1. 总控窗口更新 `board.md`、`parking-lot.md`、三条线 `inbox/*.md`。
2. 本轮无需变更既有已拍板事项；如动作枚举或环境信息后续拍板，再写入 `decisions.md`。
3. 下一轮会议在三条线回报更新、环境到位或关键阻塞需要统一裁决时召开。
