# KK_Crab 会议纪要 R04

- 会议形式：异步联合收口会
- 轮次：R04
- 时间：2026-03-16 18:39
- 主持：总控窗口
- 参与角色：前端、后台、后端、产品经理、体验设计审查

## 本轮输入

1. 前端、后台、后端已完成 R03 回报。
2. 产品经理、体验设计审查已完成 R03A 专项审查回报。
3. 本轮目标是把“技术推进状态”“产品规则缺口”“设计审查边界”统一收口，并派发下一轮任务。

## 各角色进展

### 前端

- 已把真实 / mock 切换继续收敛到 `service/request -> cloud` 链路。
- 已按后端 sample 收口 `notice-bff.list/detail`、`message-bff.list`、`application-bff.myList/detail` 的页面消费字段。
- 已撤销伪造 `applicationId` 的假直跳，当前统一安全降级到“我的报名列表”。
- 已继续推进页面显式类型化，减少 loose type 扩散。

### 后台

- 已把 `resolveReport.resultAction`、`accountAction.restrictionType`、`resolveTask.nextQueueType` 收敛到单点常量 / bridge 层，并明确标记为待确认。
- 已继续把 mock list/detail/action 输出向后端 contract / sample 靠拢。
- 已完成本轮 `typecheck` 与 `build` 校验。

### 后端

- 已将 `governance-admin.dashboard`、`governance-admin.operationLogList` 绑定到真实 handler。
- 已补 `claim/release/resolve` 系列管理端高频动作 real handler。
- 已输出后台动作请求的临时事实源、JSON sample 与说明文档，供后台继续单点对齐。

### 产品经理

- 已确认当前整体开发方向未偏离 V1 产品目标。
- 已识别出需要尽快拍板的产品规则：城市来源、`preferredView` 默认值、非列表场景跳报名详情策略、后台动作请求枚举、举报处理中可见性、处罚状态模型与英文 code。
- 已明确：在正式拍板前，前端 / 后台 / 后端都不应自行把这些项固化成最终规则。

### 体验设计审查

- 已确认当前主信息架构和主要页面模式方向基本与设计稿 / 原型 / 设计系统一致。
- 已识别出高风险体验缺口：首创空态承接未完全收口、发布侧 / 报名侧部分页面仍缺正式 contract/sample、后台动作区请求语义未冻结、工作台 / 日志页仍待 real smoke 复审。
- 已明确：城市来源、`preferredView`、直跳报名详情、mock 态工作台/日志页、Token 细节与状态页统一性当前都可暂列“临时占位 / 暂不判定”，但后续必须补齐。

## 本轮已确认结论

1. 当前五个角色的输出彼此不冲突：总体方向对齐，问题主要集中在“未拍板规则”和“环境未到位”。
2. 在产品未明确前，前端继续保持“非列表场景不直跳单条报名详情、统一安全降级到我的报名列表”的临时策略。
3. 在产品正式拍板前，后台动作请求继续以后端提供的临时事实源为单点口径，但不得当成最终产品规则写死到页面层。
4. 工作台 / 日志页、Token 细节、状态页统一性在 real smoke 或截图型回报到位前，暂不判定为最终体验完成态。

## 本轮新增阻塞与待拍板项

1. 城市筛选候选源与默认城市来源。
2. `preferredView` 最终默认值。
3. 是否要求“从通告 / 消息等非列表场景直跳单条报名详情”，以及是否必须补正式 `applicationId` 聚合字段。
4. `resolveReport.resultAction`、`accountAction.restrictionType`、`resolveTask.nextQueueType` 的最终产品枚举。
5. 举报记录对举报人的“处理中”可见性，以及举报状态正式英文 code。
6. 处罚状态模型是否采用“账号状态 + 处罚记录状态”双轨，以及对应英文 code 与解除回退规则。
7. CloudBase `dev/test` 环境 ID、已部署函数清单、管理员账号与必要密钥仍未提供。
8. 小程序 `notice-bff.myList`、`application-bff.publisherList`、`application-bff.publisherDetail` 仍缺正式 contract / sample。

## 下一轮任务分配

### 分配给前端

1. 继续收口 `notice-bff.myList`、`application-bff.publisherList`、`application-bff.publisherDetail` 的 service 适配层；在正式 sample 未到位前，不自行补业务规则。
2. 优先补齐 `creator-bff.getCard` / `publisher-bff.getProfile` 返回 `null` 时的首创空态承接，确保首访路径不掉空。
3. 继续维持“我的报名列表”安全降级路径，直到产品/后端正式确认 `applicationId` 聚合策略。
4. 下轮回报中请额外补充“关键状态页清单”，至少覆盖：广场筛选、发布校验失败、报名详情联系方式释放、我的页游客态 / 双角色态。

### 分配给后台

1. 继续把后台动作请求统一对齐到后端临时事实源：`action-payloads.ts`、`admin.action-payload-enums.json`、`admin-action-payload-suggestions.md`，但保留待确认标记。
2. 开始对齐 `governance-admin.dashboard`、`operationLogList` 的集中读模型与 service 适配，准备后续真实联调。
3. 在后端动作 handler 已到位的基础上，继续收口审核详情 / 举报详情 / 处罚页动作区确认流程，不新增页面散落枚举。
4. 下轮回报中请额外补充“关键状态页清单”，至少覆盖：审核详情、举报详情、处罚页、工作台、日志页的主要状态与弹层。

### 分配给后端

1. 优先补齐小程序缺口：`notice-bff.myList`、`application-bff.publisherList`、`application-bff.publisherDetail` 的正式 contract / sample；若暂不能定稿，需明确临时口径边界。
2. 继续推进 `notice-bff` / `application-bff` 高频 real handler，优先 `notice-bff.list/detail` 与 `application-bff.myList/detail` 真闭环。
3. 继续维持后台动作请求临时事实源的单点一致性；若产品拍板到位，立即升级为正式 contract。
4. 在环境信息一旦到位后，优先执行管理端与小程序首批真实 smoke，并把结果回写回报箱。

### 分配给产品经理

1. 基于本轮已识别的 6 个产品待拍板项，整理一份“拍板候选表”，输出推荐选项、影响范围与不拍板风险。
2. 不直接修改正式产品文档，先把候选表写入回报箱，供总控与用户确认。
3. 优先覆盖：城市来源、`preferredView` 默认值、直跳报名详情策略、后台动作枚举、举报处理中可见性、处罚状态模型与英文 code。

### 分配给体验设计审查

1. 整理下一轮体验复审清单，明确前端 / 后台需要补哪些关键页面状态、哪些需要截图 / 录屏或逐页状态清单。
2. 区分“环境未到位前可接受的临时占位”和“必须在真联调前补齐的高风险体验点”。
3. 优先覆盖：首创空态、关键状态页、动作确认弹层、工作台 / 日志页 real smoke 后复审条件、Token 级复核触发条件。

## 会议后动作

1. 总控更新 `board.md`、`decisions.md`、`parking-lot.md` 与五个 `inbox/*.md`。
2. 五个角色继续通过同一套 `inbox/outbox` 机制执行，不回退到聊天直发长任务模式。
3. 下一轮会议在以下任一条件满足时召开：
   - 五个角色都完成当前任务
   - 用户完成产品规则拍板
   - 环境信息到位并开始真实 smoke
