# KK_Crab 会议纪要 R05

- 会议形式：异步联合收口会
- 轮次：R05
- 时间：2026-03-16 19:16
- 主持：总控窗口
- 参与角色：前端、后台、后端、产品经理、体验设计审查

## 本轮输入

1. 五个角色均已提交新一轮回报。
2. 本轮重点从“补事实源 / 补 contract / 补临时策略”推进到“继续技术收口 + 形成用户可拍板决策单 + 为下一轮体验复审准备证据”。

## 各角色进展

### 前端

- 已补齐发布侧 `application-bff.publisherList` / `publisherDetail` 的 service 适配层。
- 已把 `creator-bff.getCard` / `publisher-bff.getProfile` 的 `null` 首创态承接到页面骨架，不再掉空。
- 已补关键状态页清单第一版，并继续保持“我的报名列表”安全降级路径。

### 后台

- 已将后台动作请求临时事实源拆到单独常量层 `admin-action-payloads.ts`。
- 已完成审核详情、举报详情、处罚页对单点 bridge / 常量层的迁移。
- 已补第一版后台关键状态页清单，并保持 `admin-auth` 真接口通道不回退。

### 后端

- 已补齐 `notice-bff.myList`、`application-bff.publisherList`、`application-bff.publisherDetail` 的正式 contract / sample。
- 已将 `notice-bff.list/detail/myList` 与 `application-bff.myList/detail/publisherList/publisherDetail` 推进到真实查询 handler。
- 已更新小程序 contract 状态说明，明确“已固化 / 临时口径 / 待拍板”三类边界。

### 产品经理

- 已输出 6 项待拍板问题的第一版候选表。
- 已给出当前推荐方向：
  1. 城市候选由服务端/BFF提供统一事实源，首次不默认具体城市。
  2. `preferredView` 首次保持 `null/未设置态`，单角色按唯一能力展示。
  3. V1 仅保证“我的报名列表 -> 报名详情”，非列表场景不强制直跳。
  4. 后台动作请求枚举维持当前推荐 code 映射，但仍待最终拍板。
  5. 举报人推荐可见 `processing`，举报状态需正式锁 code。
  6. 处罚推荐采用“账号状态 + 处罚记录状态”双轨模型。

### 体验设计审查

- 已输出第二轮复审所需的页面 / 状态 / 证据清单。
- 已明确哪些项必须在真联调前补齐，哪些项应在 real smoke / 截图到位后再做最终结论。

## 本轮已确认结论

1. 小程序发布侧缺少 `notice-bff.myList`、`application-bff.publisherList`、`application-bff.publisherDetail` 的 contract / sample 这一阻塞已解除。
2. 当前最大阻塞已收敛为两类：
   - 环境阻塞：CloudBase `dev/test` 环境、管理员账号、密钥未到位。
   - 产品阻塞：6 项待拍板规则尚未由用户/产品正式拍板。
3. 在用户正式拍板前，继续沿用以下临时策略：
   - `preferredView` 允许为 `null`
   - 非列表场景不保证直跳单条报名详情
   - 前端继续安全降级到“我的报名列表”
   - 后台动作请求继续以后端临时事实源为单点口径，但不视为最终规则
4. 下一轮不再只盯“有没有代码”，而是要开始补“证据材料”：关键状态页清单、必要截图/录屏或明确的无法提供原因。

## 本轮新增 / 变化项

1. 解除阻塞：小程序发布侧 `notice-bff.myList`、`application-bff.publisherList`、`application-bff.publisherDetail` 现已具备正式 contract / sample，且后端已接入真实查询 handler。
2. 新的工作重点：
   - 前端开始按正式 sample 收紧发布侧页面与状态事实。
   - 后台开始把 `dashboard` / `operationLogList` 向真实联调收口，并补状态证据。
   - 产品经理开始把候选表压缩成可直接让用户选择的“决策单”。
   - 体验设计审查开始把复审要求压缩成“证据清单模板”。

## 仍待拍板 / 阻塞

1. 城市筛选候选源与默认城市来源。
2. `preferredView` 最终默认值。
3. 是否要求非列表场景直跳单条报名详情，以及是否必须补正式 `applicationId` 聚合字段。
4. `resolveReport.resultAction`、`accountAction.restrictionType`、`resolveTask.nextQueueType` 的最终产品枚举。
5. 举报记录对举报人的“处理中”可见性，以及举报状态正式英文 code。
6. 处罚状态模型是否采用“账号状态 + 处罚记录状态”双轨，以及对应英文 code 与解除回退规则。
7. CloudBase `dev/test` 环境 ID、已部署函数清单、管理员账号与必要密钥。

## 下一轮任务分配

### 分配给前端

1. 直接按后端已补齐的 `notice-bff.myList`、`application-bff.publisherList`、`application-bff.publisherDetail` 正式 contract / sample 收紧 service 适配层，去掉不必要的临时字段假设。
2. 继续补首创空态与关键状态页事实材料；如无法提供截图 / 录屏，至少给出逐页状态说明与不可提供原因。
3. 保持“我的报名列表”安全降级，直到用户/产品正式拍板直跳策略。

### 分配给后台

1. 继续对齐 `dashboard`、`operationLogList` 与审核/举报/处罚相关读模型，准备后续真实联调。
2. 继续保持动作请求以单点临时事实源收口，不新增页面散落枚举。
3. 补第一版“后台证据包”：登录 4 态、审核详情、举报详情、处罚页、工作台、日志页的状态清单；能截图则截图，不能截图则写明原因和当前实现事实。

### 分配给后端

1. 继续推进 `application-bff` 发布侧动作接口 real handler：`markViewed`、`markContactPending`、`markCommunicating`、`markRejected`、`markCompleted`、`revealCreatorContact`。
2. 继续推进 `notice-bff` 发布侧写接口：`createDraft`、`updateDraft`、`submitReview`、`close`、`republish` 的 contract / validator / 写链路。
3. 一旦环境信息到位，优先执行管理端与小程序首批真实 smoke，并按模板回报。

### 分配给产品经理

1. 将 6 项候选表压缩成“决策单”版本，每项只保留：问题、推荐选项、备选选项、影响范围、建议优先级。
2. 输出格式要足够让用户快速逐项选择，不再写长篇分析。

### 分配给体验设计审查

1. 将当前复审要求压缩成“证据清单模板”，分别给前端和后台使用。
2. 每项只保留：页面/状态、必须证据、可接受替代证据、是否真联调前必须补齐。

## 会议后动作

1. 总控更新 `board.md`、`decisions.md`、`parking-lot.md` 和五个 `inbox/*.md`。
2. 五个角色继续按统一 `inbox/outbox` 机制执行。
3. 下轮会议触发条件：
   - 五个角色都完成当前任务
   - 用户完成 6 项规则拍板中的任意一批
   - 环境信息到位并开始真实 smoke
