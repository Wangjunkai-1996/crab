# KK_Crab 会议纪要 R06

- 会议形式：异步联合收口会
- 轮次：R06
- 时间：2026-03-17 11:52
- 主持：总控窗口
- 参与角色：前端、后台、后端、产品经理、体验设计审查、阻塞协调

## 本轮输入

1. 六个角色均已提交新一轮回报。
2. 本轮重点从“补 contract / 补决策单 / 补证据模板”推进到“对接新 real handler + 首轮证据审查 + 用户/环境阻塞拆单”。

## 各角色进展

### 前端

- 已按正式 sample 收紧 `notice-bff.myList`、`application-bff.publisherList`、`application-bff.publisherDetail` 的 service 适配层与 mock 形状。
- 已继续保持“我的报名列表”安全降级，并把 `preferredView = null`、城市只占位等临时策略落实到页面事实。
- 已提交关键状态页清单与不可提供 GUI 截图 / 真实 smoke 的原因，并完成 `tsc --noEmit` 校验。

### 后台

- 已补 `dashboard.routeKey` 归一与 `logs` 处理结果展示，继续保持动作枚举单点收口。
- 已产出首版后台证据包与 10 张页面截图，完成 `typecheck`、`build` 与本地页面级冒烟。
- 已将工作台 / 日志 / 详情页继续向现有 contract / sample 收口，但真实环境 smoke 仍未发生。

### 后端

- 已将 `application-bff` 发布侧动作接口推进到 real handler。
- 已将 `notice-bff` 写接口推进到真实写链路，并补消息 / 日志留痕。
- 已补小程序可用函数清单、接入说明与 R05 边界文档；同时明确 `republish` 仍有 `deadlineAt` 口径缺口。

### 产品经理

- 已把 6 项待拍板问题压缩为可选“决策单”。
- 已给出推荐优先级：先拍板题 4 / 5 / 6，再拍题 3，最后题 2 / 1。

### 体验设计审查

- 已把复审要求压缩为“证据清单模板”。
- 已明确真联调前必须关闭项与可后置项的分界。

### 阻塞协调

- 已输出第一版“用户阻塞处理单”。
- 已把 owner / env / permission 类阻塞拆为：`AppID`、CloudBase 环境交接包、管理员账号 / 初始化方式、6 项规则拍板。
- 已明确当前用户侧策略：先推进小程序账号注册 / `AppID`，其他信息允许后补，各线先继续非阻塞任务。

## 本轮已确认结论

1. `application-bff` 发布侧动作接口与 `notice-bff` 写接口这两个技术阻塞已解除，前端可继续按正式 contract / sample / handler 对接。
2. 前端已完成发布侧关键字段收口；后台已完成第一版证据包与首批截图；产品、体验、阻塞三类专项输出均已可直接投入下一轮使用。
3. 在 `AppID`、CloudBase 环境交接包、管理员账号与 6 项产品规则未到位前，六个角色继续推进不依赖真实环境或最终拍板的任务，不空等。
4. 继续沿用以下临时策略：
   - `preferredView` 允许为 `null`
   - 非列表场景不保证直跳单条报名详情
   - 前端继续安全降级到“我的报名列表”
   - 城市筛选只保留字段位 / 缓存位 / 回显位
   - 后台动作请求继续以后端临时事实源为单点口径，但不视为最终规则
5. `notice-bff.republish` 在正式补齐新的 `deadlineAt` 入参前，只支持“原通告仍具未来截止时间”的场景；过期通告无新截止时间不视为当前已支持。
6. 前端 / 后台下一轮回报开始，必须显式区分“截图 / 录屏证据”和“可接受替代证据”；如果无法补图，必须写明当前是 `mock` 还是 `real`、为什么无法补。

## 本轮新增 / 变化项

1. 新增显式 owner 阻塞：小程序 `AppID` / 主体准备成为独立前置项。
2. 后端新增可用函数清单与小程序接入说明，真实 smoke 的剩余阻塞已更聚焦。
3. 后台已从“状态清单”推进到“证据包 + 截图”阶段，前端下一轮需跟上同等级证据结构。
4. 体验设计审查从“提出问题”切到“按模板收证据和验缺口”阶段。
5. 阻塞协调已能单独承接用户 / 环境侧事项，后续不再混入开发线回报里拉扯。

## 仍待拍板 / 阻塞

1. 小程序 `AppID`、主体信息 / 可用公众平台账号。
2. CloudBase `dev/test` 环境 ID、已部署云函数清单、管理员账号 / 初始化方式、必要密钥 / 部署凭证。
3. 城市筛选候选源与默认城市来源。
4. `preferredView` 最终默认值。
5. 非列表场景是否要求直跳单条报名详情，以及是否必须补正式 `applicationId` 聚合字段。
6. `resolveReport.resultAction`、`accountAction.restrictionType`、`resolveTask.nextQueueType` 的最终产品枚举。
7. 举报记录对举报人的“处理中”可见性，以及举报状态正式英文 code。
8. 处罚状态模型是否采用“双轨模型”，以及正式英文 code 与解除回退规则。
9. `notice-bff.republish` 对“已过期通告补新截止时间”的正式 contract 是否要立项。

## 下一轮任务分配

### 分配给前端

1. 基于后端新 real handler，对接 `application-bff` 发布侧动作与 `notice-bff` 写接口的真实 request / service 承接层；不要自行拼联系方式字段。
2. 在页面 / service 层显式规避 `notice-bff.republish` 的“已过期且无新 deadlineAt”场景，直到正式 contract 拍板。
3. 按体验审查模板补前端“真联调前必须关闭项”的第一版证据；能截图则截图，不能截图则给状态清单 + `mock/real` 来源 + 无法提供原因。
4. 继续保持城市只占位、`preferredView` 兼容 `null`、非列表场景不直跳单条报名详情的当前策略。

### 分配给后台

1. 按体验审查模板，把已出的证据包继续补齐到“真联调前必须关闭项”，优先覆盖登录 4 态、审核详情、举报详情、处罚页、统一状态反馈。
2. 继续对齐 `dashboard`、`operationLogList`、审核 / 举报 / 处罚读模型，重点补“他人领取只读”“已处理只读”“错误态 / 保留输入”事实。
3. 保持 `admin-auth` 真接口通道不回退；环境一到位，优先完成 `login / me / logout / changePassword` 真实 smoke 与截图补齐。
4. 待拍板枚举继续只留在 bridge / 常量层，不扩散到页面文案和筛选层。

### 分配给后端

1. 优先推进 `application-bff.submit`、`withdraw` real handler，补齐达人侧报名写链路与计数更新。
2. 为后台补代表性 sample / contract 事实：`review-admin.taskDetail`、`governance-admin.reportDetail`、`accountActionList`、`operationLogList` 中“他人占用只读 / 已处理只读 / afterSnapshot 常见字段”代表样例。
3. 输出 `notice-bff.republish.deadlineAt` 的正式 contract 候选方案与 sample，但在产品拍板前仍标注为候选，不当作既定事实。
4. 保持环境阻塞表述准确；环境一到位即执行管理端与小程序首批真实 smoke，并按模板回报。

### 分配给产品经理

1. 把现有 6 题决策单继续压缩为“用户最小确认单”，优先只放高优先级 3 题：题 4、题 5、题 6。
2. 为每一题补一条“若用户接受推荐选项，可直接写入 `decisions.md` 的结论文案”。
3. 其余 3 题保留第二批确认，不混成同一轮超长问卷。

### 分配给体验设计审查

1. 基于你已给出的模板，直接对前端当前证据与后台当前证据包做第一轮 gap review。
2. 输出“已覆盖 / 缺失 / 可接受替代证据 / 暂缓项”四类结果，不新增设计稿外方案。
3. 对环境未到位导致无法出真图的项，只标记为“待环境后复核”，不要直接判开发未完成。

### 分配给阻塞协调

1. 把当前“用户阻塞处理单”压缩成可直接执行的 owner 侧行动包：
   - `AppID` / 主体准备清单
   - CloudBase 环境交接清单
   - 管理员账号 / 初始化方式索取清单
   - 可直接转发的一句话沟通文案
2. 明确区分“现在就能做”“需要等外部回复”“可以后补但必须登记”三类。
3. 一旦用户回传 `AppID`、环境对接人或规则拍板结果，第一时间回写回报箱供总控刷新全局。

## 会议后动作

1. 总控更新 `board.md`、`decisions.md`、`parking-lot.md` 和六个 `inbox/*.md`。
2. 六个角色继续按统一 `inbox/outbox` 机制执行。
3. 下轮会议触发条件：
   - 六个角色都完成当前任务
   - 用户回传 `AppID` 或 CloudBase 环境交接包
   - 用户完成 6 项规则拍板中的任意一批
   - 后端补齐达人侧写链路或后台代表 sample，需要同步前端 / 后台
