# KK_Crab 会议纪要 R07

- 会议形式：异步联合收口会
- 轮次：R07
- 时间：2026-03-17 12:44
- 主持：总控窗口
- 参与角色：前端、后台、后端、产品经理、体验设计审查、阻塞协调

## 本轮输入

1. 六个角色均已提交 R06 后续回报。
2. 本轮重点从“补新 real handler + 输出模板”推进到“继续接入达人侧写链路 + 做第一轮证据缺口审查 + 压缩 owner 侧行动项”。

## 各角色进展

### 前端

- 已把 `notice-bff` 写接口与 `application-bff` 发布侧动作继续收口到正式 mutation / sample 形状，并保持 mock / real 单切换通道集中在 request / cloud 层。
- 已对 `notice-bff.republish` 增加临时规避位：遇到“已截止且缺少新 `deadlineAt` contract”场景直接拦截，不外推业务规则。
- 已补第一版可接受替代证据，显式区分 `mock` 来源代码事实与 real-ready 承接层事实，但 GUI 截图 / 真实 smoke 仍未具备。

### 后台

- 已把后台证据包推进到 R06 版，新增 `ux-proof.md`、`state-facts.md` 与 25 张截图。
- 已补审核 / 举报详情路由参数监听、只读原因文案与本地可复现错误触发口径，便于继续收集“失败保留输入”等证据。
- 已完成 `typecheck`、`build` 与本地页面级冒烟，但真实 `admin-auth` smoke 仍待环境。

### 后端

- 已补 `application-bff.submit`、`withdraw` real handler，达人侧报名提交 / 撤回写链路已具备正式 contract / validator / sample / handler。
- 已为后台补齐“他人占用只读 / 已处理只读 / afterSnapshot 常见字段”的代表 sample。
- 已输出 `notice-bff.republish.deadlineAt` 的候选 contract / sample，但仍明确为候选，不视为正式共享 contract。

### 产品经理

- 已将 6 题待拍板问题进一步压缩为“第一批确认题（4/5/6）+ 第二批待确认题（1/2/3）”。
- 已给出若用户接受推荐选项时，可直接写入 `decisions.md` 的结论文案。

### 体验设计审查

- 已基于当前前端 / 后台回报完成第一轮证据覆盖度 gap review。
- 已明确前端当前缺 5 组高优先级证据、后台当前缺 4 组高优先级状态证据，并区分“可立即补”与“待环境后复核”。

### 阻塞协调

- 已把 owner / 环境 / 权限侧阻塞压缩为 4 个 owner 行动项。
- 已确认新增 owner 事实：当前按“个人小程序开发”路径推进；主体类型已确认为个人，但 `AppID` 仍未回传。
- 已给出可直接转发给开发 / 环境方的简短沟通文案，并拆出 CloudBase 环境交接清单与管理员账号 / 初始化方式索取清单。

## 本轮已确认结论

1. 达人侧 `application-bff.submit/withdraw` 技术阻塞已解除，前端可继续对接达人侧真实 request / service。
2. 后台对“他人占用只读 / 已处理只读 / afterSnapshot 常见字段”已有后端代表事实源，下一轮应优先据此收口后台 normalizer 与证据缺口。
3. 产品经理已把规则确认压缩成“先 4 / 5 / 6，后 1 / 2 / 3”的最小确认结构；在用户未拍板前，继续沿用当前临时策略，不漂移。
4. 体验设计审查已从“出模板”进入“做 gap review”阶段；前端下一轮优先补 5 组证据，后台下一轮优先补 4 组证据。
5. owner 侧新增已确认事实：小程序当前按个人主体路径推进；`AppID`、CloudBase `dev/test` 环境交接包、管理员账号 / 初始化方式仍未到位。
6. `notice-bff.republish.deadlineAt` 目前仍只是候选 contract；在总控 / 产品正式拍板前，前端继续规避，后端不得将其写入正式共享 contract。
7. 在 `AppID`、环境交接包、管理员账号和高优先级规则拍板未到位前，六个角色继续推进非阻塞任务，不空等。

## 本轮新增 / 变化项

1. 后端新增解除的阻塞项：达人侧 `submit/withdraw` 不再阻塞小程序报名写链路联调。
2. 后台证据从“第一版状态包”推进到“R06 版 ux-proof + state-facts + 25 张截图”。
3. 体验设计审查已给出下一轮收证据重点：
   - 前端 5 组：发布页首创空态、达人名片首创空态、报名页首访空态、通告详情页游客态 CTA、报名详情联系方式未释放 / 已释放成对截图。
   - 后台 4 组：审核详情分状态截图、举报详情分状态截图、处罚页分状态截图、统一状态反馈与确认弹层证据。
4. 阻塞协调已确认主体类型为个人，这一项不再是待确认；但 `AppID` 与环境对接人仍待确认。

## 仍待拍板 / 阻塞

1. 小程序 `AppID`、公众平台账号可持续使用情况。
2. CloudBase `dev/test` 环境 ID、已部署云函数清单、后台访问地址、管理员账号 / 初始化方式、必要密钥 / 部署凭证。
3. 题 4：后台动作请求最终枚举（高优先级）。
4. 题 5：举报记录对举报人的“处理中”可见性与举报状态正式英文 code（高优先级）。
5. 题 6：处罚状态模型与英文 code（高优先级）。
6. 题 3：非列表场景是否要求直跳单条报名详情，以及是否必须补正式 `applicationId` 聚合字段。
7. 题 2：`preferredView` 最终默认值。
8. 题 1：城市筛选候选源与默认城市来源。
9. `notice-bff.republish.deadlineAt` 是否进入正式 contract。
10. `publisher-bff` / `creator-bff` 资料写接口 real handler 尚未到位。

## 下一轮任务分配

### 分配给前端

1. 对接达人侧 `application-bff.submit/withdraw` 的真实 request / service 承接层；撤回仅支持 `applied/viewed`，不要自行扩展状态机。
2. 按体验审查 gap review，优先补 5 组高优先级证据：发布页首创空态、达人名片首创空态、报名页首访空态、通告详情页游客态 CTA、报名详情联系方式未释放 / 已释放成对证据。
3. 若仍无法给 GUI 图，继续按“页面 / 状态清单 + `mock/real` 来源 + 无法提供原因”的替代证据格式回报，且必须把通告详情页游客态 CTA 单独补进回报。
4. 继续规避 `notice-bff.republish` 的“已过期且需补新截止时间”场景，直到正式 contract 拍板。

### 分配给后台

1. 按体验审查 gap review，优先补 4 组高优先级证据：审核详情分状态截图、举报详情分状态截图、处罚页分状态截图、统一状态反馈与确认弹层证据。
2. 使用后端新增只读态 / `afterSnapshot` 代表 sample，继续收口 `taskDetail`、`reportDetail`、`accountActionList`、`operationLogList` 的 normalizer 与只读展示。
3. 明确区分“本地可立即补的证据”和“待环境后复核的真实链路证据”；不要把两类混写。
4. 环境一到位，优先 smoke `admin-auth` 的 `login / me / logout / changePassword`，补首次改密成功回工作区证据。

### 分配给后端

1. 继续推进 `publisher-bff` / `creator-bff` 资料写接口 real handler，减少对测试库预置资料的依赖。
2. 如总控 / 产品拍板接受 `notice-bff.republish.deadlineAt` 候选方案，立即升级为正式 contract、validator、handler 与 sample；未拍板前继续明确其为候选。
3. 保持环境阻塞表述准确；环境一到位立即执行管理端与小程序首批真实 smoke，并按模板回写结果。

### 分配给产品经理

1. 将第一批 3 题（4 / 5 / 6）进一步压成可直接发给用户的一条最小确认文案，尽量做到“用户一次回复即可拍板”。
2. 第二批 3 题（1 / 2 / 3）保持登记，不混入第一批确认文案。
3. 继续保留“若用户接受推荐选项，可直接写入 `decisions.md` 的结论文案”。

### 分配给体验设计审查

1. 将当前 gap review 再压缩成两张“must-close 清单”：一张给前端，一张给后台。
2. 每张只保留：项名、当前状态、补什么才算通过、是否待环境后复核。
3. 不新增设计稿外方案，不提前给最终体验通过结论。

### 分配给阻塞协调

1. 把 owner 侧事项继续压缩成 3 张可直接执行 checklist：`AppID/主体`、`CloudBase 环境交接`、`管理员账号/初始化方式`。
2. 每张 checklist 只保留：要什么、给谁问、回给总控什么事实。
3. 若用户当前只能处理一件事，明确优先级为：先 `AppID`，再环境交接包，再管理员账号 / 初始化方式。

## 会议后动作

1. 总控更新 `board.md`、`decisions.md`、`parking-lot.md` 和六个 `inbox/*.md`。
2. 六个角色继续按统一 `inbox/outbox` 机制执行。
3. 下轮会议触发条件：
   - 六个角色都完成当前收件箱任务
   - 用户回传 `AppID` 或环境交接包
   - 用户完成第一批规则（题 4 / 5 / 6）中的任意一项拍板
   - 后端将 `publisher-bff` / `creator-bff` 资料写接口推进到 real handler
