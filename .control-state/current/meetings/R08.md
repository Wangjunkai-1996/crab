# KK_Crab 会议纪要 R08

- 会议形式：异步联合收口会
- 轮次：R08
- 时间：2026-03-17 14:02
- 主持：总控窗口
- 参与角色：前端、后台、后端、产品经理、体验设计审查、阻塞协调

## 本轮输入

1. 六个角色均已提交 R07 后续回报。
2. 本轮重点从“达人侧写链路 + 第一轮证据 gap review + owner checklist”推进到“资料读写 real handler 接通 + 证据 must-close 清单化 + 用户最小拍板文案就绪”。

## 各角色进展

### 前端

- 已把达人侧 `application-bff.submit/withdraw` 接到真实承接层，并在报名详情页接入“撤回报名”入口。
- 已把 5 组前端高优先级证据全部补成单列替代证据，明确了对应页面、状态来源、`mock/real` 边界与无法提供 GUI 图的原因。
- 已继续保持 `notice-bff.republish` 候选 contract 场景规避，不外推规则。

### 后台

- 已把后台证据包继续推进，补出 `ux-proof.md`、`state-facts.md` 与 25 张截图。
- 已补审核 / 举报详情的路由监听、只读原因文案与可本地复现的失败触发口径，便于继续收集 must-close 证据。
- 真实 `admin-auth` smoke 仍待环境，但本地可复现证据已明显增加。

### 后端

- 已补 `publisher-bff.getProfile/upsertProfile` 与 `creator-bff.getCard/upsertCard` real handler，资料建档 / 编辑不再依赖测试库预置数据。
- 已补对应 contract、validator、sample 与接入说明，前端可直接切真实资料读写请求。
- `notice-bff.republish.deadlineAt` 继续保持候选边界，不写入正式 contract。

### 产品经理

- 已把第一批 3 题（4 / 5 / 6）压成可直接发用户的一段最小确认文案。
- 已继续保留第二批 3 题（1 / 2 / 3）的登记与拍板后可直接落 `decisions.md` 的文案。

### 体验设计审查

- 已将 gap review 进一步压缩为前后端 must-close 方向。
- 已明确：待环境后复核项、待拍板后复核项，不应误计入开发未完成或设计偏离。

### 阻塞协调

- 已把 owner 侧事项压成 3 张 checklist：`AppID/主体`、`CloudBase 环境交接`、`管理员账号/初始化方式`。
- 已新增 owner 事实：拟用名称为“多米通告”，类目当前选为“工具-预约/报名”；名称最终可用性仍待平台确认。
- 已明确当前优先级：先 `AppID`，再环境交接包，再管理员账号 / 初始化方式。

## 本轮已确认结论

1. `publisher-bff` / `creator-bff` 资料读写 real handler 已到位，这一技术阻塞已解除；前端资料页不再需要依赖 mock 预置 profile/card 才能联调。
2. 达人侧 `submit/withdraw` 与资料读写这两条小程序关键写链路，当前都已具备正式 contract / sample / handler；剩余真实联调主阻塞继续收敛到 `AppID`、CloudBase 环境与管理员账号。
3. 用户最小拍板文案已经可直接发出；第一批建议只拍题 4 / 5 / 6，不与第二批问题混发。
4. 前端当前 5 组高优先级证据仍以“可接受替代证据”为主；后台当前则需要继续把 must-close 证据从 `ready` 图推进到分状态、确认弹层、失败保留输入等关键状态图。
5. owner 侧新增事实可写入当前共识层面：主体类型为个人；拟用名称为“多米通告”；类目当前选为“工具-预约/报名”；其中 `AppID` 与名称最终可用性仍待确认。
6. `notice-bff.republish.deadlineAt` 仍只是候选 contract；在用户 / 产品拍板前继续保持前端规避、后端不升级为正式实现。

## 本轮新增 / 变化项

1. 新解除阻塞：`publisher-bff` / `creator-bff` 资料读写 real handler 到位。
2. 小程序真实联调的剩余技术阻塞进一步缩小为：`AppID`、CloudBase 环境交接包、管理员账号 / 初始化方式。
3. 阻塞协调新增 owner 事实：拟用名称“多米通告”、类目“工具-预约/报名”。
4. 产品经理已准备好可直接转发给你的第一批 3 题最小确认文案，总控后续可直接发你确认。

## 仍待拍板 / 阻塞

1. 小程序 `AppID`、公众平台账号可持续使用情况。
2. CloudBase `dev/test` 环境 ID、已部署云函数清单、后台访问地址、管理员账号 / 初始化方式、必要密钥 / 部署凭证。
3. 题 4：后台动作请求最终枚举（高优先级）。
4. 题 5：举报记录对举报人的“处理中”可见性与举报状态正式英文 code（高优先级）。
5. 题 6：处罚状态模型与英文 code（高优先级）。
6. 题 3：非列表场景是否要求直跳单条报名详情，以及是否必须补正式 `applicationId` 聚合字段。
7. 题 2：`preferredView` 最终默认值。
8. 题 1：城市筛选候选源与默认城市来源。
9. `notice-bff.republish.deadlineAt` 是否进入正式 contract。
10. CloudBase 环境到位后的首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给前端

1. 基于后端新 real handler，继续把发布页首创空态与达人名片首创空态的资料读写链路从 mock 替代证据推进到 real-ready 承接说明。
2. 继续细化 5 组高优先级证据中的触发路径、字段来源与状态切换说明；若仍无法给 GUI 图，必须逐项单列回报。
3. 环境一到位，优先验证：`bootstrap`、资料读写、达人侧 `submit/withdraw`、报名详情撤回入口的真实 smoke。
4. 继续维持城市只占位、`preferredView = null`、非列表场景降级到“我的报名列表”的当前策略。

### 分配给后台

1. 按 must-close 方向继续补 4 组关键状态证据：审核详情分状态、举报详情分状态、处罚页分状态、统一状态反馈与确认弹层。
2. 用后端新增 sample 继续收口 `taskDetail`、`reportDetail`、`accountActionList`、`operationLogList` 的 normalizer 与展示，不自造只读态字段。
3. 环境一到位，优先补 `admin-auth` 真实闭环与首次改密成功回工作区证据。

### 分配给后端

1. 为即将到来的真实 smoke 继续准备回归模板与必要错误样例，保持环境一到位即可验证的状态。
2. 若 `notice-bff.republish.deadlineAt` 获拍板，立即升级为正式 contract、validator、handler 与 sample；未拍板前继续保持候选边界。
3. 环境一到位后，先做管理端与小程序首批真实 smoke，并回写结果。

### 分配给产品经理

1. 保持第一批 3 题最小确认文案可直接发送状态。
2. 准备“用户部分确认时如何落盘”的最小规则，确保题 4 / 5 / 6 可按单题落 `decisions.md`。
3. 第二批 3 题继续登记，不提前混发。

### 分配给体验设计审查

1. 将当前 must-close 方向进一步压成可直接跟踪的前后台两张清单。
2. 对每一项只标：已补到什么程度、还差什么、是否待环境后复核。
3. 不提前给最终体验通过结论。

### 分配给阻塞协调

1. 继续跟踪 3 张 checklist 的事实回传情况。
2. 一旦 `AppID` 或环境交接包到位，第一时间回写 outbox 供总控刷新全局。
3. 若你准备先做规则拍板，也可把第一批 3 题确认文案单独发出，不与环境问题混发。

## 会议后动作

1. 总控更新 `board.md`、`decisions.md`、`parking-lot.md` 和六个 `inbox/*.md`。
2. 六个角色继续按统一 `inbox/outbox` 机制执行。
3. 下轮会议触发条件：
   - 六个角色都完成当前收件箱任务
   - 用户回传 `AppID` 或 CloudBase 环境交接包
   - 用户拍板第一批规则（题 4 / 5 / 6）中的任意一项
   - 后端完成真实 smoke 并回写结果
