# KK_Crab 会议纪要 R09

- 会议形式：异步联合收口会
- 轮次：R09
- 时间：2026-03-17 15:16
- 主持：总控窗口
- 参与角色：前端、后台、后端、产品经理、体验设计审查、阻塞协调

## 本轮输入

1. 六个角色均已提交 R08 后续回报。
2. 本轮重点从“资料读写 real handler 接通 + must-close 清单化 + 用户最小拍板文案就绪”推进到“`AppID` 到位后刷新全局事实、继续压缩真实联调阻塞、准备规则拍板与首批 smoke”。

## 各角色进展

### 前端

- 已把 `publisher-bff` / `creator-bff` 资料读写链路在模型、service、mock 形状上收口到正式字段口径。
- 已补“发布页首创空态”“达人名片首创空态”对应的 real-ready 承接说明，并继续把 5 组高优先级证据逐项单列。
- 已明确：当前资料链路、达人侧 `submit/withdraw`、报名详情撤回入口在环境到位后即可做真实 smoke。

### 后台

- 已继续用本地证据推进 must-close 项，当前“审核详情分状态 / 举报详情分状态 / 处罚页分状态 / 统一状态反馈与确认弹层”已进入“本地证据基本到位、待真实环境二次复核”的阶段。
- 已继续对齐后端 sample 与本地错误触发口径，为真实 `admin-auth` 与真实详情样例复核做准备。

### 后端

- 已确认 `publisher-bff.getProfile/upsertProfile` 与 `creator-bff.getCard/upsertCard` real handler 完整到位，并可供前端直接切真实资料读写请求。
- 已继续维持 `notice-bff.republish.deadlineAt` 的候选边界，不把候选方案误写成正式 contract。
- 当前主要技术动作已从“补 handler”转为“等待环境到位后执行真实 smoke”。

### 产品经理

- 已把第一批 3 题（4 / 5 / 6）压缩为可直接发用户的一条最小确认文案。
- 已补齐“用户只拍 1 题或 2 题时如何落盘”的最小规则，支持总控按单题 / 双题即时落 `decisions.md`。

### 体验设计审查

- 已继续明确前后端 must-close 项与待环境 / 待拍板后复核边界。
- 已进一步强调：环境未到位的真实 smoke 缺口不能直接计入开发未完成；规则未拍板的语义缺口不能提前判设计偏离。

### 阻塞协调

- 已确认新 owner 事实：`AppID=wxa6f615dcab1f984f` 已回传，公众平台账号可持续使用情况已确认=`是`。
- 已确认 owner 事实补充：主体类型=`个人`，拟用名称=`多米通告`，类目=`工具-预约/报名`，小程序备案=`审核中`，微信认证=`审核中`。
- 已明确：`AppID/主体` checklist 基本完成；当前 owner 侧主阻塞已切换为 `CloudBase 环境交接`，其次为 `管理员账号/初始化方式`。

## 本轮已确认结论

1. `AppID` 已到位，且公众平台账号可持续使用情况已确认=`是`；`AppID` 不再是当前主阻塞项。
2. 当前全局第一阻塞已明确切换为：CloudBase `dev/test` 环境交接包；第二阻塞为：管理员账号 / 初始化方式；第三类阻塞为：第一批规则（题 4 / 5 / 6）拍板。
3. 小程序关键写链路当前已形成三块可真实联调能力：资料读写、达人侧 `submit/withdraw`、既有发布侧动作 / 写接口；环境一到位即可优先做小程序首批 smoke。
4. 后台 must-close 当前不再是“没有证据”，而是“本地证据基本到位，待真实环境二次复核”；下一轮不应再重复铺 ready 图，而应盯真实链路闭环证据。
5. 第一批 3 题最小确认文案已可直接发用户；若用户仅确认其中 1 题或 2 题，总控应按产品经理规则局部落盘，不等待三题齐全。
6. `notice-bff.republish.deadlineAt` 仍只是候选 contract；在用户 / 产品拍板前继续保持前端规避、后端不升级为正式实现。

## 本轮新增 / 变化项

1. 新解除阻塞：`AppID` 已回传，公众平台账号可持续使用情况已确认。
2. owner 侧新增事实：小程序备案=`审核中`，微信认证=`审核中`。
3. 当前真实联调阻塞已进一步缩小为：CloudBase 环境交接包 + 管理员账号 / 初始化方式。
4. 规则拍板与环境交接现在可以拆成两条独立沟通线，不再混发。

## 仍待拍板 / 阻塞

1. CloudBase `dev/test` 环境 ID、已部署云函数清单、后台访问地址、是否已绑定目标环境、初始化完成状态、必要密钥 / 部署凭证。
2. 管理员账号 / 初始化方式：是否已有可用管理员账号、`ADMIN_INIT_USERNAME`、初始密码交付状态、首次改密链路、`login / me / logout / changePassword` 可执行闭环。
3. 题 4：后台动作请求最终枚举（高优先级）。
4. 题 5：举报记录对举报人的“处理中”可见性与举报状态正式英文 code（高优先级）。
5. 题 6：处罚状态模型与英文 code（高优先级）。
6. 题 3：非列表场景是否要求直跳单条报名详情，以及是否必须补正式 `applicationId` 聚合字段。
7. 题 2：`preferredView` 最终默认值。
8. 题 1：城市筛选候选源与默认城市来源。
9. `notice-bff.republish.deadlineAt` 是否进入正式 contract。
10. CloudBase 环境到位后的首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给前端

1. 继续把资料读写链路的 real-ready 事实补成前端回报中的单列条目，明确字段来源、写入返回与页面承接关系。
2. 保持 5 组高优先级证据逐项单列，不再合并总述；优先顺序继续为：通告详情页游客态 CTA、报名详情联系方式成对证据、发布页首创空态、达人名片首创空态、报名页首访空态。
3. 环境一到位，优先做 `bootstrap`、资料读写、达人侧 `submit/withdraw`、报名详情撤回入口的真实 smoke。

### 分配给后台

1. 继续盯 must-close 4 项，但重点改为：为真实环境二次复核准备最小闭环脚本与证据目录，不重复堆本地 ready 图。
2. 等环境 / 管理员账号到位后，优先完成 `admin-auth` 真实 `login / me / logout / changePassword` 与首次改密成功回工作区证据。
3. 继续按后端 sample 收口 normalizer 与只读 / 错误态展示，不自造字段。

### 分配给后端

1. 保持 smoke-ready，等待环境交接包；一到位就执行管理端与小程序首批真实 smoke。
2. 若 `notice-bff.republish.deadlineAt` 获拍板，立即升级为正式 contract、validator、handler 与 sample。
3. 继续维持正式口径与候选口径边界清晰，避免把候选方案写进共享事实源。

### 分配给产品经理

1. 维持第一批 3 题最小确认文案可直接发送状态。
2. 若用户仅确认 1 题或 2 题，按既定规则支持总控局部落盘。
3. 第二批 3 题继续登记，不提前混发。

### 分配给体验设计审查

1. 继续维护前后台 must-close 清单，把“本地已覆盖 / 待环境后复核”边界写得更清楚，方便总控判断何时启动第二轮复审。
2. 环境到位前，不再要求新增超出 must-close 范围的证据项。

### 分配给阻塞协调

1. 继续跟踪 `CloudBase 环境交接` 与 `管理员账号/初始化方式` 两张 checklist 的事实回传。
2. 若用户接下来先做规则拍板，请把第一批 3 题确认文案单独发出，不与环境交接问题混发。
3. 一旦环境交接包或管理员账号有任一项到位，第一时间回写 outbox。

## 会议后动作

1. 总控更新 `board.md`、`decisions.md`、`parking-lot.md` 和六个 `inbox/*.md`。
2. 六个角色继续按统一 `inbox/outbox` 机制执行。
3. 下轮会议触发条件：
   - 六个角色都完成当前收件箱任务
   - 环境交接包或管理员账号 / 初始化方式有新事实回传
   - 用户拍板第一批规则（题 4 / 5 / 6）中的任意一项
   - 后端完成首批真实 smoke 并回写结果
