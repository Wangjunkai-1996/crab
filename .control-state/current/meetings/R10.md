# KK_Crab 会议纪要 R10

- 会议形式：异步联合推进会
- 轮次：R10
- 时间：2026-03-17 17:01
- 主持：总控窗口
- 参与角色：前端、后台、后端、产品经理、体验设计审查、阻塞协调

## 本轮输入

1. 六个角色均已提交 R09 后续回报，且阻塞协调补交了新的环境推进回报。
2. 本轮重点从“候选 `dev` 环境出现后的环境交接判断”进一步推进到“确认当前真实第一阻塞已经从 owner/权限切到初始化脚本参数问题”。

## 各角色进展

### 前端

- 已重新核对资料读写 real-ready 链路、5 组高优先级替代证据与真实 smoke 切换点，当前前端回报已按逐项粒度收口。
- 已把 `bootstrap.service`、`runtime-config`、`cloud`、`request` 收敛为真实切换单点；初始化脚本修复并完成环境初始化后，即可不改页面直接切真实调用。
- 当前真实 smoke 主阻塞已收敛为：后端初始化脚本待修复重跑、管理员账号 / 初始化方式、题 4 / 5 / 6 未拍板。

### 后台

- 已补 `admin-auth` 真实 smoke 脚本、`.env` 示例和证据目录，must-close 4 项当前保持“本地证据已覆盖、待真实环境二次复核”的状态。
- 当前真实 `login / me / logout / changePassword` 与首次改密成功回工作区闭环仍待初始化脚本修复重跑完成后，再接后台访问地址与管理员账号执行。

### 后端

- 已新增 readiness 检查脚本与 npm alias，并升级 `environment-handoff` 文档到最新事实口径。
- 阻塞协调新增事实表明：`cloudbase-init` 权限已至少打通到可执行 `tcb:DescribeEnvs`，`init-database.mjs --apply` 已真正进入集合 / 索引阶段。
- 当前后端第一动作已从“等待环境事实”切换为“修正初始化脚本索引方向参数并重跑”；`notice-bff.republish.deadlineAt` 继续保持候选边界。

### 产品经理

- 第一批 3 题（题 4 / 5 / 6）最小确认文案继续保持可直接发送状态。
- 单题 / 双题拍板时的局部落盘规则已稳定，可支持总控不等三题齐全先落 `decisions.md`。

### 体验设计审查

- 已进一步稳定前端 5 项与后台 4 项 must-close 的边界表述。
- 已明确：环境未到位或初始化未闭环导致的真实 smoke 缺口不计为开发未完成；规则未拍板导致的语义缺口不计为设计偏离。

### 阻塞协调

- 已确认 `cloud1-4grxqg018586792d` 继续作为当前 `dev` 候选环境，且子用户 `cloudbase-init` 权限本轮已至少提升到可通过 `tcb:DescribeEnvs`。
- 已确认 `init-database.mjs --apply` 不再卡在环境访问权限，而是卡在第一个索引 `dm_users.idx_users_openid` 的创建阶段，CloudBase 返回 `UpdateTable InvalidParameter`。
- 已明确：当前第一阻塞不再是 owner 购买/开通/授权动作，而是开发侧初始化脚本把索引方向写成 `ASC/DESC`，与 CloudBase `UpdateTable` 所需的 `1/-1` 不匹配。

## 本轮已确认结论

1. `cloud1-4grxqg018586792d` 仍可视为当前 `dev` 候选环境，且环境访问权限已基本打通到可以执行 `tcb:DescribeEnvs`；当前不应再把“环境访问权限不足”作为主结论继续向用户施压。
2. 当前全局第一阻塞已更新为：`__BACKEND_WORKTREE__/scripts/init-database.mjs` 的索引方向参数与 CloudBase `UpdateTable` 接口不匹配，导致在创建 `dm_users.idx_users_openid` 时返回 `UpdateTable InvalidParameter`。
3. 管理员账号 / 初始化方式仍待确认，但已不再是当前第一阻塞；需先修复并重跑初始化脚本，确认集合 / 规则 / 索引 / `dm_configs` / `dm_admin_users` 的实际落地情况，再继续超管初始化闭环。
4. 前端、后台、后端本轮新增工作继续统一记为“真实 smoke 前置准备”；初始化脚本修复完成前，不再重复扩大本地 ready 证据范围。
5. 前端 5 项 must-close 与后台 4 项 must-close 当前统一记为“本地已覆盖，待环境后复核”；环境缺口或初始化未闭环不计为开发未完成，规则缺口不计为设计偏离。
6. 第一批 3 题最小确认文案继续作为独立沟通线待发，不与初始化脚本 / 管理员账号问题混发。
7. `notice-bff.republish.deadlineAt` 继续保持候选 contract 边界；未拍板前不升级为正式实现。

## 本轮新增 / 变化项

1. 新增确认环境执行事实：`init-database.mjs --apply` 已越过 `tcb:DescribeEnvs` 权限阶段，真正进入集合 / 索引初始化阶段。
2. 当前阻塞已从“owner/权限/环境访问”切换为“初始化脚本索引方向参数不符合 CloudBase 接口要求”。
3. owner 侧当前不需要继续研究腾讯云权限、服务器或环境购买；这类动作已不再是最前阻塞。
4. 后台与后端下一步都不再是等待抽象 handoff，而是等待后端修复脚本并回传重跑结果。

## 仍待拍板 / 阻塞

1. 初始化脚本阻塞：`__BACKEND_WORKTREE__/scripts/init-database.mjs` 当前将索引方向写为 `ASC/DESC`；需改为 CloudBase `UpdateTable` 所需的 `1/-1`，然后重新执行 `--apply`。
2. 初始化重跑结果待确认：脚本修复后，仍需确认业务集合、数据库规则、索引、`dm_configs`、`dm_admin_users`、已部署云函数清单、后台访问地址、必要密钥 / 部署凭证的最终交付状态。
3. 管理员账号 / 初始化方式：是否已有可用管理员测试账号、是否需要 runbook 首次初始化、`ADMIN_INIT_USERNAME`、初始密码交付状态、首次改密链路、可执行的 `login / me / logout / changePassword` 闭环。
4. 题 4：后台动作请求最终枚举（高优先级）。
5. 题 5：举报记录对举报人的“处理中”可见性与举报状态正式英文 code（高优先级）。
6. 题 6：处罚状态模型与英文 code（高优先级）。
7. 题 3：非列表场景是否要求直跳单条报名详情，以及是否必须补正式 `applicationId` 聚合字段。
8. 题 2：`preferredView` 最终默认值。
9. 题 1：城市筛选候选源与默认城市来源。
10. `notice-bff.republish.deadlineAt` 是否进入正式 contract。
11. 初始化闭环完成后的首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给前端

1. 继续保持资料读写、`bootstrap` 与达人侧写链路的 real-ready 状态，不把“候选 `dev` 环境可见”或“权限已打通”误写成“已完成真实环境初始化”。
2. 初始化脚本修复并重跑完成后，按顺序执行真实 smoke：`bootstrap` → 资料读写 → `submit/withdraw` → 报名详情撤回入口，并逐项回传证据路径。
3. 题 1 / 2 / 3 未拍板前，继续只保留兼容位与安全降级，不自造最终规则。

### 分配给后台

1. 继续维护 must-close 4 项的本地证据收口，不新增范围外证据要求；重点放在真实复核执行顺序、脚本与证据目录。
2. 初始化脚本修复并重跑完成后，一旦拿到后台访问地址与管理员账号 / 初始化方式，立即执行 `admin-auth` 真实 `login / me / logout / changePassword`，并补首次改密成功回工作区证据。
3. 题 4 / 5 / 6 未拍板前，继续只在 bridge / constants / normalizer 单点层保留临时口径，不扩散到页面层。

### 分配给后端

1. 立即修正 `__BACKEND_WORKTREE__/scripts/init-database.mjs` 的索引方向参数（`ASC/DESC` → `1/-1`），重新执行 `init-database.mjs --apply`，并回传集合 / 规则 / 索引 / `dm_configs` / `dm_admin_users` / 超管初始化的实际结果。
2. 若重跑后仍失败，必须回传具体失败集合 / 索引名、接口返回和当前脚本参数，不再只报抽象“环境未就绪”。
3. 若 `notice-bff.republish.deadlineAt` 获拍板，立即升级为正式 contract、validator、handler 与 sample；未拍板前继续维持候选边界。

### 分配给产品经理

1. 继续保持第一批 3 题最小确认文案可直接发送。
2. 若用户只拍板 1 题或 2 题，按既定规则立即给出可落 `decisions.md` 的正式结论文案。
3. 第二批 3 题继续登记，不与初始化脚本 / 账号问题混发。

### 分配给体验设计审查

1. 继续维护前端 5 项 + 后台 4 项 must-close 清单，统一标注“本地已覆盖 / 待环境后复核”或“待拍板后复核”。
2. 初始化未闭环前不扩大 must-close 范围；真实 smoke 证据到位后再决定第二轮复审顺序。
3. 不提前给最终体验通过结论。

### 分配给阻塞协调

1. 停止把当前主阻塞表述为 owner 权限或环境购买问题；改为跟踪“初始化脚本修复重跑结果 + 后台访问地址 + 管理员账号 / 初始化方式”。
2. 若用户先推进管理员账号初始化，给出最小行动清单与所需真实值列表；若用户先推进规则拍板，继续单独发送第一批 3 题文案。
3. 任何未在文档、会议纪要、回报中明确出现的内容，一律继续标记为待确认。

## 会议后动作

1. 总控更新 `board.md`、`parking-lot.md` 和相关 `inbox/*.md`。
2. 六个角色继续按统一 `inbox/outbox` 机制执行。
3. 本轮完成后，用户只需向六个角色窗口发送短句：`请读取收件箱并继续。`
4. 下轮会议触发条件：
   - 六个角色都完成当前收件箱任务
   - 初始化脚本重跑结果、后台访问地址或管理员账号 / 初始化方式有新事实回传
   - 用户拍板第一批规则（题 4 / 5 / 6）中的任意一项
   - 后端或后台完成首批真实 smoke 并回写结果
