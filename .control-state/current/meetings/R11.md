# KK_Crab 会议纪要 R11

- 会议形式：异步联合推进会
- 轮次：R11
- 时间：2026-03-17 17:38
- 主持：总控窗口
- 参与角色：前端、后台、后端、产品经理、体验设计审查、阻塞协调

## 本轮输入

1. 六个角色均已提交 R10 后续回报。
2. 本轮重点从“初始化脚本参数修复”推进到“确认脚本修复已完成、当前第一阻塞已更新为真实 `TENCENTCLOUD_SECRETID/TENCENTCLOUD_SECRETKEY` 缺失或仍为占位值”。

## 各角色进展

### 前端

- 已继续保持资料读写、`bootstrap`、runtime switch 与 5 组 must-close 证据的 real-ready 状态，未把“候选 `dev` 环境可见”或“权限已打通”误写成“初始化已完成”。
- 已新增一处符合既定口径的兼容修正：消息页点击报名类消息不再假直跳单条详情，而是安全降级进入“我的报名列表”。
- 本地 TypeScript 校验再次通过，当前等待初始化闭环后执行首批真实 smoke。

### 后台

- 已新增 `R10` must-close 状态总览、真实 smoke readiness 单点说明，并完善 `admin-auth` smoke 脚本的 `--help`、分步命令与 dry-run 入口。
- 已完成 `bash -n`、`--help`、多种 `--dry-run`、`npm run typecheck`、`npm run build` 等本地校验。
- 当前真实 `admin-auth` 闭环仍等待初始化闭环、后台访问地址与管理员账号 / 初始化方式。

### 后端

- 已修正 `__BACKEND_WORKTREE__/scripts/init-database.mjs` 的索引方向参数，`Direction` 已从 `ASC/DESC` 改为 CloudBase `UpdateTable` 所需的 `1/-1`。
- 修复后 dry-run 已正常输出初始化计划：14 个集合、14 组数据库规则、25 个索引、8 组 `dm_configs` seed 与超管初始化步骤。
- 实际 `--apply` 结果表明：当前 shell 缺少真实 `TENCENTCLOUD_SECRETID/TENCENTCLOUD_SECRETKEY`，历史命令中的对应值也是占位串，因此仍未进入任何集合创建成功阶段。
- 按后端本轮回报拆解：`collections`、`databaseRules`、`indexes`、`dm_configs`、`dm_admin_users` / 超管初始化当前都还未开始成功落地。

### 产品经理

- 第一批 3 题（题 4 / 5 / 6）最小确认文案保持可直接发送状态。
- 单题 / 双题拍板时的局部落盘规则保持不变，可继续支持总控不等三题齐全先落 `decisions.md`。

### 体验设计审查

- 继续维持前端 5 项与后台 4 项 must-close 的统一边界：当前都属于“本地已覆盖 / 待环境后复核”。
- 已明确：在初始化脚本未闭环、未拿到后台访问地址 / 账号、未执行真实 smoke 前，不应启动第二轮体验复审。

### 阻塞协调

- 已确认：当前主阻塞不再是 owner 权限、环境购买或环境找不到；owner 侧暂不需要继续补新的腾讯云权限或新环境。
- 已把环境侧主等待项统一为：真实 `TENCENTCLOUD_SECRETID/TENCENTCLOUD_SECRETKEY`、初始化脚本重跑结果、后台访问地址、管理员账号 / 初始化方式。

## 本轮已确认结论

1. `__BACKEND_WORKTREE__/scripts/init-database.mjs` 的索引方向参数问题已修复，不再是当前第一阻塞。
2. 当前全局第一阻塞已更新为：缺少真实可用的 `TENCENTCLOUD_SECRETID/TENCENTCLOUD_SECRETKEY`；当前 shell 中没有真实值，历史命令中的对应值也是占位串，导致 `init-database.mjs --apply` 仍无法完成初始化。
3. 在真实密钥到位并完成 `--apply` 前，不能宣称任何集合、数据库规则、索引、`dm_configs`、`dm_admin_users` 或超管已成功初始化。
4. 当前第二阻塞为：初始化闭环后的后台访问地址、管理员账号 / 初始化方式；第三类阻塞为：第一批规则（题 4 / 5 / 6）拍板。
5. `cloud1-4grxqg018586792d` 仍只代表当前 `dev` 候选环境；候选环境可见、访问权限打通、dry-run 输出计划，都不等于“环境初始化完成”。
6. 前端 5 项 must-close 与后台 4 项 must-close 继续统一记为“本地已覆盖 / 待环境后复核”；在真实 smoke 执行前不计为开发未完成或体验未通过。
7. `notice-bff.republish.deadlineAt` 继续保持候选 contract 边界；未拍板前不升级为正式实现。

## 本轮新增 / 变化项

1. 新解除阻塞：初始化脚本索引方向参数错误已修复。
2. 新确认阻塞：当前 `--apply` 的直接失败点已切换为真实腾讯云密钥缺失 / 占位值不可用。
3. 环境问题的表述进一步收紧为“真实密钥 + 初始化落地结果 + 后台访问地址 + 管理员账号”，不再泛化成“环境不通”或“权限还没开”。
4. 后台本轮已把真实 smoke 入口拆成 `standard / first-reset / full` 三段，本地执行说明已就绪；前端也已继续收口所有非列表直跳的安全降级策略。

## 仍待拍板 / 阻塞

1. 真实 `TENCENTCLOUD_SECRETID/TENCENTCLOUD_SECRETKEY`：需要环境侧 / owner 提供真实可用值，而不是占位串。
2. 初始化重跑结果：真实密钥到位后，仍需确认集合、数据库规则、索引、`dm_configs`、`dm_admin_users`、超管初始化、已部署云函数清单是否实际落地。
3. 后台访问地址：`ADMIN_WEB_BASE_URL`。
4. 管理员账号 / 初始化方式：是否已有可用管理员测试账号、是否继续沿用 `ADMIN_INIT_USERNAME=domi_admin`、初始密码交付状态、首次改密链路、可执行的 `login / me / logout / changePassword` 闭环。
5. 题 4：后台动作请求最终枚举（高优先级）。
6. 题 5：举报记录对举报人的“处理中”可见性与举报状态正式英文 code（高优先级）。
7. 题 6：处罚状态模型与英文 code（高优先级）。
8. 题 3：非列表场景是否要求直跳单条报名详情，以及是否必须补正式 `applicationId` 聚合字段。
9. 题 2：`preferredView` 最终默认值。
10. 题 1：城市筛选候选源与默认城市来源。
11. `notice-bff.republish.deadlineAt` 是否进入正式 contract。
12. 初始化闭环后的首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给前端

1. 继续保持资料读写、`bootstrap`、达人侧写链路与消息页“报名类消息安全降级到我的报名列表”的既定口径，不新增未拍板规则。
2. 真实密钥与初始化结果未回传前，不把任何候选环境或 dry-run 信号写成真实联调完成态。
3. 初始化闭环后，按顺序执行真实 smoke：`bootstrap` → 资料读写 → `submit/withdraw` → 报名详情撤回入口，并逐项回传证据路径。

### 分配给后台

1. 继续保持 must-close 4 项与真实 smoke 脚本入口 ready，不再扩范围堆本地证据。
2. 真实密钥与初始化结果到位后，一旦拿到后台访问地址与管理员账号 / 初始化方式，按 `standard -> first-reset -> full` 执行真实 `admin-auth` smoke，并补证据。
3. 题 4 / 5 / 6 未拍板前，继续只在 bridge / constants / normalizer 单点层保留临时口径。

### 分配给后端

1. 继续保持脚本修复结果，补一层显式检查：让 readiness / 初始化流程能明确识别“缺少真实密钥或仍为占位值”的状态，减少误判。
2. 一旦拿到真实 `TENCENTCLOUD_SECRETID/TENCENTCLOUD_SECRETKEY`，立即重新执行 `node __BACKEND_WORKTREE__/scripts/init-database.mjs --env cloud1-4grxqg018586792d --apply`，并逐项回传 `collections / databaseRules / indexes / dm_configs / dm_admin_users / seed-admin-users` 的实际结果。
3. 若 `notice-bff.republish.deadlineAt` 获拍板，立即升级为正式 contract、validator、handler 与 sample；未拍板前继续维持候选边界。

### 分配给产品经理

1. 继续保持第一批 3 题最小确认文案可直接发送。
2. 若用户只拍板 1 题或 2 题，按既定规则立即给出可落 `decisions.md` 的正式结论文案。
3. 第二批 3 题继续登记，不与真实密钥 / 初始化 / 账号问题混发。

### 分配给体验设计审查

1. 继续维护前端 5 项 + 后台 4 项 must-close 清单，统一标注“本地已覆盖 / 待环境后复核”或“待拍板后复核”。
2. 真实密钥未到位、初始化未闭环、后台访问地址 / 账号未到位前，不启动第二轮体验复审。
3. 不提前给最终体验通过结论。

### 分配给阻塞协调

1. 统一外部沟通口径：当前主阻塞是“真实腾讯云密钥 + 初始化重跑结果 + 后台访问地址 + 管理员账号 / 初始化方式”，不要再回退成环境购买或权限申请问题。
2. 若用户先推进管理员账号初始化，只索取最小真实值清单；若用户先推进规则拍板，继续单独发送第一批 3 题文案。
3. 一旦真实密钥、后台访问地址或管理员账号 / 初始化方式有任一新事实到位，第一时间回写 outbox。

## 会议后动作

1. 总控更新 `board.md`、`decisions.md`、`parking-lot.md` 和六个 `inbox/*.md`。
2. 六个角色继续按统一 `inbox/outbox` 机制执行。
3. 本轮完成后，用户只需向六个角色窗口发送短句：`请读取收件箱并继续。`
4. 下轮会议触发条件：
   - 六个角色都完成当前收件箱任务
   - 真实密钥、初始化重跑结果、后台访问地址或管理员账号 / 初始化方式有新事实回传
   - 用户拍板第一批规则（题 4 / 5 / 6）中的任意一项
   - 后端或后台完成首批真实 smoke 并回写结果
