# KK_Crab 会议纪要 R12

- 会议形式：异步联合推进会
- 轮次：R12
- 时间：2026-03-17 18:15
- 主持：总控窗口
- 参与角色：前端、后台、后端、产品经理、体验设计审查、阻塞协调

## 本轮输入

1. 六个角色均已提交 R11 后续回报。
2. 本轮重点从“确认真实密钥缺失”推进到“确认 owner 已授权使用现存有效密钥，但基于安全性不把明文写入同步文档；当前第一执行动作转为后端拿到安全转交后的密钥并立即重跑初始化”。

## 各角色进展

### 前端

- 已继续保持资料读写、`bootstrap`、runtime switch 与 5 组 must-close 证据的 real-ready 状态。
- 已再次确认：非列表场景不假直跳单条报名详情，消息页报名类消息继续安全降级进入“我的报名列表”。
- 本地 TypeScript 校验继续通过，当前等待初始化闭环后执行首批真实 smoke。

### 后台

- 已继续完善 `admin-auth` 真实 smoke 的分步入口、dry-run 与 readiness 文档。
- 当前真实 `admin-auth` 闭环仍等待初始化闭环、后台访问地址与管理员账号 / 初始化方式。

### 后端

- 已完成初始化脚本索引方向参数修复，dry-run 正常输出初始化计划。
- 已确认：此前 `--apply` 失败点已从“脚本参数”切到“真实密钥不可用 / 当前 shell 没有真实值”。
- 当前下一步不再是继续分析脚本，而是拿到已授权的现存有效密钥后立即重跑 `init-database.mjs --apply` 并回传逐项结果。

### 产品经理

- 第一批 3 题（题 4 / 5 / 6）最小确认文案保持可直接发送状态。
- 单题 / 双题拍板时的局部落盘规则保持不变。

### 体验设计审查

- 继续维持前端 5 项与后台 4 项 must-close 的统一边界：当前都属于“本地已覆盖 / 待环境后复核”。
- 已明确：在初始化重跑结果、后台访问地址 / 账号、真实 smoke 未到位前，不应启动第二轮体验复审。

### 阻塞协调

- 已确认新 owner 事实：用户已明确授权总控把现存有效的 `cloudbase-init` 子账号腾讯云密钥转交给后端，用于初始化重跑。
- 同时已确认：出于安全性，这些真实密钥不应写入 `board.md`、`meetings/*.md`、`parking-lot.md`、`inbox/outbox` 或仓库快照。
- 当前 owner 侧关于“是否存在真实密钥”的阻塞已解除；主等待项已切换为“后端实际使用已授权密钥完成初始化重跑并回传结果”。

## 本轮已确认结论

1. owner 侧关于“是否存在真实可用的 `cloudbase-init` 腾讯云密钥”的阻塞已解除；用户已明确授权后端使用现存有效密钥执行初始化重跑。
2. 基于跨电脑同步与仓库快照安全要求，真实密钥明文不写入 `board.md`、`meetings/*.md`、`parking-lot.md`、`inbox/outbox`、`.control-state/current/` 或其他同步文档。
3. 当前全局第一执行动作已更新为：后端通过安全转交拿到已授权密钥后，立即重跑 `node __BACKEND_WORKTREE__/scripts/init-database.mjs --env cloud1-4grxqg018586792d --apply`，并逐项回传 `collections / databaseRules / indexes / dm_configs / dm_admin_users / seed-admin-users` 的实际结果。
4. 在初始化重跑成功结果回传前，仍不能宣称任何集合、数据库规则、索引、`dm_configs`、`dm_admin_users` 或超管已成功初始化。
5. 当前第二阻塞为：初始化闭环后的后台访问地址 `ADMIN_WEB_BASE_URL` 与管理员账号 / 初始化方式；第三类阻塞为：第一批规则（题 4 / 5 / 6）拍板。
6. `cloud1-4grxqg018586792d` 继续只代表当前 `dev` 候选环境；候选环境可见、权限可访问、dry-run 输出计划、owner 确认存在密钥，都不等于“初始化完成”。
7. 前端 5 项 must-close 与后台 4 项 must-close 继续统一记为“本地已覆盖 / 待环境后复核”；在真实 smoke 执行前不计为开发未完成或体验未通过。
8. `notice-bff.republish.deadlineAt` 继续保持候选 contract 边界；未拍板前不升级为正式实现。

## 本轮新增 / 变化项

1. 新解除阻塞：owner 侧“是否有真实密钥”问题已解除。
2. 新增安全规则：真实密钥、初始密码等敏感值不进入同步文档与仓库快照。
3. 当前会议后的唯一关键执行动作已经非常明确：后端立即拿密钥重跑初始化，而不是继续追问新权限、新环境或新密钥。
4. 后台与前端继续保持 real-ready；体验审查继续不提前开第二轮复审。

## 仍待拍板 / 阻塞

1. 安全转交给后端的真实 `TENCENTCLOUD_SECRETID/TENCENTCLOUD_SECRETKEY` 尚未完成实际使用与初始化重跑回传。
2. 初始化重跑结果：仍需确认集合、数据库规则、索引、`dm_configs`、`dm_admin_users`、超管初始化、已部署云函数清单是否实际落地。
3. 后台访问地址：`ADMIN_WEB_BASE_URL`。
4. 管理员账号 / 初始化方式：是否已有可用管理员测试账号、是否继续沿用 `ADMIN_INIT_USERNAME=domi_admin`、初始密码交付状态、首次改密链路、可执行的 `login / me / logout / changePassword` 闭环。
5. 题 4：后台动作请求最终枚举（高优先级）。
6. 题 5：举报记录对举报人的“处理中”可见性与举报状态正式英文 code（高优先级）。
7. 题 6：处罚状态模型与英文 code（高优先级）。
8. 题 3：非列表场景是否要求直跳单条报名详情，以及是否必须补正式 `applicationId` 聚合字段。
9. 题 2：`preferredView` 最终默认值。
10. 题 1：城市筛选候选源与默认城市来源。
11. `notice-bff.republish.deadlineAt` 是否进入正式 contract。
12. 初始化闭环后的首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给前端

1. 继续保持资料读写、`bootstrap`、达人侧写链路与消息页“报名类消息安全降级到我的报名列表”的既定口径，不新增未拍板规则。
2. 初始化重跑结果未回传前，不把任何候选环境、权限可访问、dry-run 或“密钥已存在”信号写成真实联调完成态。
3. 初始化闭环后，按顺序执行真实 smoke：`bootstrap` → 资料读写 → `submit/withdraw` → 报名详情撤回入口，并逐项回传证据路径。

### 分配给后台

1. 继续保持 must-close 4 项与真实 smoke 分步入口 ready，不新增范围外本地证据要求。
2. 初始化重跑结果回传后，一旦拿到后台访问地址与管理员账号 / 初始化方式，按 `standard -> first-reset -> full` 执行真实 `admin-auth` smoke，并补证据。
3. 题 4 / 5 / 6 未拍板前，继续只在 bridge / constants / normalizer 单点层保留临时口径。

### 分配给后端

1. 当前第一动作是：在收到用户 / 总控通过安全渠道转交的真实 `TENCENTCLOUD_SECRETID/TENCENTCLOUD_SECRETKEY` 后，立即执行 `node __BACKEND_WORKTREE__/scripts/init-database.mjs --env cloud1-4grxqg018586792d --apply`。
2. 执行后必须逐项回传：`collections / databaseRules / indexes / dm_configs / dm_admin_users / seed-admin-users` 的实际结果；若仍失败，写清具体失败步骤、接口返回与当前参数。
3. 不得把明文密钥写入仓库文件、控制中心同步文档、截图或回报文件；回报里只写“已收到并使用安全转交密钥”或“仍未收到”。
4. `notice-bff.republish.deadlineAt` 继续保持候选边界；若获拍板，立即升级为正式 contract、validator、handler 与 sample。

### 分配给产品经理

1. 继续保持第一批 3 题最小确认文案可直接发送。
2. 若用户只拍板 1 题或 2 题，按既定规则立即给出可落 `decisions.md` 的正式结论文案。
3. 第二批 3 题继续登记，不与密钥转交 / 初始化 / 账号问题混发。

### 分配给体验设计审查

1. 继续维护前端 5 项 + 后台 4 项 must-close 清单，统一标注“本地已覆盖 / 待环境后复核”或“待拍板后复核”。
2. 初始化重跑结果、后台访问地址 / 账号、真实 smoke 未到位前，不启动第二轮体验复审。
3. 不提前给最终体验通过结论。

### 分配给阻塞协调

1. 统一外部沟通口径：当前 owner 已授权现存有效密钥，但明文不进同步文档；当前主等待项是“后端实际重跑结果 + 后台访问地址 + 管理员账号 / 初始化方式”。
2. 若用户先推进管理员账号初始化，只索取最小真实值清单；若用户先推进规则拍板，继续单独发送第一批 3 题文案。
3. 一旦初始化重跑结果、后台访问地址或管理员账号 / 初始化方式有任一新事实到位，第一时间回写 outbox。

## 会议后动作

1. 总控更新 `board.md`、`decisions.md`、`parking-lot.md` 和六个 `inbox/*.md`。
2. 六个角色继续按统一 `inbox/outbox` 机制执行。
3. 本轮完成后，用户只需向六个角色窗口发送短句：`请读取收件箱并继续。`
4. 由于本轮涉及真实密钥，后端窗口需要额外通过安全渠道单独拿到密钥，不能仅靠同步文档继续。
5. 下轮会议触发条件：
   - 六个角色都完成当前收件箱任务
   - 初始化重跑结果、后台访问地址或管理员账号 / 初始化方式有新事实回传
   - 用户拍板第一批规则（题 4 / 5 / 6）中的任意一项
   - 后端或后台完成首批真实 smoke 并回写结果
