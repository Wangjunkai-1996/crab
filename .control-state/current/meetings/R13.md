# KK_Crab 会议纪要 R13

- 会议形式：异步联合推进会
- 轮次：R13
- 时间：2026-03-17 18:44
- 主持：总控窗口
- 参与角色：前端、后台、后端、产品经理、体验设计审查、阻塞协调

## 本轮输入

1. 六个角色均已提交 R12 后续回报。
2. 本轮重点从“后端安全接收密钥并重跑初始化”推进到“确认初始化主体步骤已成功落地、当前主阻塞切换为 `storageRule` 套餐限制，以及后台真实联调所需 `ADMIN_WEB_BASE_URL` / 管理员账号闭环”。

## 各角色进展

### 前端

- 已继续保持资料读写、`bootstrap`、runtime switch 与 5 组 must-close 证据的 real-ready 状态。
- 已再次核对：非列表场景不假直跳单条报名详情，消息页报名类消息继续安全降级进入“我的报名列表”。
- 本地 TypeScript 校验继续通过；当前等待初始化剩余阻塞与真实 handoff 明确后执行首批真实 smoke。

### 后台

- 已继续维持 must-close 4 项、`dry-run -> standard -> first-reset -> full` 分步入口与真实 smoke readiness 文档。
- 已把黑名单页、日志页的游标分页统一迁移到 `useCursorPager` helper，进一步压缩页面层重复状态逻辑。
- 当前真实 `admin-auth` 闭环仍等待 `ADMIN_WEB_BASE_URL` 与管理员登录入口 / 账号闭环。

### 后端

- 已通过安全渠道接收并使用已授权密钥，实际执行 `node __BACKEND_WORKTREE__/scripts/init-database.mjs --env cloud1-4grxqg018586792d --apply`。
- 当前实际结果已明确：`collections 14/14 success`、`databaseRules 14/14 success`、`indexes 25/25 success`、`configSeeds 8 groups success`、首个超管 seed 已创建成功。
- 当前唯一明确失败步骤为 `storageRule`：CloudBase `ModifyStorageSafeRule` 返回 `OperationDenied.FreePackageDenied`，表明当前套餐无法执行该动作。
- 已额外发现：`dm_configs_count=16` 与本轮 `8` 组 seed 存在差异，需后续按 `groupKey` 级别做数据归属核查。

### 产品经理

- 第一批 3 题（题 4 / 5 / 6）最小确认文案继续保持可直接发送状态。
- 当前仍建议与初始化 / 后台地址 / 管理员账号问题分开发送，不混发。

### 体验设计审查

- 继续维持前端 5 项与后台 4 项 must-close 的统一边界：当前都属于“本地已覆盖 / 待环境后复核”。
- 已明确：尽管初始化主体步骤已回传成功结果，但在 `storageRule` 方案、后台访问地址 / 账号、真实 smoke 未到位前，不应启动第二轮体验复审。

### 阻塞协调

- 已确认：owner 侧“是否存在真实密钥”的问题已解除，不再是当前阻塞。
- 当前对外统一口径应切换为：初始化主体步骤已完成回传，但仍待 `storageRule` 套餐限制处理方案、`ADMIN_WEB_BASE_URL`、管理员账号 / 初始化方式。

## 本轮已确认结论

1. 后端已通过安全渠道接收并使用已授权密钥，初始化重跑已取得实质性结果：集合、数据库规则、索引、`dm_configs` seed、超管 seed 已成功执行。
2. 当前全局第一阻塞已从“密钥是否可用 / 初始化是否能启动”切换为：`storageRule` 无法落地，CloudBase 返回 `OperationDenied.FreePackageDenied`；需明确正式处理口径——接受其为当前环境已知例外，或升级套餐后重新执行。
3. 当前第二阻塞为：后台访问地址 `ADMIN_WEB_BASE_URL` 与管理员登录入口 / 管理员账号闭环；这直接阻塞后台 `admin-auth` 的真实 `login / me / logout / changePassword` 与首次改密验证。
4. 当前第三类待处理问题为：`dm_configs_count=16` 与本轮 `8` 组 seed 的差异，需要确认是环境历史数据、重复写入，还是需要补 `groupKey` 级幂等校验。
5. `cloud1-4grxqg018586792d` 仍只代表当前 `dev` 候选环境；即使主体初始化步骤已成功回传，在 `storageRule` 方案未明确前，仍不宣称“环境初始化全部完成”。
6. 前端 5 项 must-close 与后台 4 项 must-close 继续统一记为“本地已覆盖 / 待环境后复核”；在真实 smoke 执行前不计为开发未完成或体验未通过。
7. `notice-bff.republish.deadlineAt` 与 `preferredView` 默认值继续保持候选 / 待拍板边界；未拍板前不升级为正式业务规则。

## 本轮新增 / 变化项

1. 新解除阻塞：owner 侧真实密钥已安全转交且已被后端成功用于初始化重跑。
2. 新确认进展：集合、数据库规则、索引、配置 seed、超管 seed 已拿到实际执行结果。
3. 新确认阻塞：`storageRule` 在当前套餐下返回 `OperationDenied.FreePackageDenied`，成为当前第一阻塞。
4. 新确认数据关注项：`dm_configs_count=16` 与本轮 `8` 组 seed 存在差异，需单独核查。

## 仍待拍板 / 阻塞

1. `storageRule` 正式处理口径：接受当前免费套餐限制作为已知环境例外，还是升级套餐后重新执行存储安全规则。
2. 后台访问地址：`ADMIN_WEB_BASE_URL`。
3. 管理员账号 / 初始化方式：是否已有可用管理员测试账号、管理员真实登录入口、初始密码交付状态、首次改密链路、可执行的 `login / me / logout / changePassword` 闭环。
4. `dm_configs` 数据归属：现有 `16` 条数据是历史既有数据、重复 seed，还是需要增加 `groupKey` 级幂等校验。
5. 题 4：后台动作请求最终枚举（高优先级）。
6. 题 5：举报记录对举报人的“处理中”可见性与举报状态正式英文 code（高优先级）。
7. 题 6：处罚状态模型与英文 code（高优先级）。
8. 题 3：非列表场景是否要求直跳单条报名详情，以及是否必须补正式 `applicationId` 聚合字段。
9. 题 2：`preferredView` 最终默认值。
10. 题 1：城市筛选候选源与默认城市来源。
11. `notice-bff.republish.deadlineAt` 是否进入正式 contract。
12. 初始化闭环后的首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给前端

1. 继续保持资料读写、`bootstrap`、达人侧写链路与消息页“报名类消息安全降级到我的报名列表”的既定口径，不新增未拍板规则。
2. 在 `storageRule` 处理口径、函数 handoff 与初始化结果明确后，按顺序执行真实 smoke：`bootstrap` → 资料读写 → `submit/withdraw` → 报名详情撤回入口，并逐项回传证据路径。
3. 在真实 smoke 前，继续只保留兼容位与安全降级，不把主体初始化成功回传误写成整体验证已完成。

### 分配给后台

1. 继续保持 must-close 4 项与真实 smoke 分步入口 ready，不新增范围外本地证据要求。
2. 一旦拿到 `ADMIN_WEB_BASE_URL` 与管理员登录入口 / 账号闭环，按 `standard -> first-reset -> full` 执行真实 `admin-auth` smoke，并补入证据目录。
3. 继续保持黑名单页、日志页等列表页的游标分页逻辑收口在 `useCursorPager` helper，不扩散页面层状态重复代码。
4. 题 4 / 5 / 6 未拍板前，继续只在 bridge / constants / normalizer 单点层保留临时口径。

### 分配给后端

1. 继续保留当前初始化结果与结构化回传能力，不重复落无效重跑；优先补充 `storageRule` 的处理建议与可重试入口。
2. 按 `groupKey` 级别核查 `dm_configs` 的 `16` 条现有数据，回传是历史既有数据、重复 seed，还是当前幂等策略仍需收口。
3. 在 `ADMIN_WEB_BASE_URL` 到位后，按 `__BACKEND_WORKTREE__/scripts/coordination/r08-real-smoke-template.md` 与后台线协同执行 `admin-auth` 首批真实 smoke。
4. `notice-bff.republish.deadlineAt` 与 `preferredView` 默认值未拍板前，继续维持候选边界；如获拍板，立即升级为正式 contract / sample / 实现口径。

### 分配给产品经理

1. 继续保持第一批 3 题最小确认文案可直接发送。
2. 若用户只拍板 1 题或 2 题，按既定规则立即给出可落 `decisions.md` 的正式结论文案。
3. 第二批 3 题继续登记，不与 `storageRule` / 后台地址 / 管理员账号问题混发。

### 分配给体验设计审查

1. 继续维护前端 5 项 + 后台 4 项 must-close 清单，统一标注“本地已覆盖 / 待环境后复核”或“待拍板后复核”。
2. `storageRule` 方案、后台访问地址 / 账号、真实 smoke 未到位前，不启动第二轮体验复审。
3. 不提前给最终体验通过结论。

### 分配给阻塞协调

1. 统一对外口径：当前已不需要再追真实密钥；当前主等待项是“`storageRule` 处理方案 + `ADMIN_WEB_BASE_URL` + 管理员账号 / 初始化方式”。
2. 若用户先推进管理员账号初始化，只索取最小真实值清单；若用户先推进规则拍板，继续单独发送第一批 3 题文案。
3. 一旦 `storageRule` 处理方案、后台访问地址或管理员账号 / 初始化方式有任一新事实到位，第一时间回写 outbox。

## 会议后动作

1. 总控更新 `board.md`、`decisions.md`、`parking-lot.md` 和六个 `inbox/*.md`。
2. 六个角色继续按统一 `inbox/outbox` 机制执行。
3. 本轮完成后，用户只需向六个角色窗口发送短句：`请读取收件箱并继续。`
4. 下轮会议触发条件：
   - 六个角色都完成当前收件箱任务
   - `storageRule` 处理方案、后台访问地址或管理员账号 / 初始化方式有新事实回传
   - 用户拍板第一批规则（题 4 / 5 / 6）中的任意一项
   - 后端或后台完成首批真实 smoke 并回写结果
