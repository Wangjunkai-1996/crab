# KK_Crab 会议纪要 R15

- 会议形式：异步联合推进会
- 轮次：R15
- 时间：2026-03-18 11:01
- 主持：总控窗口
- 参与角色：前端、后台、后端、产品经理、体验设计审查、阻塞协调

## 本轮输入

1. 六个角色均已提交 R14 后续回报。
2. 本轮重点从“确认 `storageRule` 套餐限制与后台联调入口缺失”推进到“由总控正式裁决当前 `dev` 环境对 `storageRule` 的处理口径，并把主阻塞切换到 `ADMIN_WEB_BASE_URL` 与管理员账号闭环”。

## 各角色进展

### 前端

- 已继续保持资料读写、`bootstrap`、runtime switch 与 5 组 must-close 证据的 real-ready 状态。
- 已继续确认：非列表场景不假直跳单条报名详情，消息页报名类消息继续安全降级进入“我的报名列表”。
- 当前等待 `storageRule` 口径、函数 handoff 与真实 smoke 条件齐备后执行首批真实 smoke。

### 后台

- 已继续把游标分页状态统一收口到 `useCursorPager` / `createCursorPagerState`，减少页面层与 store 的重复状态声明。
- 已继续维持 must-close 4 项与真实 smoke 分步入口 `dry-run -> standard -> first-reset -> full` 的 ready 状态。
- 当前真实 `admin-auth` smoke 仍等待 `ADMIN_WEB_BASE_URL`、管理员登录入口 / 账号闭环。

### 后端

- 已新增 `retry-storage-rule.mjs`、`audit-config-groups.mjs`、`dedupe-config-groups.mjs` 三个独立入口，分别覆盖 `storageRule` 重试、`dm_configs` 分组审计与重复清理。
- 已修正 `init-database.mjs` 在 secret 模式下对 `dm_configs` 与超管 seed 的查询路径，避免后续继续制造新的重复记录。
- 当前已确认：`collections 14/14 success`、`databaseRules 14/14 success`、`indexes 25/25 success`、`configSeeds 8 groups success`、首个超管 seed 创建成功。
- 当前唯一明确失败步骤仍是 `storageRule`：CloudBase `ModifyStorageSafeRule` 返回 `OperationDenied.FreePackageDenied`。
- 已确认 `dm_configs_count=16` 不是模板外历史脏组，而是 8 个正式 `groupKey` 各重复 1 次；dry-run 已证明按 `oldest` 策略可安全去重。

### 产品经理

- 第一批 3 题（题 4 / 5 / 6）最小确认文案继续保持可直接发送状态。
- 当前仍建议与后台地址 / 管理员账号问题分开发送，不混发。

### 体验设计审查

- 继续维持前端 5 项与后台 4 项 must-close 的统一边界：当前都属于“本地已覆盖 / 待环境后复核”。
- 已明确：在后台访问地址 / 账号与真实 smoke 未到位前，不应启动第二轮体验复审。

### 阻塞协调

- 已确认：用户当前无法自行处理 `storageRule` 套餐限制问题，因此该项不应继续作为用户待办抛回。
- 当前对外统一口径应切换为：总控已给出当前 `dev` 环境 `storageRule` 处理口径；现主等待项为 `ADMIN_WEB_BASE_URL` 与管理员账号闭环。

## 本轮已确认结论

1. 总控正式裁决：对当前 `dev` 候选环境 `cloud1-4grxqg018586792d`，`storageRule` 的 `OperationDenied.FreePackageDenied` 暂按“`dev` 已知环境例外”处理，不再阻塞当前 `dev` 环境的首批联调与真实 smoke 准备。
2. 上述裁决只适用于当前 `dev` 候选环境的阶段性联调，不代表生产环境或未来正式环境可以跳过存储安全规则；若后续升级套餐或切换到支持该能力的新环境，应执行 `__BACKEND_WORKTREE__/scripts/retry-storage-rule.mjs` 补齐该项。
3. 当前全局第一阻塞已从 `storageRule` 套餐限制切换为：`ADMIN_WEB_BASE_URL`、管理员真实登录入口 / 管理员账号闭环；在其到位前，后台真实 `admin-auth` 的 `login / me / changePassword / logout` 与首次改密验证无法启动。
4. 当前第二类待处理问题为：`dm_configs` 的 8 组重复数据清理策略；已知重复来自 8 个正式 `groupKey` 各重复 1 次，且 dry-run 已证明可以安全按 `oldest` 策略去重，但在总控本轮未把它升级为当前第一阻塞。
5. `cloud1-4grxqg018586792d` 仍只代表当前 `dev` 候选环境；即使主体初始化步骤已成功回传且 `storageRule` 已按例外处理，仍不宣称“正式环境初始化全部完成”。
6. 前端 5 项 must-close 与后台 4 项 must-close 继续统一记为“本地已覆盖 / 待环境后复核”；在真实 smoke 执行前不计为开发未完成或体验未通过。
7. `notice-bff.republish.deadlineAt` 与 `preferredView` 默认值继续保持候选 / 待拍板边界；未拍板前不升级为正式业务规则。

## 本轮新增 / 变化项

1. 新拍板环境口径：`storageRule` 在当前 `dev` 候选环境按“已知环境例外”处理。
2. 主阻塞前移：`ADMIN_WEB_BASE_URL` 与管理员账号闭环成为当前第一阻塞。
3. `dm_configs` 重复问题从“来源不明”推进到“来源已明确、清理策略待定”。
4. 后端环境脚本体系进一步完整：已具备重试、审计、去重三个独立入口。

## 仍待拍板 / 阻塞

1. 后台访问地址：`ADMIN_WEB_BASE_URL`。
2. 管理员账号 / 初始化方式：是否已有可用管理员测试账号、管理员真实登录入口、初始密码交付状态、首次改密链路、可执行的 `login / me / logout / changePassword` 闭环。
3. `dm_configs` 重复清理策略：保留最早 8 条、删除后插入的 8 条，还是采用其他策略。
4. 题 4：后台动作请求最终枚举（高优先级）。
5. 题 5：举报记录对举报人的“处理中”可见性与举报状态正式英文 code（高优先级）。
6. 题 6：处罚状态模型与英文 code（高优先级）。
7. 题 3：非列表场景是否要求直跳单条报名详情，以及是否必须补正式 `applicationId` 聚合字段。
8. 题 2：`preferredView` 最终默认值。
9. 题 1：城市筛选候选源与默认城市来源。
10. `notice-bff.republish.deadlineAt` 是否进入正式 contract。
11. 首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给前端

1. 继续保持资料读写、`bootstrap`、达人侧写链路与消息页“报名类消息安全降级到我的报名列表”的既定口径，不新增未拍板规则。
2. 当前 `storageRule` 已不再作为当前 `dev` 环境首批联调阻塞；在函数 handoff 与真实 smoke 条件齐备后，按顺序执行真实 smoke：`bootstrap` → 资料读写 → `submit/withdraw` → 报名详情撤回入口，并逐项回传证据路径。
3. 在真实 smoke 前，继续只保留兼容位与安全降级，不把主体初始化成功回传误写成整体验证已完成。

### 分配给后台

1. 继续维护 must-close 4 项与真实 smoke 分步入口，不新增范围外本地证据要求。
2. 一旦拿到 `ADMIN_WEB_BASE_URL` 与管理员登录入口 / 账号闭环，按 `standard -> first-reset -> full` 执行真实 `admin-auth` smoke，并补入证据目录。
3. 继续保持黑名单页、日志页等列表页的游标分页逻辑收口在 `useCursorPager` helper，不扩散页面层状态重复代码。
4. 题 4 / 5 / 6 未拍板前，继续只在 bridge / constants / normalizer 单点层保留临时口径。

### 分配给后端

1. 继续保留当前初始化结果与结构化回传能力，不重复做无效重跑；当前 `storageRule` 对 `dev` 环境已按已知例外处理，不再作为首批 smoke 阻塞项反复上报。
2. 在 `ADMIN_WEB_BASE_URL` 到位后，按 `__BACKEND_WORKTREE__/scripts/coordination/r08-real-smoke-template.md` 与后台线协同执行 `admin-auth` 首批真实 smoke。
3. 继续保留 `retry-storage-rule.mjs` 作为后续升级套餐或切换环境后的补执行入口。
4. 继续保留 `audit-config-groups.mjs` 与 `dedupe-config-groups.mjs`；若总控后续确认 `oldest` 去重策略，再执行 `dedupe-config-groups --strategy oldest --apply` 并回传结果。
5. `notice-bff.republish.deadlineAt` 与 `preferredView` 默认值未拍板前，继续维持候选边界；如获拍板，立即升级为正式 contract / sample / 实现口径。

### 分配给产品经理

1. 继续保持第一批 3 题最小确认文案可直接发送。
2. 若用户只拍板 1 题或 2 题，按既定规则立即给出可落 `decisions.md` 的正式结论文案。
3. 第二批 3 题继续登记，不与后台地址 / 管理员账号问题混发。

### 分配给体验设计审查

1. 继续维护前端 5 项 + 后台 4 项 must-close 清单，统一标注“本地已覆盖 / 待环境后复核”或“待拍板后复核”。
2. 后台访问地址 / 账号、真实 smoke 未到位前，不启动第二轮体验复审。
3. 不提前给最终体验通过结论。

### 分配给阻塞协调

1. 统一对外口径：当前 `storageRule` 已按当前 `dev` 环境已知例外处理；现主等待项是 `ADMIN_WEB_BASE_URL` + 管理员账号 / 初始化方式。
2. 若用户先推进管理员账号初始化，只索取最小真实值清单；若用户先推进规则拍板，继续单独发送第一批 3 题文案，不与后台地址问题混发。
3. 一旦后台访问地址或管理员账号 / 初始化方式有任一新事实到位，第一时间回写 outbox。

## 会议后动作

1. 总控更新 `board.md`、`decisions.md`、`parking-lot.md` 和六个 `inbox/*.md`。
2. 六个角色继续按统一 `inbox/outbox` 机制执行。
3. 本轮完成后，用户只需向六个角色窗口发送短句：`请读取收件箱并继续。`
4. 下轮会议触发条件：
   - 六个角色都完成当前收件箱任务
   - 后台访问地址或管理员账号 / 初始化方式有新事实回传
   - 用户拍板第一批规则（题 4 / 5 / 6）中的任意一项
   - 后端或后台完成首批真实 smoke 并回写结果
