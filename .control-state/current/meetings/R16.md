# KK_Crab 会议纪要 R16

- 会议形式：异步联合推进会
- 轮次：R16
- 时间：2026-03-18 11:56
- 主持：总控窗口
- 参与角色：前端、后台、后端、产品经理、体验设计审查、阻塞协调

## 本轮输入

1. 六个角色均已提交 R15 后续回报。
2. 本轮重点从“`storageRule` 已完成总控裁决”推进到“确认当前无新增正式拍板、继续把主阻塞锁定在 `ADMIN_WEB_BASE_URL` 与管理员账号闭环，并把 `dm_configs` 来源核清结论固化进总控状态”。

## 各角色进展

### 前端

- 本轮未新增业务规则，已继续复核资料读写、`bootstrap`、runtime switch 与消息页安全降级链路，避免把“主体初始化步骤已成功回传”误写成“整体验证已完成”。
- 已确认当前小程序真实 smoke 顺序继续保持为：`bootstrap` → 资料读写 → `submit/withdraw` → 报名详情撤回入口。
- 本地 `tsc --noEmit` 已再次通过；当前仍等待函数 handoff 与真实 smoke 条件齐备。

### 后台

- 已新增 `__ADMIN_WORKTREE__/admin-web/src/utils/admin-labels.ts`，并把风险等级、身份类型、审核状态、举报状态等页面展示标签继续收口到单点工具。
- 已继续维持 must-close 4 项与真实 smoke 分步入口 `dry-run -> standard -> first-reset -> full` 的 ready 状态。
- 已完成本地 `npm run typecheck` 与 `npm run build`；当前真实 `admin-auth` smoke 仍等待 `ADMIN_WEB_BASE_URL`、管理员登录入口与账号闭环。

### 后端

- 已继续保留 `retry-storage-rule.mjs`、`audit-config-groups.mjs`、`dedupe-config-groups.mjs` 三个脚本入口，并维持 `init-database.mjs` 的 secret 模式查询修复。
- 已把 `storageRule` 的最新口径同步到 `__BACKEND_WORKTREE__/scripts/coordination/environment-handoff.md`：当前 `dev` 候选环境的 `OperationDenied.FreePackageDenied` 继续保留为失败事实，但按已拍板口径记为“已知环境例外”。
- 已核清 `dm_configs_count=16` 的来源：即 8 个正式 `groupKey` 各重复 1 次；`oldest` 策略 dry-run 继续保持 `duplicateGroupCount=8`、`totalDeleteCount=8`、`safeGroupCount=8`、`unsafeGroupCount=0`。

### 产品经理

- 第一批 3 题（题 4 / 5 / 6）最小确认文案继续保持可直接发送状态。
- 已明确单题 / 双题拍板时的局部落盘规则，当前仍建议与后台访问地址 / 管理员账号问题分开发送。
- 第二批 3 题仍继续单独登记，未混入第一批确认单。

### 体验设计审查

- 已继续维持前端 5 项 must-close 与后台 4 项 must-close 的统一边界：当前都属于“本地已覆盖 / 待环境后复核”或“待拍板后复核”。
- 已明确：在函数 handoff、`ADMIN_WEB_BASE_URL`、管理员账号与真实 smoke 证据未到位前，不应启动第二轮体验复审。

### 阻塞协调

- 已确认 owner / 环境侧口径继续稳定：`storageRule` 不再作为当前 `dev` 联调阻塞项向用户重复追问。
- 当前主等待项继续收敛为：`ADMIN_WEB_BASE_URL`、管理员真实登录入口、管理员账号 / 初始化方式、首次改密链路是否可执行。
- 第一批 3 题当前仍为待发送 / 待用户回复状态，继续建议与环境阻塞分开发送。

## 本轮已确认结论

1. `R15` 已拍板结论继续有效：对当前 `dev` 候选环境 `cloud1-4grxqg018586792d`，`storageRule` 的 `OperationDenied.FreePackageDenied` 继续按“已知环境例外”处理，不阻塞当前 `dev` 联调准备。
2. 本轮无新增产品规则或业务规则拍板；`decisions.md` 继续沿用 `R15` 已确认内容，不新增题 4 / 5 / 6、题 1 / 2 / 3 或 `notice-bff.republish.deadlineAt` 的正式决议。
3. `dm_configs` 的事实边界已进一步收紧：当前已确认其差异来源不是历史额外脏组，而是 8 个正式 `groupKey` 各重复 1 次；当前待处理只剩“是否执行 `oldest` 去重 apply”的策略拍板。
4. 当前全局第一阻塞继续保持为：`ADMIN_WEB_BASE_URL`、管理员真实登录入口 / 管理员账号闭环；在其到位前，后台真实 `admin-auth` 的 `login / me / changePassword / logout` 与首次改密验证无法启动。
5. 前端 5 项 must-close 与后台 4 项 must-close 继续统一记为“本地已覆盖 / 待环境后复核”；在真实 smoke 执行前，不计为开发未完成或体验未通过。
6. 当前仍不宣称“正式环境初始化全部完成”；`cloud1-4grxqg018586792d` 继续只代表当前首批联调可用的 `dev` 候选环境。

## 本轮新增 / 变化项

1. 前端再次通过 `tsc --noEmit`，并再次确认不把候选环境可联调误写为整体验证完成。
2. 后台新增 `admin-labels` 单点标签工具，进一步减少页面层展示规则重复。
3. 后端已把 `dm_configs` 来源核清从“待确认”推进到“事实已明确，策略待拍板”。
4. 当前总控结论变为：本轮主要是状态收口与口径稳定，无新增正式拍板。

## 仍待拍板 / 阻塞

1. 后台访问地址：`ADMIN_WEB_BASE_URL`。
2. 管理员账号 / 初始化方式：是否已有可用管理员测试账号、管理员真实登录入口、初始密码交付状态、首次改密链路、可执行的 `login / me / logout / changePassword` 闭环。
3. `dm_configs` 重复清理策略：是否执行“保留最早 8 条、删除后插入的 8 条”的 `oldest` 去重 apply。
4. 题 4：后台动作请求最终枚举（高优先级）。
5. 题 5：举报记录对举报人的“处理中”可见性与举报状态正式英文 code（高优先级）。
6. 题 6：处罚状态模型与英文 code（高优先级）。
7. 题 3：非列表场景是否要求直跳单条报名详情，以及是否必须补正式 `applicationId` 聚合字段。
8. 题 2：`preferredView` 最终默认值。
9. 题 1：城市筛选候选源与默认城市来源。
10. `notice-bff.republish.deadlineAt` 是否进入正式 contract。
11. 首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给前端

1. 继续保持资料读写、runtime 切换、消息页安全降级与 5 组 must-close 替代证据不回退，不为“没有新环境事实”做无效改动。
2. 在函数 handoff 与真实 smoke 条件齐备后，按固定顺序执行真实 smoke：`bootstrap` → 资料读写 → `submit/withdraw` → 报名详情撤回入口，并逐项回传证据路径。
3. 题 1 / 2 / 3 未拍板前，继续保持城市筛选只保留字段位、`preferredView` 兼容 `null`、非列表场景降级到“我的报名列表”，不新增 `applicationId` 聚合假设。

### 分配给后台

1. 继续维护 must-close 4 项与真实 smoke 分步入口，不新增范围外本地证据要求。
2. 继续把展示标签、重复状态文案与分页状态收口在单点 helper / composable，不把未拍板枚举扩散到页面层。
3. 一旦拿到 `ADMIN_WEB_BASE_URL`、管理员登录入口与账号闭环，按 `standard -> first-reset -> full` 执行真实 `admin-auth` smoke，并补入证据目录。
4. 题 4 / 5 / 6 未拍板前，继续只在 bridge / constants / normalizer 单点层保留临时口径。

### 分配给后端

1. `dm_configs` 数据来源已核清；后续不再以“来源不明”反复上报，当前只保留去重策略待确认这一层。
2. 继续保留当前初始化结果与结构化回传能力，不做无意义重跑；当前 `storageRule` 对 `dev` 环境继续按已知例外处理。
3. 在 `ADMIN_WEB_BASE_URL` 与管理员账号链路到位后，按 `__BACKEND_WORKTREE__/scripts/coordination/r08-real-smoke-template.md` 与后台线协同执行 `admin-auth` 首批真实 smoke。
4. 若总控后续确认 `oldest` 去重策略，再执行 `dedupe-config-groups --strategy oldest --apply` 并回传结果。
5. `notice-bff.republish.deadlineAt` 与 `preferredView` 默认值未拍板前，继续维持候选边界。

### 分配给产品经理

1. 继续保持第一批 3 题最小确认文案可直接发送。
2. 把第二批 3 题（城市筛选、`preferredView`、非列表场景 / `applicationId` 聚合）压缩成可直接让用户回复的最小确认文案，但暂不与第一批或环境阻塞问题混发。
3. 若用户只拍板 1 题或 2 题，按既定规则立即给出可直接写入 `decisions.md` 的正式结论文案。

### 分配给体验设计审查

1. 继续维护前端 5 项 + 后台 4 项 must-close 清单，统一标注“本地已覆盖 / 待环境后复核”或“待拍板后复核”。
2. 将“启动第二轮体验复审的前置条件”继续压缩为总控可直接复用的 checklist，但在真实 smoke 证据到位前，不启动第二轮复审。
3. 不提前给最终体验通过结论。

### 分配给阻塞协调

1. 统一对外口径继续保持：当前 `storageRule` 已按当前 `dev` 环境已知例外处理；现主等待项是 `ADMIN_WEB_BASE_URL` + 管理员账号 / 初始化方式。
2. 若用户先推进管理员账号初始化，只索取最小真实值清单；若用户先推进规则拍板，继续单独发送第一批 3 题文案，不与后台地址问题混发。
3. 补一版可直接向环境 / 后台索取管理员最小真实值的正式文案，便于总控或用户需要时直接转发。
4. 一旦后台访问地址或管理员账号 / 初始化方式有任一新事实到位，第一时间回写 outbox。

## 会议后动作

1. 总控更新 `board.md`、`parking-lot.md`、`decisions.md`（仅刷新元信息，不新增拍板）和六个 `inbox/*.md`。
2. 六个角色继续按统一 `inbox/outbox` 机制执行。
3. 本轮完成后，用户只需向六个角色窗口发送短句：`请读取收件箱并继续。`
4. 下轮会议触发条件：
   - 六个角色都完成当前收件箱任务
   - 后台访问地址或管理员账号 / 初始化方式有新事实回传
   - 用户拍板第一批规则（题 4 / 5 / 6）中的任意一项
   - 后端或后台完成首批真实 smoke 并回写结果
