# KK_Crab 会议纪要 R18

- 会议形式：异步联合推进会
- 轮次：R18
- 时间：2026-03-18 12:30
- 主持：总控窗口
- 参与角色：前端、后台、后端、产品经理、体验设计审查、阻塞协调

## 本轮输入

1. 六个角色均已提交 R17 后续回报。
2. 本轮重点不是新增技术或产品拍板，而是确认各线已经吸收 `R17` 的责任边界：当前仍应优先向后台 / 环境 / 部署持有方索取管理员联调最小真实值，而不是继续把这批事实当成用户先拍板事项。

## 各角色进展

### 前端

- 本轮未新增业务代码，已继续复核资料读写、`bootstrap`、runtime switch 与消息页安全降级链路，避免把候选 `dev` 环境可联调误写成“整体验证已完成”。
- 已继续确认：当前小程序真实 smoke 顺序保持为 `bootstrap` → 资料读写 → `submit/withdraw` → 报名详情撤回入口。
- 本地 `tsc --noEmit` 已再次通过；当前仍等待函数 handoff 与真实 smoke 条件齐备。

### 后台

- 已继续保留 `usePageLoadState` 与 `admin-labels` 的单点收口，不再向页面层扩散通用 `loading/error` 状态与展示标签逻辑。
- 已继续维持 must-close 4 项与真实 smoke 分步入口 `dry-run -> standard -> first-reset -> full` 的 ready 状态。
- 当前真实 `admin-auth` smoke 仍未执行；直接原因仍是 `ADMIN_WEB_BASE_URL`、管理员登录入口与账号闭环未到位。

### 后端

- 已继续保留 `retry-storage-rule.mjs`、`audit-config-groups.mjs`、`dedupe-config-groups.mjs` 三个脚本入口，并维持 `init-database.mjs` 的 secret 模式查询修复。
- 已继续确认：`storageRule` 在当前 `dev` 环境按“已知环境例外”处理，保留失败事实但不再作为首批 smoke 阻塞项反复上报。
- 已继续确认：`dm_configs_count=16` 的来源是 8 个正式 `groupKey` 各重复 1 次，`oldest` 策略 dry-run 结果继续稳定。

### 产品经理

- 第一批 3 题（题 4 / 5 / 6）最小确认文案继续保持可直接发送状态。
- 第二批 3 题（城市筛选、`preferredView`、非列表场景 / `applicationId` 聚合）最小确认文案继续保持 ready，但本轮仍只备稿、不自动发送。
- 已继续明确：若用户只拍板 1 题或 2 题，只局部落盘已确认项。

### 体验设计审查

- 已继续维持前端 5 项 must-close 与后台 4 项 must-close 的统一边界：当前都属于“本地已覆盖 / 待环境后复核”或“待拍板后复核”。
- 已继续明确：在函数 handoff、`ADMIN_WEB_BASE_URL`、管理员账号与真实 smoke 证据未到位前，不应启动第二轮体验复审。

### 阻塞协调

- 已继续强调责任边界：`ADMIN_WEB_BASE_URL`、后台真实登录入口、可用管理员账号链路、初始密码交付方式、首次改密链路等，当前首先是后台 / 环境 / 部署持有方应回传的真实事实。
- 已继续保留可直接向后台 / 环境 / 部署持有方索取管理员联调最小真实值的正式文案。
- 当前主等待项继续保持为：`ADMIN_WEB_BASE_URL`、管理员真实登录入口、管理员账号 / 初始化方式、首次改密链路是否可执行。

## 本轮已确认结论

1. `R15`、`R16`、`R17` 已拍板结论继续有效：当前 `dev` 候选环境 `cloud1-4grxqg018586792d` 上，`storageRule` 的 `OperationDenied.FreePackageDenied` 继续按“已知环境例外”处理，不阻塞当前 `dev` 联调准备。
2. 本轮无新增产品规则或业务规则拍板；`decisions.md` 继续不新增题 4 / 5 / 6、题 1 / 2 / 3 或 `notice-bff.republish.deadlineAt` 的正式业务决议。
3. `R17` 新增的总控协作决议继续有效：`ADMIN_WEB_BASE_URL`、后台真实登录入口、当前可用管理员账号、初始密码交付方式与首次改密链路等联调最小真实值，优先由后台 / 环境 / 部署持有方直接回传；仅当存在多个可选方案时，再由用户做最小确认。
4. `dm_configs` 的事实边界继续保持收紧：当前已确认其差异来源不是历史额外脏组，而是 8 个正式 `groupKey` 各重复 1 次；待处理只剩“是否执行 `oldest` 去重 apply”的策略拍板。
5. 当前全局第一阻塞继续保持为：`ADMIN_WEB_BASE_URL`、管理员真实登录入口 / 管理员账号闭环；在其到位前，后台真实 `admin-auth` 的 `login / me / changePassword / logout` 与首次改密验证无法启动。
6. 前端 5 项 must-close 与后台 4 项 must-close 继续统一记为“本地已覆盖 / 待环境后复核”；在真实 smoke 执行前，不计为开发未完成或体验未通过。

## 本轮新增 / 变化项

1. 本轮无新增正式技术拍板或产品拍板。
2. 各线对 `R17` 的责任边界已对齐：管理员联调最小真实值继续视为“待后台 / 环境 / 部署持有方回传”的事实项，而不是先让用户拍板。
3. 当前总控重点继续保持为：稳定事实边界，等待真实联调入口，而不是扩大待拍板范围。

## 仍待拍板 / 阻塞

1. 后台访问地址：`ADMIN_WEB_BASE_URL`。
2. 管理员账号 / 初始化方式：是否已有可用管理员测试账号、管理员真实登录入口、初始密码交付状态、首次改密链路、可执行的 `login / me / logout / changePassword` 闭环。
3. `dm_configs` 重复清理策略：是否执行“保留最早 8 条、删除后插入的 8 条”的 `oldest` 去重 apply。
4. 题 4：后台动作请求最终枚举（高优先级）。
5. 题 5：举报记录对举报人的“处理中”可见性与举报状态正式英文 code（高优先级）。
6. 题 6：处罚状态模型与英文 code（高优先级）。
7. 题 3：非列表场景是否要求直跳单条报名详情，以及是否必须补正式 `applicationId` 聚合字段。
8. 题 2：`preferredView` 最终默认值。
9. 题 1：城市筛选候选源与默认城市来源。
10. `notice-bff.republish.deadlineAt` 是否进入正式 contract。
11. 首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给前端

1. 继续保持资料读写、runtime 切换、消息页安全降级与 5 组 must-close 替代证据不回退，不为“没有新环境事实”做无效改动。
2. 在函数 handoff 与真实 smoke 条件齐备后，按固定顺序执行真实 smoke：`bootstrap` → 资料读写 → `submit/withdraw` → 报名详情撤回入口，并逐项回传证据路径。
3. 题 1 / 2 / 3 未拍板前，继续保持城市筛选只保留字段位、`preferredView` 兼容 `null`、非列表场景降级到“我的报名列表”，不新增 `applicationId` 聚合假设。

### 分配给后台

1. 继续维护 must-close 4 项与真实 smoke 分步入口，不新增范围外本地证据要求。
2. 继续把展示标签、通用 `loading/error` 状态、分页状态与重复状态文案收口在单点 helper / composable，不把未拍板枚举扩散到页面层。
3. 若你本线已掌握 `ADMIN_WEB_BASE_URL`、后台真实登录入口、当前可用管理员账号链路中的任一明确事实，请直接回写 outbox；若尚未掌握，请按字段逐项写清“缺哪一项、由谁提供”，不要只笼统写“环境未到位”。
4. 一旦拿到 `ADMIN_WEB_BASE_URL`、管理员登录入口与账号闭环，按 `standard -> first-reset -> full` 执行真实 `admin-auth` smoke，并补入证据目录。

### 分配给后端

1. `dm_configs` 数据来源已核清；后续不再以“来源不明”反复上报，当前只保留去重策略待确认这一层。
2. 继续保留当前初始化结果与结构化回传能力，不做无意义重跑；当前 `storageRule` 对 `dev` 环境继续按已知例外处理。
3. 若你本线已掌握管理员联调最小真实值中的任一明确事实（后台地址、登录入口、可用账号链路、初始密码交付方式、首次改密链路），请直接回写 outbox；若尚未掌握，请按字段逐项写清仍需哪一方回传。
4. 在 `ADMIN_WEB_BASE_URL` 与管理员账号链路到位后，按 `__BACKEND_WORKTREE__/scripts/coordination/r08-real-smoke-template.md` 与后台线协同执行 `admin-auth` 首批真实 smoke。
5. 若总控后续确认 `oldest` 去重策略，再执行 `dedupe-config-groups --strategy oldest --apply` 并回传结果。

### 分配给产品经理

1. 继续保持第一批 3 题最小确认文案可直接发送。
2. 继续保留第二批 3 题最小确认文案 ready，但在总控明确切换批次前不自动发送。
3. 若用户拍板任意 1 题或 2 题，按既定规则立即给出可直接写入 `decisions.md` 的正式结论文案。

### 分配给体验设计审查

1. 继续维护前端 5 项 + 后台 4 项 must-close 清单，统一标注“本地已覆盖 / 待环境后复核”或“待拍板后复核”。
2. 将“启动第二轮体验复审的前置条件”继续保持为总控可直接复用的 checklist；在真实 smoke 证据到位前，不启动第二轮复审。
3. 不提前给最终体验通过结论。

### 分配给阻塞协调

1. 统一对外口径继续保持：当前 `storageRule` 已按当前 `dev` 环境已知例外处理；现主等待项是 `ADMIN_WEB_BASE_URL` + 管理员账号 / 初始化方式。
2. 继续保留“向后台 / 环境 / 部署持有方索取管理员联调最小真实值”的正式文案，必要时可直接供总控转发。
3. 若用户先推进规则拍板，继续单独发送第一批 3 题文案；若后台访问地址或管理员账号 / 初始化方式有任一新事实到位，第一时间回写 outbox。

## 会议后动作

1. 总控更新 `board.md`、`decisions.md`、`parking-lot.md` 和六个 `inbox/*.md`。
2. 六个角色继续按统一 `inbox/outbox` 机制执行。
3. 本轮完成后，用户只需向六个角色窗口发送短句：`请读取收件箱并继续。`
4. 下轮会议触发条件：
   - 六个角色都完成当前收件箱任务
   - 后台访问地址或管理员账号 / 初始化方式有新事实回传
   - 用户拍板第一批规则（题 4 / 5 / 6）中的任意一项
   - 后端或后台完成首批真实 smoke 并回写结果
