# KK_Crab 会议纪要 R19

- 会议形式：异步联合推进会
- 轮次：R19
- 时间：2026-03-18 13:55
- 主持：总控窗口
- 参与角色：前端、后台、后端、产品经理、体验设计审查、阻塞协调

## 本轮输入

1. 六个角色均已提交 R18 后续回报。
2. 本轮重点是把“`ADMIN_WEB_BASE_URL` + 管理员账号闭环”这个大阻塞继续拆实，明确哪些已经由后台 / 后端给出了默认技术结论，哪些才是真正还缺的外部事实。

## 各角色进展

### 前端

- 本轮未新增业务代码，已继续保持资料读写、`bootstrap`、runtime switch 与消息页安全降级链路稳定。
- 本地 `tsc --noEmit` 已再次通过；当前仍等待函数 handoff 与真实 smoke 条件齐备。

### 后台

- 已更新 `__ADMIN_WORKTREE__/admin-web/evidence/r10/admin-auth-handoff-fields.md`，把登录页实际入口与后台访问地址现状补成明确技术结论。
- 已确认代码侧登录路由定义为 `/login`，且工程未配置额外 `base`；因此在站点部署于域名根路径的前提下，默认登录入口技术上成立为 `${ADMIN_WEB_BASE_URL}/login`。
- 当前仍未执行真实 `admin-auth` smoke；直接原因继续收口为：缺 `ADMIN_WEB_BASE_URL`，且部署侧尚未确认真实入口是否就是默认 `/login`。

### 后端

- 已给出首批 `admin-auth` 联调默认方案：当前默认直接沿用已创建 seed 账号 `domi_admin` 作为首个联调账号，不把“额外标准账号 / 额外首次改密账号”作为首批 smoke 前置阻塞项。
- 已确认当前 seed 账号角色为 `super_admin`、状态为 `active`，且初始化时写入 `mustResetPassword=true`；因此在当前实现上，首次改密链路是可执行的。
- 当前管理员联调最小真实值已进一步收口为 4 项：`ADMIN_WEB_BASE_URL` 未提供、真实登录入口未提供、seed 初始密码是否已生成未确认、seed 初始密码是否已通过安全渠道交付未确认。

### 产品经理

- 第一批与第二批最小确认文案继续保持 ready。
- 当前仍建议不把规则题与 `admin-auth` 联调阻塞问题混发。

### 体验设计审查

- 继续维持前端 5 项 must-close 与后台 4 项 must-close 的统一边界：当前都属于“本地已覆盖 / 待环境后复核”或“待拍板后复核”。
- 在真实 `admin-auth` smoke 未启动前，不启动第二轮体验复审。

### 阻塞协调

- 已继续强调：老板 / 外部协调窗口不再承接“登录入口判断、seed 账号方案、首次改密可执行性”这类开发线本可自行收口的技术事实。
- 当前只接真正落到外部部署地址、访问权限与安全交付链路的事项。

## 本轮已确认结论

1. `R15` 至 `R18` 已拍板结论继续有效：当前 `dev` 候选环境 `cloud1-4grxqg018586792d` 上，`storageRule` 的 `OperationDenied.FreePackageDenied` 继续按“已知环境例外”处理，不阻塞当前 `dev` 联调准备。
2. 本轮无新增产品规则拍板；`decisions.md` 继续不新增题 4 / 5 / 6、题 1 / 2 / 3 或 `notice-bff.republish.deadlineAt` 的正式业务决议。
3. 本轮新增技术协调结论：当前首批 `admin-auth` 联调默认方案采用已创建 seed 账号 `domi_admin`；不再把“额外标准账号 / 额外首次改密账号”作为首批 smoke 前置阻塞。
4. 本轮新增技术协调结论：当前后台代码侧默认登录入口技术结论为 `${ADMIN_WEB_BASE_URL}/login`；若部署后真实入口不是该路径，后台线应直接回传真实路径并自行调整 smoke 脚本。
5. 当前第一阻塞已从笼统的“后台地址 + 管理员账号闭环”收口为 4 项：`ADMIN_WEB_BASE_URL`、真实登录入口最终确认、seed 初始密码是否已生成、seed 初始密码是否已通过安全渠道交付。
6. 老板 / 外部协调窗口当前只处理：外部部署地址、访问权限、安全交付、或多方案选择；开发线本可自行收口的技术事实不再抛给老板。

## 本轮新增 / 变化项

1. 后台首次把默认登录入口的技术事实写明确了，不再只说“环境未到位”。
2. 后端首次把首批联调账号方案写明确了，不再把“额外账号”当成前置阻塞。
3. 当前 `admin-auth` 联调阻塞已显著收口，剩下的外部缺口比上一轮更少、更具体。

## 仍待拍板 / 阻塞

1. `ADMIN_WEB_BASE_URL`。
2. 部署侧对真实登录入口是否就是 `${ADMIN_WEB_BASE_URL}/login` 的最终确认；若不是，需直接回传真实入口路径。
3. seed 初始密码是否已生成。
4. seed 初始密码是否已通过安全渠道交付。
5. `dm_configs` 重复清理策略：是否执行“保留最早 8 条、删除后插入的 8 条”的 `oldest` 去重 apply。
6. 题 4：后台动作请求最终枚举（高优先级）。
7. 题 5：举报记录对举报人的“处理中”可见性与举报状态正式英文 code（高优先级）。
8. 题 6：处罚状态模型与英文 code（高优先级）。
9. 题 3：非列表场景是否要求直跳单条报名详情，以及是否必须补正式 `applicationId` 聚合字段。
10. 题 2：`preferredView` 最终默认值。
11. 题 1：城市筛选候选源与默认城市来源。
12. `notice-bff.republish.deadlineAt` 是否进入正式 contract。
13. 首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给前端

1. 继续保持资料读写、runtime 切换、消息页安全降级与 5 组 must-close 替代证据不回退。
2. 在函数 handoff 与真实 smoke 条件齐备后，按固定顺序执行真实 smoke：`bootstrap` → 资料读写 → `submit/withdraw` → 报名详情撤回入口，并逐项回传证据路径。

### 分配给后台

1. 继续维护 must-close 4 项与真实 smoke 分步入口。
2. 下轮回报必须明确回答两件事：
   - 代码侧默认登录入口是否仍坚持 `${ADMIN_WEB_BASE_URL}/login`
   - 当前后台访问地址现状到底是“未部署 / 未知持有方”还是“已部署但地址未回写”
3. 若后续得到真实入口路径且不是 `/login`，你本线直接调整 smoke 脚本，不再把这件事继续挂成笼统阻塞。

### 分配给后端

1. 下轮回报必须明确回答三件事：
   - 是否最终沿用 seed 账号 `domi_admin` 作为首批联调账号
   - seed 初始密码是否已生成
   - seed 初始密码是否已通过安全渠道交付；若未交付，当前缺的是“生成”还是“交付动作”
2. 继续保留当前初始化结果与结构化回传能力，不做无意义重跑。
3. 若总控后续确认 `oldest` 去重策略，再执行 `dedupe-config-groups --strategy oldest --apply` 并回传结果。

### 分配给产品经理

1. 继续保持第一批 3 题最小确认文案可直接发送。
2. 继续保留第二批 3 题最小确认文案 ready，但在总控明确切换批次前不自动发送。

### 分配给体验设计审查

1. 继续维护前端 5 项 + 后台 4 项 must-close 清单。
2. 在真实 `admin-auth` smoke 启动前，不启动第二轮体验复审。

### 分配给阻塞协调（老板/外部协调）

1. 当前只承接 2 类外部事项：`ADMIN_WEB_BASE_URL` 的外部部署地址事实、以及 seed 初始密码的安全交付链路确认。
2. 不再把登录入口判断、seed 账号方案、首次改密可执行性抛给老板。
3. 若后台 / 后端继续未把上述技术事实说清，总控下轮继续压回对应开发线，不再转老板待办。

## 会议后动作

1. 总控更新 `board.md`、`decisions.md`、`parking-lot.md` 和六个 `inbox/*.md`。
2. 六个角色继续按统一 `inbox/outbox` 机制执行。
3. 本轮完成后，用户只需向六个角色窗口发送短句：`请读取收件箱并继续。`
4. 下轮会议触发条件：
   - 六个角色都完成当前收件箱任务
   - `ADMIN_WEB_BASE_URL`、真实登录入口、seed 密码生成/交付状态有任一新事实回传
   - 用户拍板第一批规则（题 4 / 5 / 6）中的任意一项
   - 后端或后台完成首批真实 smoke 并回写结果
