# KK_Crab 会议纪要 R20

- 会议形式：异步联合推进会
- 轮次：R20
- 时间：2026-03-18 13:58
- 主持：总控窗口
- 参与角色：前端、后台、后端、产品经理、体验设计审查、阻塞协调

## 本轮输入

1. 六个角色均已提交 R19 后续回报。
2. 本轮重点是继续打穿 `admin-auth` 联调阻塞，并把它从 4 项外部缺口进一步收口到 3 项。

## 各角色进展

### 前端

- 本轮未新增业务代码，继续保持资料读写、`bootstrap`、runtime switch 与消息页安全降级链路稳定。
- 本地 `tsc --noEmit` 继续通过；当前仍等待函数 handoff 与真实 smoke 条件齐备。

### 后台

- 已把后台真实 `admin-auth` smoke 的最小联调方案从“默认双账号”收口到“默认 seed 单账号 `domi_admin` + 可选独立标准账号兼容”，并把该口径统一落到脚本、环境模板、执行说明和 handoff 文档。
- 已确认默认技术结论仍为 `/login`，且脚本已支持通过 `ADMIN_WEB_LOGIN_PATH` 覆盖真实入口；一旦部署侧回传非 `/login` 路径即可直接实跑。
- 当前仍未执行真实 `admin-auth` smoke；后台侧阻塞继续收口为：缺 `ADMIN_WEB_BASE_URL`、真实登录入口外部确认、seed 初始密码安全交付状态未确认。

### 后端

- 已继续明确：首批 `admin-auth` 联调默认方案最终沿用 seed 账号 `domi_admin`，不再把额外标准账号 / 首次改密账号作为首批 smoke 前置阻塞。
- 已新增确认：seed 初始密码状态已收口为 `已生成=是`、`已通过安全渠道交付=未确认`；当前缺的是“交付动作”，不是“是否生成”。
- 当前管理员联调外部缺口已进一步收口为 3 项：`ADMIN_WEB_BASE_URL`、真实登录入口最终确认、seed 初始密码是否已通过安全渠道交付。

### 产品经理

- 第一批与第二批最小确认文案继续保持 ready。
- 当前仍建议不把规则题与 `admin-auth` 联调阻塞问题混发。

### 体验设计审查

- 继续维持前端 5 项 must-close 与后台 4 项 must-close 的统一边界：当前都属于“本地已覆盖 / 待环境后复核”或“待拍板后复核”。
- 在真实 `admin-auth` smoke 未启动前，不启动第二轮体验复审。

### 阻塞协调

- 已继续强调：老板 / 外部协调窗口当前只承接外部部署地址、真实入口外部确认与 seed 初始密码安全交付事项。
- 当前不再把“登录入口判断、seed 账号方案、首次改密可执行性、seed 密码是否已生成”这类技术事实抛给老板。

## 本轮已确认结论

1. `R15` 至 `R19` 已拍板结论继续有效：当前 `dev` 候选环境 `cloud1-4grxqg018586792d` 上，`storageRule` 的 `OperationDenied.FreePackageDenied` 继续按“已知环境例外”处理，不阻塞当前 `dev` 联调准备。
2. 本轮无新增产品规则拍板；`decisions.md` 继续不新增题 4 / 5 / 6、题 1 / 2 / 3 或 `notice-bff.republish.deadlineAt` 的正式业务决议。
3. 本轮新增技术事实：后端已确认 seed 账号 `domi_admin` 的初始密码已生成；当前剩余外部缺口不再包含“是否已生成”。
4. 当前第一阻塞已从 4 项进一步收口为 3 项：`ADMIN_WEB_BASE_URL`、真实登录入口最终确认、seed 初始密码是否已通过安全渠道交付。
5. 老板 / 外部协调窗口当前只处理：外部部署地址、真实入口外部确认、安全交付；开发线本可自行收口的技术事实不再抛给老板。

## 本轮新增 / 变化项

1. 后台把真实 smoke 最小联调方案进一步统一到“默认 seed 单账号 + 可选兼容”。
2. 后端确认 seed 密码已生成，外部缺口比上一轮再少 1 项。
3. 当前 `admin-auth` 联调阻塞已经缩到最后 3 个外部事实。

## 仍待拍板 / 阻塞

1. `ADMIN_WEB_BASE_URL`。
2. 部署侧对真实登录入口是否就是 `${ADMIN_WEB_BASE_URL}/login` 的最终确认；若不是，需直接回传真实入口路径 / `ADMIN_WEB_LOGIN_PATH`。
3. seed 初始密码是否已通过安全渠道交付。
4. `dm_configs` 重复清理策略：是否执行“保留最早 8 条、删除后插入的 8 条”的 `oldest` 去重 apply。
5. 题 4：后台动作请求最终枚举（高优先级）。
6. 题 5：举报记录对举报人的“处理中”可见性与举报状态正式英文 code（高优先级）。
7. 题 6：处罚状态模型与英文 code（高优先级）。
8. 题 3：非列表场景是否要求直跳单条报名详情，以及是否必须补正式 `applicationId` 聚合字段。
9. 题 2：`preferredView` 最终默认值。
10. 题 1：城市筛选候选源与默认城市来源。
11. `notice-bff.republish.deadlineAt` 是否进入正式 contract。
12. 首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给前端

1. 继续保持资料读写、runtime 切换、消息页安全降级与 5 组 must-close 替代证据不回退。
2. 在函数 handoff 与真实 smoke 条件齐备后，按固定顺序执行真实 smoke：`bootstrap` → 资料读写 → `submit/withdraw` → 报名详情撤回入口，并逐项回传证据路径。

### 分配给后台

1. 继续维护 must-close 4 项与真实 smoke 分步入口。
2. 下轮回报必须明确回答两件事：
   - 当前是否仍坚持默认入口结论 `${ADMIN_WEB_BASE_URL}/login`
   - 若部署侧回传真实入口不是 `/login`，你本线是否已准备好直接改用 `ADMIN_WEB_LOGIN_PATH`
3. 继续把后台访问地址现状说清楚：到底是“未部署 / 未知持有方”，还是“已部署但地址未回写”。
4. 一旦拿到 `ADMIN_WEB_BASE_URL`、真实登录入口与 seed 密码交付状态，按实际账号方案执行真实 `admin-auth` smoke。

### 分配给后端

1. 下轮回报不必再重复“密码是否已生成未知”这种旧口径；你本线已确认生成完成。
2. 当前只需继续盯 1 个后端相关外部缺口：seed 初始密码是否已通过安全渠道交付；若未交付，请明确当前缺的是哪一个外部动作。
3. 继续保留当前初始化结果与结构化回传能力，不做无意义重跑。
4. 若总控后续确认 `oldest` 去重策略，再执行 `dedupe-config-groups --strategy oldest --apply` 并回传结果。

### 分配给产品经理

1. 继续保持第一批 3 题最小确认文案可直接发送。
2. 继续保留第二批 3 题最小确认文案 ready，但在总控明确切换批次前不自动发送。

### 分配给体验设计审查

1. 继续维护前端 5 项 + 后台 4 项 must-close 清单。
2. 在真实 `admin-auth` smoke 启动前，不启动第二轮体验复审。

### 分配给阻塞协调（老板/外部协调）

1. 当前只承接 3 项外部事实中的 2 类事项：`ADMIN_WEB_BASE_URL` / 真实入口外部确认，以及 seed 初始密码安全交付。
2. 不再把 seed 密码“是否已生成”当成老板待确认项。
3. 若后台 / 后端继续未把技术事实说清，总控下轮继续压回对应开发线，不再转老板待办。

## 会议后动作

1. 总控更新 `board.md`、`decisions.md`、`parking-lot.md` 和六个 `inbox/*.md`。
2. 六个角色继续按统一 `inbox/outbox` 机制执行。
3. 本轮完成后，用户只需向六个角色窗口发送短句：`请读取收件箱并继续。`
4. 下轮会议触发条件：
   - 六个角色都完成当前收件箱任务
   - `ADMIN_WEB_BASE_URL`、真实登录入口、seed 密码交付状态有任一新事实回传
   - 用户拍板第一批规则（题 4 / 5 / 6）中的任意一项
   - 后端或后台完成首批真实 smoke 并回写结果
