# KK_Crab 会议纪要 R21

- 会议形式：异步专项阻塞会
- 轮次：R21
- 时间：2026-03-18 15:13
- 主持：总控窗口
- 参与角色：后台、后端、阻塞协调

## 本轮输入

1. `R20` 之后，后台、后端、阻塞协调均已提交新回报。
2. 用户明确提出疑问：`ADMIN_WEB_BASE_URL` 与“真实登录入口最终确认”到底由谁负责，不允许继续模糊漂移。

## 各角色最新事实

### 后台

- 已明确：当前仍坚持默认技术结论 `${ADMIN_WEB_BASE_URL}/login`。
- 已明确：若部署后真实入口不是 `/login`，本线已准备好直接改用 `ADMIN_WEB_LOGIN_PATH`，脚本与环境模板均已就位。
- 当前未执行真实 `admin-auth` smoke 的原因继续收口为 3 项外部缺口：`ADMIN_WEB_BASE_URL`、真实登录入口最终确认、seed 初始密码安全交付。

### 后端

- 已明确：首批联调账号继续沿用 seed 账号 `domi_admin`。
- 已明确：seed 初始密码 `已生成=是`；当前缺的是“是否已通过安全渠道交付”的外部事实。
- 后端当前不再承担 `ADMIN_WEB_BASE_URL` 与真实登录入口最终确认的 owner 身份。

### 阻塞协调

- 已明确：老板 / 外部协调窗口当前只承接 3 类外部事项：`ADMIN_WEB_BASE_URL`、真实登录入口外部部署确认、seed 初始密码安全交付状态。
- 已明确：登录入口技术判断、seed 账号方案、首次改密可执行性、seed 密码是否已生成，都不再算老板待判断事项。

## 本轮拍板结论

1. `ADMIN_WEB_BASE_URL` 的 owner 明确为：`阻塞协调（老板/外部协调）` 去对接部署 / 环境持有方索取并回写；不是后台 owner，也不是后端 owner。
2. “真实登录入口最终确认”的 owner 同样明确为：`阻塞协调（老板/外部协调）` 去对接部署侧确认；不是后台 owner，也不是后端 owner。
3. 后台对这两项只承担“技术消费方”责任：默认按 `${ADMIN_WEB_BASE_URL}/login`；若外部确认不是该路径，后台直接改用 `ADMIN_WEB_LOGIN_PATH` 并执行 smoke。
4. 后端对这两项只承担“技术配合方”责任：提供 seed 账号链路、密码生成/交付状态与后端联调配合；不承担后台部署地址或真实入口路径的 owner 身份。
5. 若阻塞协调暂时仍拿不到 `ADMIN_WEB_BASE_URL` 或真实入口，后续回报必须写清楚到底是：`后台尚未部署`、`已部署但地址未回写`、还是 `部署持有方待确认`；不允许再笼统写“后端 / 环境侧提供”。
6. 当前管理员联调仍只剩 3 项外部缺口：`ADMIN_WEB_BASE_URL`、真实登录入口最终确认、seed 初始密码是否已通过安全渠道交付。

## 本轮新增 / 变化项

1. 将用户困惑点正式落盘：`ADMIN_WEB_BASE_URL` 与真实登录入口最终确认，责任已从“模糊的环境侧”改为明确归属 `阻塞协调（老板/外部协调）`。
2. 后台与后端后续回报里，可以继续把这两项写成阻塞，但必须标注 owner 为 `阻塞协调 / 部署侧`，不能再写成自己要去搞定。

## 仍待阻塞

1. `ADMIN_WEB_BASE_URL`。
2. 真实登录入口最终确认；若不是 `${ADMIN_WEB_BASE_URL}/login`，需回传真实 `ADMIN_WEB_LOGIN_PATH`。
3. seed 初始密码是否已通过安全渠道交付。
4. `dm_configs` 是否执行 `oldest` 去重 apply。
5. 题 4 / 5 / 6 高优先级规则拍板。
6. 题 1 / 2 / 3 与 `notice-bff.republish.deadlineAt` 拍板。
7. 首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给后台

1. 继续保持 real smoke ready，不回退脚本、模板、证据目录与 must-close 口径。
2. 下轮回报里，`ADMIN_WEB_BASE_URL` 与真实入口项必须标注 owner 为 `阻塞协调 / 部署侧`，不要再写成 `后端 / 环境侧提供`。
3. 一旦阻塞协调回传地址、真实入口与 seed 密码交付状态，立即执行真实 `admin-auth` smoke 并补证据。

### 分配给后端

1. 下轮回报继续只盯 seed 初始密码安全交付状态这 1 个后端相关外部缺口。
2. `ADMIN_WEB_BASE_URL` 与真实登录入口项可以继续保留在“跨线阻塞”里，但 owner 必须标注为 `阻塞协调 / 部署侧`。
3. 一旦地址与入口闭环，立即与后台协同执行 `admin-auth` 真实 smoke。

### 分配给阻塞协调

1. 本轮你是 `ADMIN_WEB_BASE_URL` 与真实登录入口最终确认的明确 owner。
2. 下轮回报必须二选一回清楚：
   - 已拿到实际值并回传；或
   - 明确写出当前卡在哪个外部事实：`未部署`、`地址未回写`、`部署持有方待确认`。
3. 若真实入口不是 `/login`，必须直接回传真实路径 / `ADMIN_WEB_LOGIN_PATH`。
4. 继续跟进 seed 初始密码安全交付状态，但不在同步文档中写明文密码。

## 会议结论摘要

- `ADMIN_WEB_BASE_URL`：归 `阻塞协调（老板/外部协调）` 去拿。
- 真实登录入口最终确认：归 `阻塞协调（老板/外部协调）` 去拿。
- 后台：负责消费这些值并执行 smoke。
- 后端：负责 seed 账号链路与后端联调配合，不负责后台部署地址。
