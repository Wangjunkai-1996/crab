# KK_Crab 会议纪要 R24

- 会议形式：异步事实收敛会
- 轮次：R24
- 时间：2026-03-18 17:14
- 主持：总控窗口
- 参与角色：后台、后端、阻塞协调

## 本轮输入

1. 阻塞协调提交了新的 owner 回报。
2. 本轮出现关键新事实：`admin-web` 已实际发布并可访问，`ADMIN_WEB_BASE_URL` 与真实登录入口不再缺失。
3. 同时出现新的现象事实：当前 `cloud1-4grxqg018586792d` 控制台云函数列表为空，登录提交报 `PERMISSION_DENIED`。

## 各角色最新事实

### 阻塞协调

- 已确认 `admin-web` 已通过 CloudBase 控制台完成发布。
- 已确认后台地址为 `https://cloud1-4grxqg018586792d-1412048057.tcloudbaseapp.com/`。
- 已确认真实登录入口当前为 `/login`，即 `https://cloud1-4grxqg018586792d-1412048057.tcloudbaseapp.com/login` 可访问。
- 已确认匿名登录开关为开启状态。
- 已新增关键现象事实：当前 `cloud1-4grxqg018586792d` 控制台云函数列表显示为空，至少未见 `admin-auth` 部署结果。

### 后台

- 之前卡住的“静态托管发布权限 / 账号 / 登录态”问题，已被新事实解除。
- 当前后台侧不再缺 `ADMIN_WEB_BASE_URL` 与真实登录入口。
- 当前后台真实 smoke 仍不能启动的原因，已切换为：云函数部署 / 环境一致性待确认，以及 seed 初始密码安全交付状态未确认。

### 后端

- 后端 owner 边界不变：继续负责 CloudBase 环境初始化、规则 / 索引、seed、云函数部署。
- 在新事实下，后端当前第一优先动作不再是解释地址问题，而是立即核清“云函数是否真的部署在 `cloud1-4grxqg018586792d`”。

## 本轮拍板结论

1. `ADMIN_WEB_BASE_URL` 阻塞已关闭，正式值为 `https://cloud1-4grxqg018586792d-1412048057.tcloudbaseapp.com/`。
2. 真实登录入口最终确认已关闭，当前真实入口为 `/login`。
3. 当前管理员联调根阻塞已进一步收口为 2 条：
   - 云函数部署 / 环境一致性待确认；
   - seed 初始密码是否已通过安全渠道交付未确认。
4. 当前 `PERMISSION_DENIED` 不能按“用户名或密码错误”处理；在 `cloud1-4grxqg018586792d` 控制台云函数列表为空这一事实未解释前，应优先按“`admin-auth` 未部署到同一环境，或前后端指向环境不一致”处理。
5. 云函数部署 / 环境一致性这一条的 owner 明确归 `后端`：
   - 需要回清楚是 `已部署到 cloud1`、`部署在别的环境`、还是 `尚未部署`。
   - 若尚未部署到 `cloud1-4grxqg018586792d`，应尽快部署。
   - 若部署在别的环境，必须直接回传真实环境 ID，不允许继续隐含假设“就是 cloud1”。
6. seed 初始密码安全交付状态这一条，继续由 `阻塞协调` 跟进，只回写 `已交付 / 未交付 / 卡在哪一步`，不写明文。
7. 后台在以上两条闭环后，立即执行真实 `admin-auth` smoke，并补回证据。
8. 登录页里的演示账号文案不是正式联调账号事实，不得再拿来判断联调是否已具备账号条件。

## 本轮新增 / 变化项

1. `admin-web` 发布权限链路从当前主阻塞中移除。
2. `ADMIN_WEB_BASE_URL` 与真实登录入口从“待确认”变为“已确认正式值”。
3. 当前主阻塞从“后台发布权限链路 + 安全交付链路”调整为“云函数环境一致性 + 安全交付链路”。

## 仍待阻塞

1. `admin-auth` 等云函数是否已部署到 `cloud1-4grxqg018586792d`，或是否实际部署在其他环境。
2. seed 初始密码是否已通过安全渠道交付。
3. `dm_configs` 是否执行 `oldest` 去重 apply。
4. 题 4 / 5 / 6 高优先级规则拍板。
5. 题 1 / 2 / 3 与 `notice-bff.republish.deadlineAt` 拍板。
6. 首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给后台

1. 将已确认的 `ADMIN_WEB_BASE_URL` 与真实 `/login` 事实同步到真实 smoke 文档、环境模板与证据索引，不回退。
2. 继续保持真实 `admin-auth` smoke ready。
3. 一旦后端回清云函数部署环境且 seed 密码交付状态闭环，立即执行真实 `admin-auth` smoke，并回传 `login / me / changePassword / logout` 结果。
4. 当前若再遇到 `PERMISSION_DENIED`，先按“函数或环境不一致”记录，不误判成账号密码错误。

### 分配给后端

1. 立即核对云函数部署事实，并在下轮回报中明确三选一结论：`已部署到 cloud1-4grxqg018586792d`、`部署在别的环境`、或 `尚未部署`。
2. 若尚未部署到 `cloud1-4grxqg018586792d`，优先补部署至少 `shared` 与 `admin-auth`，并回传结果。
3. 若部署在别的环境，直接回传真实环境 ID，并说明后台当前是否连错环境。
4. 继续保持 seed / session / 首次改密链路 ready，不回退“已生成=是”的既有事实。

### 分配给阻塞协调

1. `ADMIN_WEB_BASE_URL` 与真实登录入口事项本轮已关闭，不再继续追。
2. 继续只盯 seed 初始密码安全交付状态，并回传 `已交付 / 未交付 / 卡在哪一步`。
3. 若后端回报需要外部环境事实或控制台权限配合，你再介入；否则不抢后端 owner 的部署判断。

## 会议结论摘要

- `admin-web` 已发布，`ADMIN_WEB_BASE_URL` 与 `/login` 已确认。
- 当前不再卡后台地址，而是卡“云函数是否部署在同一环境”与“seed 密码是否已安全交付”。
- `云函数环境一致性` 归后端，`seed 密码交付状态` 归阻塞协调，后台等两项闭环后立即跑真实 smoke。
