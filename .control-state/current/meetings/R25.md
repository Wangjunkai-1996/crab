# KK_Crab 会议纪要 R25

- 会议形式：异步事实收口会
- 轮次：R25
- 时间：2026-03-18 18:01
- 主持：总控窗口
- 参与角色：后台、后端、阻塞协调

## 本轮输入

1. `R24` 之后，后台与后端提交了新回报。
2. 用户提出希望“seed 初始密码可以直接放邮件里”，总控需就密码传递方式给出正式裁决。

## 各角色最新事实

### 后台

- 已把正式 `ADMIN_WEB_BASE_URL` 与真实 `/login` 同步进后台联调资料。
- 已确认浏览器级进入后台登录页链路可用，正式登录页可达。
- 当前后台真实 `admin-auth` smoke 仍未执行，但剩余阻塞已收口为：云函数部署 / 环境一致性待回清、seed 初始密码安全交付状态待闭环。

### 后端

- 已正式回报：云函数部署 / 环境一致性已核清为 `已部署到 cloud1-4grxqg018586792d`。
- 已正式回报：当前凭据下可见环境仅该 1 套，且 `admin-auth` 已在该环境部署并处于 `Active`。
- 已正式回报：后端侧用于首批 `admin-auth` 真实 smoke 的核心前提已满足，剩余外部缺口只剩 seed 初始密码是否已通过安全渠道交付。

### 阻塞协调

- 已确认 `ADMIN_WEB_BASE_URL` 与真实 `/login` 事项已关闭，不再继续追。
- 当前阻塞协调窗口只继续跟进 1 条主线：`domi_admin` 初始密码是否已通过安全渠道交付。
- 本轮无新增 seed 密码交付状态事实，当前仍为 `待确认`。

## 本轮拍板结论

1. 云函数部署 / 环境一致性阻塞已关闭；正式结论为：`admin-auth` 已部署到 `cloud1-4grxqg018586792d`，并处于 `Active`。
2. 当前管理员联调第一阻塞已进一步收口为 1 条：seed 初始密码是否已通过安全渠道交付。
3. 后台一旦拿到 seed 初始密码，即可立即执行首批真实 `admin-auth` smoke，并回传 `login / me / changePassword / logout` 结果。
4. 关于“是否可以把 seed 初始密码直接写进邮件 / 收件箱 / 回报箱 / 会议纪要 / 看板 / `.control-state` 同步快照”——本轮正式裁决为：`不可以`。
5. 原因不是线上与否，而是当前这套控制文件会进入同步目录与跨电脑续接快照；按现有控制规则，真实密钥、初始密码等敏感值不得写入这些同步文档。
6. 允许的做法只有两类：
   - 通过不进入仓库 / 不进入同步快照的单独消息，直接一次性发给实际执行 smoke 的窗口；或
   - 由执行者在本地手动录入临时环境变量 / 本地私有文件，不回写到同步文档。
7. 同步文档里只允许回写状态：`已交付`、`未交付`、`卡在哪一步`；不允许写明文密码。
8. 登录页中的演示账号文案继续不得视为正式联调账号事实。

## 本轮新增 / 变化项

1. 云函数环境一致性阻塞从主阻塞中移除。
2. 首批 `admin-auth` 联调根阻塞从 2 条进一步收口为 1 条：seed 密码交付状态。
3. 密码传递方式被正式拍板：明文不进同步邮件，只能走非同步的一次性交付。

## 仍待阻塞

1. seed 初始密码是否已通过安全渠道交付。
2. `dm_configs` 是否执行 `oldest` 去重 apply。
3. 题 4 / 5 / 6 高优先级规则拍板。
4. 题 1 / 2 / 3 与 `notice-bff.republish.deadlineAt` 拍板。
5. 首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给后台

1. 继续保持真实 `admin-auth` smoke ready，不回退已确认的正式 URL、真实 `/login` 与浏览器进入路径。
2. 一旦 seed 密码以非同步方式交付到你本线，立即执行真实 `admin-auth` smoke，并回传 `login / me / changePassword / logout` 结果。
3. 回报箱里只写“已收到密码并完成 smoke / 已收到但 smoke 未完成 / 仍未收到”，不要写明文。

### 分配给后端

1. 继续保持 `admin-auth`、seed / session / 首次改密链路 ready，不回退“已部署到 cloud1 + admin-auth Active”的既有事实。
2. 本轮不再重复解释环境一致性问题，除非后台 smoke 出现新的函数级报错。
3. 若总控确认 `dm_configs` 采用 `oldest` 策略，执行 apply 并回传结果。

### 分配给阻塞协调

1. 你当前只盯 1 条主线：seed 初始密码是否已通过安全渠道交付。
2. 若准备把密码交给执行方，必须用非同步方式直接交付，不写入 `inbox/outbox`、`meetings`、`board`、`decisions`、`.control-state/current` 或其他同步文件。
3. 下轮回报只写状态：`已交付`、`未交付`、或 `卡在哪一步`。
4. 一旦交付完成，立即通知后台执行真实 smoke。

## 会议结论摘要

- `admin-web` 已发布，`admin-auth` 也已确认部署到同一环境。
- 当前只剩 1 个首要阻塞：seed 密码交付状态。
- 明文密码不能进同步邮件；只能走一次性、非同步交付。
- 后台拿到密码后立即跑真实 `admin-auth` smoke。
