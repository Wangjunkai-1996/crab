# KK_Crab 会议纪要 R28

- 会议形式：异步复核纠偏会
- 轮次：R28
- 时间：2026-03-18 19:05
- 主持：总控窗口
- 参与角色：后台、后端、体验设计审查

## 本轮输入

1. 后台提交了第二轮 must-close 收口回报。
2. 后端提交了下一批函数部署回报。
3. 体验设计审查提交了后台第二轮复审结论，并指出现有真实证据包与摘要存在不一致。

## 各角色最新事实

### 后台

- 已补后台第二轮 must-close 复核结论，并明确把“已被真实 smoke 验证的链路”与“仍仅处于 mock / 待联调的业务域”分开记录。
- 已完成真实读接口切换层的结构准备，并增加 `VITE_ENABLE_REAL_ADMIN_READS` 开关，为 `dashboard`、`operationLogList`、`taskList`、`taskDetail`、`reportList`、`reportDetail`、`accountActionList` 切真做好单点承接。
- 当前无新增工程阻塞。

### 后端

- 已在 `cloud1-4grxqg018586792d` 补部署下一批 9 个云函数，当前云函数总数已扩到 10 个，且均为 `Active`。
- 已补足后续后台 / 小程序联调所需的主要函数覆盖面。
- 当前后端侧主要待决事项仍是 `dm_configs` 是否执行 `oldest` 去重 apply，以及规则题拍板。

### 体验设计审查

- 已启动后台第二轮体验复审。
- 已明确指出：当前后台 `admin-auth` 真实证据包与摘要结论不一致，现有截图 / 快照不足以直接支撑“首次改密通过”“标准登录通过”“进入工作台通过”“退出登录通过”这些体验结论。
- 已明确区分：这是“真实链路证据自洽性问题”，不等同于重新否定技术 smoke 曾执行成功这一事实。

## 本轮拍板结论

1. `admin-auth` 的**技术 smoke 成功**这一事实继续成立；不回退。
2. 但“后台第二轮体验复审已通过”这一结论当前**不能成立**；原因是现有真实证据包与摘要不自洽。
3. 因此从本轮开始，要把两件事分开记录：
   - 技术链路事实：已执行并成功闭环；
   - 体验 / must-close 事实：仍待补一版可直接核对、前后自洽的真实证据后，才能判定通过。
4. 后台当前第一优先任务不是重复大范围改代码，而是先补一版**证据自洽**的真实链路材料，至少能直接证明：
   - 首次改密弹层出现并成功提交；
   - 成功回到工作区；
   - 退出登录后回到登录页；
   - 标准登录再次进入工作台；
   - 再次退出登录成功。
5. 在这版证据补齐前，总控不再把后台 must-close 写成“真实验证已通过”。
6. 后端本轮新增部署的 9 个函数与 `admin-auth` 已形成下一阶段联调基础，可支持后台读接口切真与前端首批真实 smoke 推进。
7. 当前阶段主关注点调整为三条：
   - 后台补自洽的 `admin-auth` 真实证据；
   - 后台 / 后端推进业务域真实读接口与 mock 替换；
   - 前端基于已部署函数清单启动下一批小程序真实 smoke。

## 本轮新增 / 变化项

1. “后台真实 `admin-auth` smoke 成功”与“后台体验复审通过”被正式拆成两件事。
2. 后台当前新增一个明确待办：补自洽证据，而不是泛泛重复 smoke。
3. 后端新增的 9 个 Active 云函数，使下一批后台 / 小程序联调条件进一步齐备。

## 仍待阻塞

1. 后台 `admin-auth` 真实证据包与摘要的一致性仍待补齐。
2. `dm_configs` 是否执行 `oldest` 去重 apply。
3. 题 4 / 5 / 6 高优先级规则拍板。
4. 题 1 / 2 / 3 与 `notice-bff.republish.deadlineAt` 拍板。
5. 后台其余业务域真实 handler / mock 替换与更大范围真实联调。
6. 小程序侧首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给后台

1. 优先补一版自洽的真实 `admin-auth` 证据包，至少补齐“首次改密成功回工作区”“标准登录进入工作台”“退出登录后回登录页”的可直接核对截图 / 快照 / 摘要一致性。
2. 在补证据的同时，继续推进 `dashboard`、`operationLogList` 与审核 / 举报 / 处罚相关读模型的真实接口切换准备。
3. 回报中明确区分：技术链路已通过的事实，与体验证据是否足够支撑复审通过的事实。

### 分配给后端

1. 继续保持 10 个 Active 云函数的部署事实不回退。
2. 继续配合后台 / 前端下一批真实 smoke；如出现新增真实报错，直接按函数与 action 补查。
3. 等总控拍板后，再决定是否执行 `dm_configs` 的 `oldest` apply。

### 分配给体验设计审查

1. 继续维持“技术 smoke 成功 ≠ 体验复审已通过”的边界，不放宽标准。
2. 后台补新证据后，优先复核这组新证据是否自洽，再决定 must-close 是否可转为“真实验证通过”。
3. 前端 5 项 must-close 仍继续保持“本地已覆盖 / 待环境后复核”。

## 会议结论摘要

- 后台 `admin-auth` 技术 smoke 成功仍成立。
- 但体验复审指出：当前证据包不够自洽，还不能宣布 must-close 真通过。
- 下一步先补自洽证据，再由体验复核；后端则继续支撑下一批真实联调。
