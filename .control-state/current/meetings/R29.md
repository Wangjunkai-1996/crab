# KK_Crab 会议纪要 R29

- 会议形式：异步执行纠偏会
- 轮次：R29
- 时间：2026-03-19 10:21
- 主持：总控窗口
- 参与角色：后台、后端、体验设计审查

## 本轮输入

1. 后台继续回报了证据自洽收口进展。
2. 后端继续回报了当前环境函数覆盖与部署状态。
3. 体验设计审查继续明确：当前后台 `admin-auth` 的体验复审仍缺一版可直接核对的新证据。

## 各角色最新事实

### 后台

- 已完成 R28 的证据清理与索引重整，新增 `__ADMIN_WORKTREE__/admin-web/evidence/r09/real-admin-auth/artifact-index.md` 作为统一证据入口。
- 已把“技术链路结论”和“体验 / must-close 结论”在 `__ADMIN_WORKTREE__/admin-web/evidence/r10/must-close-status.md` 中拆开记录，不再混写。
- 已确认当前历史截图包存在重复哈希：3 张 `first-reset` 截图一致，2 张 `standard` 截图一致；因此当前老截图不能单独支撑完整阶段差异证明。
- 已将真实 smoke 脚本改为默认清理临时快照、生成脱敏 stage log，并避免保存 session / localStorage 明细，为下一次重跑做准备。
- 当前后台侧第一阻塞不是工程实现，而是：缺一套**当前可信的真实联调凭据**，无法补跑出新的一致性证据。

### 后端

- 已再次远端复核：当前 `cloud1-4grxqg018586792d` 里 10 个云函数仍全部为 `Active`，部署事实未回退。
- 已继续维持当前边界：`admin-auth` 已真实参与联调，其余 9 个函数当前属于“已部署待验证”。
- 当前后端没有新增函数级报错待修；核心工作转为继续支撑下一批真实联调，以及等待总控是否拍板 `dm_configs` 的 `oldest` apply。

### 体验设计审查

- 已再次确认：后台本轮新增的是文档性收口与切真准备，而不是一组新的、可直接支撑 must-close 通过的自洽体验证据。
- 已再次确认：`admin-auth` 的**技术 smoke 成功**仍成立，但“体验 / must-close 已真实通过”仍不能成立。
- 已明确要求：下一次必须补一版能逐项证明“首次改密成功、进入工作台、退出登录、标准登录再次进入、再次退出登录”的新证据。

## 本轮拍板结论

1. `admin-auth` 的技术链路成功继续成立，不回退。
2. 但当前后台体验复审仍不能通过；原因不再是“链路跑不通”，而是“缺可信凭据，导致无法补一版新的自洽证据”。
3. 因此，当前第一优先任务正式收口为：**为后台补一套可信的真实联调凭据，再重跑一次证据自洽版 smoke**。
4. 这套可信凭据允许两种路径，后端二选一即可：
   - 路径 A：重置 `domi_admin.mustResetPassword`，并重新下发一次性密码；
   - 路径 B：提供当前可用的标准登录密码。
5. 无论走 A 还是 B，明文仍不得写入同步文档；只允许通过既定的非同步方式一次性交付给后台执行窗口。
6. 后台拿到这套可信凭据后，优先补一版“证据自洽”的真实 `admin-auth` 证据；在此之前，总控不再把后台 must-close 写成“真实验证通过”。
7. 后端当前新增部署的 9 个函数继续视为“下一批联调条件已齐备”，但不自动等于“已真实验证通过”。

## 本轮新增 / 变化项

1. 当前第一优先级从“补自洽证据”进一步收口到“先补可信凭据，再补自洽证据”。
2. 后台当前的主要缺口被正式定义为“可信凭据缺口”，而不是“脚本能力缺口”或“工程阻塞”。
3. 后端被正式指定为本轮凭据重跑路径的第一技术 owner。

## 仍待阻塞

1. 后台缺一套可信的当前真实联调凭据，无法补跑新的自洽证据。
2. `dm_configs` 是否执行 `oldest` 去重 apply。
3. 题 4 / 5 / 6 高优先级规则拍板。
4. 题 1 / 2 / 3 与 `notice-bff.republish.deadlineAt` 拍板。
5. 后台其余业务域真实 handler / mock 替换与更大范围真实联调。
6. 小程序侧首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给后台

1. 继续保持当前证据收口成果，不回退文档拆分、artifact index 与脱敏脚本策略。
2. 一旦收到可信凭据，立即重跑一轮“证据自洽版”真实 `admin-auth` smoke，并优先补齐：
   - 首次改密成功回工作区
   - 退出登录后回登录页
   - 标准登录再次进入工作台
   - 再次退出登录成功
3. 新回报中明确区分：
   - 历史技术链路成功事实
   - 新一轮证据是否已足够支撑体验复审通过

### 分配给后端

1. 本轮请二选一给后台补齐可信凭据路径：
   - 重置 `domi_admin.mustResetPassword` 并准备一次性密码；或
   - 确认当前可用的标准登录密码。
2. 无论选择哪条路径，明文都不要写进同步文档；只回报“已准备好交付”或“仍卡在哪一步”。
3. 继续保持 10 个 `Active` 云函数的部署事实不回退。
4. 若总控拍板 `dm_configs` 使用 `oldest` 策略，再执行 apply。

### 分配给体验设计审查

1. 继续维持当前边界：技术 smoke 成功 ≠ 体验通过。
2. 等后台补一轮新证据后，优先复核这轮新证据是否已自洽，再决定 must-close 是否可转为“真实验证通过”。
3. 前端 5 项 must-close 继续保持“本地已覆盖 / 待环境后复核”。

## 会议结论摘要

- 后台 `admin-auth` 技术 smoke 成功仍成立。
- 体验复审仍未通过，当前核心缺口是“缺可信凭据，无法补新证据”。
- 下一步不是盲目重复跑，而是先让后端补可信凭据，再由后台补一轮自洽证据。
