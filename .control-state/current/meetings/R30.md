# KK_Crab 会议纪要 R30

- 会议形式：异步交付推进会
- 轮次：R30
- 时间：2026-03-19 10:21
- 主持：总控窗口
- 参与角色：后台、后端、体验设计审查

## 本轮输入

1. 后台继续回报了证据自洽收口进展。
2. 后端提交了可信凭据路径的执行结果。
3. 体验设计审查继续维持“技术成功 ≠ 体验通过”的边界。

## 各角色最新事实

### 后台

- 已继续保持 R28 的证据收口成果，不回退 `artifact-index`、must-close 拆分记录与脱敏脚本策略。
- 已明确：当前第一阻塞仍是缺可信的当前真实联调凭据，因此尚未补跑新的“证据自洽版”真实 `admin-auth` smoke。
- 已继续推进业务域读模型真实切换准备，并继续收缩 normalizer 中明确无用的 legacy 兼容分支。

### 后端

- 已按 `R29` 要求选择路径 A：为 `domi_admin` 重置了一套新的可信联调凭据，即 `一次性密码 + mustResetPassword=true`。
- 已新增 `__BACKEND_WORKTREE__/scripts/reset-admin-credential.mjs`，并增加 `reset:admin-credential` 入口，后续可复用。
- 已对这套新凭据做脱敏登录校验，结果为 `code=0`、`message=ok`、`mustResetPassword=true`、`hasToken=true`。
- 当前后端给出的状态是：`可信凭据已准备好交付`，且明文未写入任何同步文档、仓库或证据文件。

### 体验设计审查

- 已继续确认：后台本轮新增的是证据清理、索引重整、脱敏脚本准备，以及“缺可信凭据”的阻塞收口。
- 已继续确认：在新证据未到位前，后台 `admin-auth` 的体验 / must-close 仍不能判定真实通过。

## 本轮拍板结论

1. `R29` 中定义的“可信凭据缺口”已从“未准备好”推进到“已准备好交付”。
2. 当前第一阻塞进一步收口为 1 个动作：**把这套一次性密码按既定非同步方式交付到后台执行窗口**。
3. 凭据交付完成后，后台立即重跑一轮“证据自洽版”真实 `admin-auth` smoke。
4. 技术链路成功继续成立，不回退；但体验 / must-close 仍要等这一轮新证据补齐后再判定。
5. 明文密码继续不得写入 `inbox/outbox`、`meetings`、`board`、`decisions`、`.control-state/current` 或任何同步文档；当前只允许记录状态为：`已准备好交付`、`已交付`、`已收到`、`已重跑`。
6. 后端新增的重置脚本本轮被正式纳入可复用工具链；后续若需要再次重置后台联调凭据，优先复用该脚本。

## 本轮新增 / 变化项

1. 当前缺口已从“后端要不要补凭据”推进为“凭据已就位，待完成非同步交付”。
2. 后台下一次执行重点已被进一步压缩：拿到凭据后直接补跑自洽证据，不再继续空转等待。
3. 后端本轮新增了一条后续可复用的凭据重置脚本链路。

## 仍待阻塞

1. 本轮一次性密码尚未完成非同步交付到后台执行窗口。
2. 后台尚未补跑新的自洽 `admin-auth` 真实证据。
3. `dm_configs` 是否执行 `oldest` 去重 apply。
4. 题 4 / 5 / 6 高优先级规则拍板。
5. 题 1 / 2 / 3 与 `notice-bff.republish.deadlineAt` 拍板。
6. 后台其余业务域真实 handler / mock 替换与更大范围真实联调。
7. 小程序侧首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给后台

1. 一旦收到本轮一次性密码，立即重跑一轮“证据自洽版”真实 `admin-auth` smoke`。
2. 这轮新证据至少逐项补齐：
   - 首次改密成功回工作区
   - 退出登录后回登录页
   - 标准登录再次进入工作台
   - 再次退出登录成功
3. 回报中必须明确：`已收到凭据 / 已重跑 / 新证据是否已足够支撑体验复审通过`。
4. 在凭据到位前，继续保持当前工程与证据收口成果，不回退。

### 分配给后端

1. 当前状态已进入 `已准备好交付`，请继续按既定非同步方式完成本轮一次性密码交付。
2. 回报中只写状态：`已准备好交付`、`已交付`、或 `仍卡在哪一步`；不写明文。
3. 继续保持当前 10 个 `Active` 云函数的部署事实不回退。
4. 若后台在重跑后出现新增函数级报错，立即按函数与 action 补查。

### 分配给体验设计审查

1. 继续维持当前边界，不因“凭据已准备好交付”而提前放宽结论。
2. 等后台补一轮新证据后，优先复核这轮新证据是否已能逐项支撑 must-close 通过。
3. 前端 5 项 must-close 继续保持“本地已覆盖 / 待环境后复核”。

## 会议结论摘要

- 后端已经把可信凭据准备好了。
- 当前只差一次非同步交付动作。
- 后台拿到后立刻补跑一轮自洽证据；体验再基于这轮新证据判断是否通过。
