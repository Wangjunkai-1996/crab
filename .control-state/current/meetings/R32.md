# KK_Crab 会议纪要 R32

- 会议形式：异步体验复核结论会
- 轮次：R32
- 时间：2026-03-19 12:29
- 主持：总控窗口
- 参与角色：体验设计审查、后台、后端

## 本轮输入

1. 体验设计审查已完成对后台 `R30` 新证据的优先复核。
2. 后台与后端本轮无更新回报，沿用 `R31` 已确认事实。

## 各角色最新事实

### 体验设计审查

- 已复核：
  - `__ADMIN_WORKTREE__/admin-web/evidence/r30/admin-auth-evidence-assessment.md`
  - `__ADMIN_WORKTREE__/admin-web/evidence/r30/real-admin-auth/artifact-index.md`
  - `__ADMIN_WORKTREE__/admin-web/evidence/r30/real-admin-auth/summary.md`
  - `__ADMIN_WORKTREE__/admin-web/evidence/r10/must-close-status.md`
- 已明确结论：后台 `admin-auth` 的体验 / must-close 结果不是“可通过”，也不是“只差一张首次改密后工作区锚点”，而是 **仍缺其他具体证据**。
- 当前已可接受的证据：
  - 两次退出后回到登录页。
  - 技术链路成功继续成立。
- 当前仍缺 3 个最小证据点：
  - 首次改密弹层的独立视觉证据。
  - 首次改密成功后进入工作区的独立视觉证据。
  - 标准登录后进入工作区的独立视觉证据。

### 后台

- 维持 `R31` 已确认事实：已收到本轮一次性凭据，并完成一轮新的真实 `admin-auth` 重跑。
- 当前后台不需要扩大返工；下一步只需围绕体验指出的 3 个最小缺口补证据。

### 后端

- 维持 `R31` 已确认事实：`reset-admin-credential.mjs` 已修复可复用，且当前 10 个云函数持续 `Active`。
- 若后台为补这 3 个最小锚点需要再次重置一次性凭据，可直接复用现有脚本，不必扩大返工范围。

## 本轮拍板结论

1. 后台 `admin-auth` 体验复核结果正式记录为：`仍缺其他具体证据（逐项列出）`。
2. 当前缺口不能压缩成“只差一张首次改密后工作区独立锚点”；最小缺口是 3 项：
   - 首次改密弹层
   - 首次改密后工作区
   - 标准登录后工作区
3. 两次退出回登录页的当前证据可接受，下一轮不需要重复补这两项。
4. 技术 smoke 成功继续成立，但体验 / must-close 仍未通过；继续禁止混写。
5. 下一步执行面只收敛到“后台补 3 个最小证据点；若需要，后端提供一轮新的凭据重置支援”。

## 本轮新增 / 变化项

1. 当前第一阻塞已从“等待体验复核”推进为“后台补 3 个最小证据点”。
2. 上一轮预设的“也许只差一张工作区锚点”被正式否定，避免错误缩小问题范围。
3. 当前补证据范围已被体验精确压缩，不需要整链路返工，也不需要重复补退出登录页。

## 仍待阻塞

1. 后台尚未补齐 3 个最小证据点：
   - 首次改密弹层
   - 首次改密后工作区
   - 标准登录后工作区
2. 若后台补证据需要新的一次性凭据，需后端再执行一轮重置并完成非同步交付。
3. `dm_configs` 是否执行 `oldest` 去重 apply。
4. 题 4 / 5 / 6 高优先级规则拍板。
5. 题 1 / 2 / 3 与 `notice-bff.republish.deadlineAt` 拍板。
6. 后台其余业务域真实 handler / mock 替换与更大范围真实联调。
7. 小程序侧首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给后台

1. 下一轮只补 3 个最小证据点，不扩大返工范围：
   - 一张能明确看出“首次改密弹层”的独立截图或等价独立锚点；
   - 一张能明确看出“首次改密成功后进入工作区”的独立截图或等价独立锚点；
   - 一张能明确看出“标准登录后进入工作区”的独立截图或等价独立锚点。
2. 两次退出回登录页当前已被体验接受，不要重复补。
3. 若现有状态无法补这 3 项，立即请求后端重置一次性凭据，再做一轮最小范围补跑。
4. 下一次回报必须明确写：`这 3 项是否已逐项补齐 / 哪一项仍缺 / 缺口原因`。

### 分配给后端

1. 保持待命；若后台提出需要新的首次改密机会，立即复用 `__BACKEND_WORKTREE__/scripts/reset-admin-credential.mjs` 重置一次性凭据。
2. 回报中继续只写状态：`已再次准备好`、`已交付`、或 `仍卡在哪一步`；不写明文。
3. 保持当前 10 个 `Active` 云函数事实不回退。

### 分配给体验设计审查

1. 等后台补齐 3 个最小证据点后，再次复核，不提前放宽标准。
2. 下一次回报继续给出明确结论：`可通过` 或 `仍缺哪些具体证据`。

## 会议结论摘要

- 体验复核已经给出明确结论：现在还不能通过。
- 真正缺的不是 1 张，而是 3 个最小证据点。
- 下一步就是后台小范围补证据，后端按需给一次性凭据支援。
