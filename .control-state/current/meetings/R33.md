# KK_Crab 会议纪要 R33

- 会议形式：异步补证据推进会
- 轮次：R33
- 时间：2026-03-19 12:41
- 主持：总控窗口
- 参与角色：后台、后端

## 本轮输入

1. 后台已按 `R32` 要求复核并修正真实 smoke 脚本的证据断言，确认当前 3 个最小证据点都还未补齐。
2. 后台已正式提出新的协作请求：需要后端再重置一次 `domi_admin` 的一次性凭据，以便补抓 3 个最小证据点。
3. 后端已回报：当前 10 个云函数继续全部 `Active`，并补了一条快速核查脚本；但由于本轮之前尚未收到正式重置请求，暂未进入新的凭据准备 / 交付动作。

## 各角色最新事实

### 后台

- 已确认 `R30` 的 3 张候选图当前都不能作为可接受的独立视觉证据：
  - `first-reset-01-password-dialog.png`
  - `first-reset-02-workspace-after-change.png`
  - `standard-01-workspace.png`
- 已修正真实 smoke 脚本的证据断言，后续工作区截图前必须同时满足：
  - 工作台 heading 可见
  - 退出登录按钮可见
  - 改密弹层已消失
  - 登录按钮不存在
- 已将当前证据口径更新到：
  - `__ADMIN_WORKTREE__/admin-web/evidence/r32/admin-auth-min-evidence-gap.md`
  - `__ADMIN_WORKTREE__/admin-web/evidence/r10/must-close-status.md`
  - `__ADMIN_WORKTREE__/admin-web/evidence/r30/admin-auth-evidence-assessment.md`
  - `__ADMIN_WORKTREE__/admin-web/evidence/r30/real-admin-auth/artifact-index.md`
- 当前后台已明确：若没有新的首次改密机会，无法在现状态下补齐“首次改密弹层 / 首次改密后工作区”；“标准登录后工作区”也需要在下一轮有效凭据下重新抓取。

### 后端

- 已新增 `__BACKEND_WORKTREE__/scripts/check-cloudfunctions-active.mjs`，并补 `check:functions-active` 入口。
- 已执行 `npm run check:functions-active -- --env cloud1-4grxqg018586792d`，结果继续为 10 个云函数全部 `Active`。
- 已更新 `__BACKEND_WORKTREE__/scripts/coordination/environment-handoff.md` 到 `R32` 口径。
- 当前后端仍处于“待命 + 按需凭据支援”状态；但本轮后台已正式提出新的一次性凭据请求，因此下一步应直接进入新的凭据准备与非同步交付。

## 本轮拍板结论

1. 当前第一阻塞已进一步收口为：**后端立即响应后台的正式请求，再次重置一次性凭据并完成非同步交付**。
2. 凭据一旦交付，后台立即按已修正脚本做一轮最小范围补跑，只抓 3 个最小证据点：
   - 首次改密弹层
   - 首次改密后工作区
   - 标准登录后工作区
3. 两次退出回登录页已被体验接受，继续不重复补。
4. 后端新增的函数状态核查脚本本轮被正式纳入可复用工具链；当前 10 个云函数全部 `Active` 的事实继续成立。
5. 技术链路成功 与 体验 / must-close 通过 继续分开写，禁止混写。

## 本轮新增 / 变化项

1. 后台已把“要补什么”与“当前为什么补不了”说清楚，当前不再是模糊等待。
2. 后端从“待命”切换为“收到正式请求后立即执行凭据支援”。
3. 环境稳定性本轮又补强了一条可复用核查脚本，减少后续重复确认成本。

## 仍待阻塞

1. 后端尚未完成本轮新的一次性凭据准备与非同步交付。
2. 后台尚未基于新凭据补齐 3 个最小视觉证据点。
3. 体验设计审查尚未基于这轮新证据做下一次明确判定。
4. `dm_configs` 是否执行 `oldest` 去重 apply。
5. 题 4 / 5 / 6 高优先级规则拍板。
6. 题 1 / 2 / 3 与 `notice-bff.republish.deadlineAt` 拍板。
7. 后台其余业务域真实 handler / mock 替换与更大范围真实联调。
8. 小程序侧首批真实 smoke 与证据补齐。

## 下一轮任务分配

### 分配给后端

1. 立即复用 `__BACKEND_WORKTREE__/scripts/reset-admin-credential.mjs` 再重置一次 `domi_admin` 的一次性凭据。
2. 按既定非同步方式完成交付；同步文档里只写状态：`已再次准备好`、`已交付`、或 `仍卡在哪一步`。
3. 保持当前 10 个 `Active` 云函数事实不回退。

### 分配给后台

1. 一旦收到新的非同步一次性凭据，立即按已修正脚本做一轮最小范围补跑。
2. 只抓 3 个最小证据点，不扩大返工范围，不重复补退出登录页。
3. 下一次回报必须明确：这 3 项是否逐项补齐、证据路径在哪、哪一项若仍缺则原因是什么。

### 分配给体验设计审查

1. 继续等待后台补齐 3 个最小证据点后的新证据。
2. 收到新证据后，再输出 `可通过 / 仍缺哪些具体证据` 的明确结论。

## 会议结论摘要

- 后台已经把缺口和原因说清楚了。
- 现在第一动作不是讨论，而是后端立刻再给一轮一次性凭据。
- 后台拿到后，只补 3 张关键证据，再交给体验复核。
