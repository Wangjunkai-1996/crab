# KK_Crab 会议纪要 R36

- 会议形式：总控单窗口环境修复会
- 轮次：R36
- 时间：2026-03-19 16:25
- 主持：总控窗口
- 参与角色：总控、后台、后端

## 本轮输入

1. 总控已通过官方文档再次确认：CloudBase 云函数安全规则仅对客户端 SDK `callFunction` 生效，且匿名登录会命中 `auth.loginType == 'ANONYMOUS'`。
2. 总控已从 CloudBase CLI 内置实现中确认可用底层动作：`DescribeSecurityRule` / `ModifySecurityRule`。
3. 总控已在当前 `dev` 环境直接读取到函数安全规则真实值。

## 本轮新事实

### 已确认

- 当前 `dev` 环境 `cloud1-4grxqg018586792d` 的函数安全规则真实值为：

```json
{
  "*": {
    "invoke": "auth != null && auth.loginType != 'ANONYMOUS'"
  }
}
```

- 因此，后台 `admin-web` 现有“匿名 CloudBase 登录 + SDK callFunction”链路会被当前 wildcard 规则直接拦掉；`R35` 的高概率推论已升级为**已实锤根因**。
- 当前失败不再归因为：
  - UI 锚点问题
  - 当前总控会话无密钥
  - 用户名密码错误

### 已执行

- 总控已对当前 `dev` 环境直接执行最小 unblock，新的函数安全规则为：

```json
{
  "*": {
    "invoke": "auth != null && auth.loginType != 'ANONYMOUS'"
  },
  "admin-auth": {
    "invoke": true
  }
}
```

- 上述改动只放开 `admin-auth` 的匿名客户端入口，默认 wildcard 规则仍保留。
- 后台真实鉴权仍继续依赖函数内部的 `meta.adminSessionToken`、`requireAdminAuth` 与角色校验，不把 CloudBase 规则当成业务鉴权替代。

## 本轮拍板结论

1. `R35` 的“后端补查 web 侧 `PERMISSION_DENIED` 根因”任务已关闭，当前根因已确认并已对 `dev` 环境执行最小修复。
2. 当前第一阻塞切换为：后台基于已修复的函数安全规则，立即重跑 `admin-auth` 的 3 个最小证据点。
3. 若后台重跑后仍失败，必须继续带上 `*-after-submit-snapshot.txt` 与页面错误文案回传；在拿到新失败证据前，不再回退为旧根因。
4. `review-admin` / `governance-admin` 当前尚未一并放开；若后续后台开启真实读，后端需先评估是扩规则还是改 transport。

## 下一轮任务分配

### 分配给后台

1. 立即按已修复环境重跑真实 `admin-auth` smoke。
2. 只补 3 个最小证据点：
   - 首次改密弹层
   - 首次改密后工作区
   - 标准登录后工作区
3. 若仍失败，继续回传：
   - `*-after-submit-snapshot.txt`
   - 页面错误文案
   - 当前证据目录路径

### 分配给后端

1. 把“当前真实规则 / 已执行最小 unblock / 后续扩展方案”同步到正式交接文档。
2. 继续评估：若后台后续启用 `review-admin` / `governance-admin` 真实 transport，是扩函数安全规则，还是调整 Web transport 方案。
3. 若后台重跑后仍失败，优先围绕新证据补查，不回退旧根因。

### 分配给体验设计审查

1. 等后台新证据到位后，立即按既有 must-close 清单复审。

## 会议结论摘要

- 根因已经不是“猜测”，而是当前环境真实函数安全规则。
- 最小 unblock 已在 `dev` 环境执行完成。
- 现在轮到后台重跑 3 个关键证据点。
