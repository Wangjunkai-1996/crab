# KK_Crab 会议纪要 R37

- 会议形式：总控单窗口复盘会
- 轮次：R37
- 时间：2026-03-19 17:29
- 主持：总控窗口
- 参与角色：总控、后台、后端

## 本轮输入

1. 后台已回传 `R36` 新证据：环境函数权限最小修复后，登录阶段不再报 `PERMISSION_DENIED`，但页面提示切换为 `用户名或密码错误`。
2. 总控为避免继续在“凭据是否过期”上空转，已直接补做一轮不依赖外部窗口转交的对照核验。

## 本轮新事实

### 已确认

- 后台 `R36` 回报里“当前可能是凭据不匹配”的推论，当前**不能再当成既定根因**。
- 总控已直接完成服务端对照校验，结论为：
  - `admin-auth.login`：`code=0`
  - `admin-auth.changePassword`：`code=0`
  - 同一 `adminSessionToken` 下 `admin-auth.me`：`code=0`
- 上述对照说明：
  - 当前 fresh 凭据本身可用；
  - `changePassword` 后端动作可用；
  - `changePassword` 后紧跟 `me` 的会话续用也可用；
  - 当前主阻塞已不再是“后端没给对密码”。

### 总控新增动作

- 已更新 `__ADMIN_WORKTREE__/admin-web/scripts/smoke-admin-auth-real.sh`：
  1. 当“标准账号”和“首次改密账号”实为同一账号时，`all` 模式改为自动按 `first-reset -> standard` 顺序执行，不再误走 `standard -> first-reset`；
  2. 在首次改密提交后新增失败诊断落点：
     - `first-reset-after-change-failed-snapshot.txt`
     - `first-reset-02-after-change-failed.png`
- 总控已基于新脚本直接重跑一轮 `R39` 真实 smoke。

### `R39` 关键结论

- 首次登录已成功进入改密弹层，说明：
  - 环境函数权限修复有效；
  - fresh 凭据有效；
  - 登录动作本身可用。
- 当前新失败点已进一步收口为：
  - 点击“确认修改”后，改密弹层**没有关闭**；
  - 页面仍停留在 `/dashboard`，同时继续可见“修改后台密码”弹层；
  - 截图中未见显式字段级报错或 toast 文案。
- 因此当前第一阻塞进一步切换为：**后台 Web 首次改密提交流程 / smoke 自动化提交后的页面状态处理异常**，而不是 CloudBase 权限、凭据口径或后端 `changePassword/me` 链路。

## 本轮拍板结论

1. “后端重新发凭据”不再作为当前第一动作；除非后台能给出新证据证明 fresh 凭据再次失效，否则不再围绕密码交付来回打转。
2. 当前第一技术 owner 切换为 `后台`。
3. `后端` 当前降为支援位：只有在后台补出新的 action 级错误证据时再介入。
4. `体验设计审查` 继续等待；在后台真正补出 3 个最小证据点前，不进入复审。

## 下一轮任务分配

### 分配给后台

1. 以 `R39` 新证据为准，优先补查“首次改密点击确认后弹层不关闭”的真实原因。
2. 排查方向先收敛为两类：
   - 页面交互 / 请求链路：确认点击“确认修改”后，真实请求是否发出、返回什么；
   - smoke 自动化：确认脚本是否真的完成了输入 / 点击，或是否因等待条件写错而把成功态误判成失败。
3. 基于总控已更新的 smoke 脚本继续推进；不要再回退到旧脚本版本。
4. 若复查后确认是页面代码问题，请直接修页面；若确认是 smoke 诊断不足，请继续补脚本证据。

### 分配给后端

1. 当前保持待命；不再把“重新准备凭据”作为默认动作。
2. 继续沿用现有结论：服务端 `login -> changePassword -> me` 当前可用。
3. 仅在后台给出新的 action 级失败证据时，再补查服务端细节。

## 会议结论摘要

- `PERMISSION_DENIED` 已经过去了。
- “凭据不对”现在也不是主根因了。
- 当前主阻塞已经收口到：后台首次改密提交后，弹层不关闭，需由后台线继续追。
