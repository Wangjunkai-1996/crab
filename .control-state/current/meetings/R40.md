# KK_Crab 会议纪要 R40

- 会议形式：总控夜间推进会
- 轮次：R40
- 时间：2026-03-19 19:25
- 主持：总控窗口
- 参与角色：总控、后台、后端（支援）

## 本轮输入

1. 后台已回报 `changePassword` action 级真实返回体成功。
2. 总控继续直接执行“部署版本一致性”核查，而不是停留在猜测。

## 本轮新事实

### 1. 线上 `admin-web` 与本地最新 `dist` 不一致

- 本地 `dist/index.html` 当前引用：
  - `index-D__sBJkU.js`
  - `client-Bej43dyI.js`
- 线上根路径当前引用：
  - `index-XBpBlyZh.js`
  - `client-IygAFb7F.js`

### 2. 线上 bundle 仍命中旧逻辑

- 总控已直接比对本地与线上 JS：
  - 本地 bundle 含 `markPasswordResetCompleted`
  - 线上 bundle不含 `markPasswordResetCompleted`
  - 线上 bundle 仍命中旧逻辑片段：`changePassword(e){await rq.changePassword(e),await this.fetchMe()}`
- 这与当前“提交改密后弹层仍不关闭”的表现一致。

### 3. 已有标准化部署指纹证据

- 已执行：
  - `cd __BACKEND_WORKTREE__/scripts && npm run check:admin-web-deploy -- --base-url https://cloud1-4grxqg018586792d-1412048057.tcloudbaseapp.com --local-dist __ADMIN_WORKTREE__/admin-web/dist --output __ADMIN_WORKTREE__/admin-web/evidence/r40/real-admin-auth-capture/admin-web-deploy-check.json`
- 结果明确：
  - `markersAllFound=false`
  - `localDistAligned=false`
  - `missingInRemote=["index-D__sBJkU.js","client-Bej43dyI.js"]`
- 关键证据文件：
  - `__ADMIN_WORKTREE__/admin-web/evidence/r40/real-admin-auth-capture/admin-web-deploy-check.json`
  - `__ADMIN_WORKTREE__/admin-web/evidence/r40/real-admin-auth-capture/deploy-runtime-mismatch-summary.md`

### 4. 已补可直接执行的发布脚本，并完成一轮真实重发版

- 新增：
  - `__BACKEND_WORKTREE__/scripts/publish-admin-web-hosting.mjs`
  - `__BACKEND_WORKTREE__/scripts/package.json`
- 已完成：
  - dry-run
  - 一轮真实 `admin-web` 重发版

### 5. 新版资源已生效，但 queryless 根页存在短缓存

- 总控已实际完成一轮真实重发版。
- `cache-busting query` 下，线上已命中新版关键标记：
  - `markPasswordResetCompleted`
  - `index-D__sBJkU.js`
- 但 queryless 根页 `/` 仍可能短时间返回旧 HTML；当前响应头显示：
  - `cache-control: max-age=120`
- 关键新增证据：
  - `__BACKEND_WORKTREE__/scripts/admin-web-deploy-check.post-publish.json`
  - `__BACKEND_WORKTREE__/scripts/admin-web-deploy-check.cache-bust.json`

## 本轮拍板结论

1. “线上 `admin-web` 仍在跑旧版前端逻辑”已被确认，且当前已进入处理完成态：总控已实际完成一轮真实重发版。
2. 当前剩余短阻塞不再是“是否发上去”，而是 queryless 根页的短缓存窗口。
3. 下一步主路径固定为：
   - 等 queryless 根页缓存窗口过去
   - 重新执行首次改密真实 smoke
   - 判断问题是否已随新版 bundle 一并关闭

## 下一轮任务分配

### 分配给后台

1. 在短缓存窗口后，立即重跑首次改密真实 smoke。
2. 若新版 bundle 下问题已关闭，立即补齐 3 个最小证据点。
3. 若新版 bundle 下问题仍在，再回到页面运行态排查。

### 分配给后端

1. 当前以脚本支援位为主；已提供部署核查脚本与发布脚本。
2. 当前默认待命；除非新版 bundle 下问题仍复现，否则不再重复解释旧根因。

## 会议结论摘要

- `changePassword` 已成功，不是后端 action 问题。
- 线上站点此前跑的是旧前端逻辑，这已经有硬证据，且总控已完成重发版。
- 现在最值钱的动作不是继续发版，而是等短缓存过去后重跑真实首改 smoke。
