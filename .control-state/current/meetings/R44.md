# KK_Crab 会议纪要 R44

- 会议形式：总控夜间推进会
- 轮次：R44
- 时间：2026-03-19 20:36
- 主持：总控窗口
- 参与角色：总控、后台、后端

## 本轮新事实

1. 总控已新增可复用脚本 `__BACKEND_WORKTREE__/scripts/update-function-security-rule.mjs`，并已在 `dev` 环境完成：
   - `review-admin.invoke=true`
   - `governance-admin.invoke=true`
   - 保留 wildcard 非匿名基线
2. 后台代码侧已落地“真实读下写动作默认只读”的工程兜底：
   - 新增环境开关 `VITE_ENABLE_REAL_ADMIN_WRITES`
   - 默认值为 `false`
   - 当 `VITE_ENABLE_REAL_ADMIN_READS=true` 且 `VITE_ENABLE_REAL_ADMIN_WRITES=false` 时，审核详情、举报详情、处罚页写动作保持只读
3. 上述后台代码变更已通过本地校验：
   - `npm run typecheck`
   - `npm run build`

## 关键证据

- `__BACKEND_WORKTREE__/scripts/update-function-security-rule.mjs`
- `__ADMIN_WORKTREE__/admin-web/src/constants/ui.ts`
- `__ADMIN_WORKTREE__/admin-web/src/pages/review/detail/index.vue`
- `__ADMIN_WORKTREE__/admin-web/src/pages/report/detail/index.vue`
- `__ADMIN_WORKTREE__/admin-web/src/pages/blacklist/list/index.vue`
- `__ADMIN_WORKTREE__/admin-web/README.md`

## 拍板结论

1. 当前后台真实读策略已从“待选方案”收口为保守默认值：**先开真实读，写动作先只读**。
2. 该策略只用于真实写 transport 未就绪阶段；后续当写动作 transport 补齐后，再单独开启 `VITE_ENABLE_REAL_ADMIN_WRITES=true`。
3. 这意味着当前可以安全推进下一步：
   - 对齐 `dashboard`、`operationLogList`、审核 / 举报 / 处罚读模型
   - 在 `dev` 环境开启 `VITE_ENABLE_REAL_ADMIN_READS` 后做首批真实读 smoke

## 下一步

1. 后台优先收口 adapter/model 层字段，再开启 `VITE_ENABLE_REAL_ADMIN_READS` 做读链路 smoke。
2. 后端继续支援真实读 smoke 的 `code/requestId` 定位，不回退已验证结论。
3. 前端维持既定准备态，等待执行侧在微信开发者工具中以真实 `AppID` 拉起 cloud 运行态。
