# KK_Crab 会议纪要 R53

- 会议形式：总控推进会
- 轮次：R53
- 时间：2026-03-20 18:59
- 主持：总控窗口
- 参与角色：总控、前端、后台、后端、产品经理、阻塞协调

## 本轮新事实

1. `R53` 主线已正式切到两条：
   - 小程序真实联调 readiness
   - 正式上线倒排与验收清单
2. 小程序工程已完成 4 项 readiness 固化：
   - `__FRONT_WORKTREE__/miniprogram/project.config.json` 已切到真实 `AppID=wxa6f615dcab1f984f`
   - `__FRONT_WORKTREE__/miniprogram/constants/ui.ts` 已预置 `DEFAULT_CLOUD_ENV_ID=cloud1-4grxqg018586792d`
   - 默认 `DEFAULT_API_MODE` 继续保持 `mock`
   - `__FRONT_WORKTREE__/miniprogram/app.ts` 已新增 `App.globalData.runtimeDebug` helper
3. 小程序真实联调证据位已落仓：
   - `__FRONT_WORKTREE__/miniprogram/evidence/r53/summary.md`
   - `__FRONT_WORKTREE__/miniprogram/evidence/r53/runtime/README.md`
   - `__FRONT_WORKTREE__/miniprogram/evidence/r53/bootstrap/README.md`
   - `__FRONT_WORKTREE__/miniprogram/evidence/r53/profile/README.md`
   - `__FRONT_WORKTREE__/miniprogram/evidence/r53/application/README.md`
4. 小程序真实联调 runbook 已落仓：
   - `__MAIN_REPO_ROOT__/docs/engineering/MiniProgram-Real-Smoke-Runbook-R53.md`
   - 明早人工步骤已固定为：导入工程 -> console 切 `cloud` -> `bootstrap -> 资料读写 -> submit -> withdraw`
5. 后端 `R53` 支援物已刷新：
   - `__BACKEND_WORKTREE__/scripts/check-real-smoke-readiness.mjs`
   - `__BACKEND_WORKTREE__/scripts/coordination/miniprogram-cloudbase-integration.md`
   - `__BACKEND_WORKTREE__/scripts/coordination/r08-real-smoke-template.md`
   - 当前已明确：今晚不做脚本伪造 `OPENID` 的“假真实 smoke”
6. 正式上线倒排已从静态清单升级为执行版 owner 表：
   - `__MAIN_REPO_ROOT__/docs/operations/Miniapp-Launch-Execution-Plan-R53.md`
   - 已按 `账号/环境`、`小程序提审资料`、`内容与合规`、`首批冷启动内容`、`正式 smoke`、`发布/回滚/值守` 六段拆表
7. 控制面已同步切到 `R53`：
   - `__RUNTIME_DIR__/board.md`
   - `__RUNTIME_DIR__/decisions.md`
   - `__MAIN_REPO_ROOT__/.autonomous/r53-miniprogram-launch/task_list.md`

## 拍板结论

1. 今晚只宣称 `R53 readiness` 已落仓，不宣称小程序真实 smoke 已完成。
2. 后台 / 后端从本轮开始默认转维护态；除非小程序真实联调暴露新缺口，否则不主动扩新功能。
3. 小程序真实联调入口固定为 console helper，不新增可见调试页面，也不允许手改 storage key 作为常规流程。
4. 明早唯一人工关口固定为：
   - 微信开发者工具导入真实 `AppID` 工程
   - 调用 `runtimeDebug.useCloud()`
   - 按 `bootstrap -> 资料读写 -> submit -> withdraw` 顺序执行
5. 若 `bootstrap` 失败，必须停止后续链路，只回传 `runtime summary + request logs + error`。

## 下一步

1. 明早人工执行 `R53` 首轮真实 smoke，并把证据回填到 `miniprogram/evidence/r53/`。
2. 若真实 smoke 暴露最小 handler / 数据问题，由后端或后台按维护态即时修复。
3. 产品经理 / 阻塞协调继续收口提审资料、合规文案、冷启动内容与发布值守 owner。
4. 总控在拿到明早证据后，再决定是否扩大到 `notice-bff`、消息与发布侧更宽链路。
