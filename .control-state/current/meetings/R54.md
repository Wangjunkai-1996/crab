# KK_Crab 会议纪要 R54

- 时间：2026-03-20 20:14
- 轮次：R54
- 会议方式：总控落盘

## 本轮结论

1. `R54` 主目标正式切到：把小程序真实联调从“人工逐页点 UI”收口成“开发者控制台半自动真实 smoke + 结构化证据”。
2. 前端已落：
   - `App.globalData.devSmoke`
   - `build-miniprogram-js.mjs`
   - `check-devtools-readiness.mjs`
   - `miniprogram/evidence/r54/`
   - `MiniProgram-Real-Smoke-Runbook-R54.md`
3. 后端已落：
   - `check-miniprogram-dev-smoke-sample.mjs`
   - `R54` 版接入说明
   - `R54` 版 smoke 模板
   - `R54` 版 readiness 口径
4. 后台继续维持维护态，不主动扩治理功能；只有被小程序真实联调暴露出的最小共享缺口才进入修复。
5. 产品经理与阻塞协调继续围绕上线倒排 owner 收口，不并发开新题。
6. 体验设计审查本轮继续待命，等待 `R54` 结构化真实证据后再决定是否启动前台复核。

## 本轮明确不做

1. 不把 `DEFAULT_API_MODE` 改成 `cloud`
2. 不新增前台可见 debug UI
3. 不用脚本伪造 `OPENID`
4. 不把 `notice-bff` 更宽写链路、消息和广场真实行为挤进首轮 smoke

## 明早最小人工关口

1. 运行 `build-miniprogram-js.mjs`
2. 运行 `check-devtools-readiness.mjs`
3. 微信开发者工具导入工程
4. console 执行 `runtimeDebug.useCloud()`
5. console 执行 `devSmoke.runFirstBatch()`
6. 回填 `runtime / smoke / logs / summary`

## 当前阻塞

1. `R54` 首轮真实 `cloud` smoke 仍未实际执行。
2. 上线倒排中的提审资料、合规文案、发布/回滚/值守 owner 仍未全部到位。
3. 少量冻结规则与产品拍板项仍待后续收口。

## 下一步

1. 明早执行 `R54` 首轮半自动真实 smoke。
2. 若失败，后台 / 后端只处理最小缺口。
3. 若通过，总控切下一轮到“上线前执行与更宽真实链路扩面”。
