1. 第一批最小确认文案
- 明日建议直接发送这版“最小决策单”（与环境/联调问题分开发送，不混发）：
  【明日最小决策单｜只回一行也行】
  请你只拍板规则题 4/5/6。你可以：
  - 一次性回复：`4 选推荐，5 选推荐，6 选推荐`
  - 或只拍 1 题 / 2 题（未回复题目继续待定）
  
  4）后台动作枚举（推荐）  
  `resolveTask.reviewResult` 沿用现有正式枚举；`nextQueueType` 仅开放 `manual_review`；`resolveReport.resultAction` 仅保留“举报成立后的处置动作”；`accountAction.restrictionType` 统一为 `watchlist / restricted_publish / restricted_apply / banned`。
  
  5）举报状态（推荐）  
  举报人可见完整处理进展；状态 code 统一为 `pending / processing / confirmed / rejected / closed`。
  
  6）处罚状态模型（推荐）  
  采用“双轨模型”：`accountStatus` 管账号能力状态，`restrictionRecord.status` 管处罚记录生命周期；解除后按仍生效限制重新计算账号状态。

2. 第二批最小确认文案
- 当前版本继续 ready，但本轮只备稿，不自动发送：
  第二批 3 题你可以按 `1 / 2 / 3` 逐题回复，也可以先拍其中 1 题或 2 题。
  1）城市筛选：请回复“候选城市来源=___；默认城市来源=___”。
  2）`preferredView`：是否正式默认 `null`？可直接回“2 选 `null`”。
  3）报名详情直跳：是否维持“非列表场景不直跳，统一降级到我的报名列表”？可直接回“3 保持不直跳”；若要直跳，请同时确认补正式 `applicationId` 聚合字段。

3. 单题/双题拍板时的落盘规则
- 本规则同时适用于第一批与第二批；若用户只拍板 1 题或 2 题，只落已确认题目，其余题继续留在 `parking-lot.md`，并在会议纪要中写明“其余题继续待拍板”。
- 若用户回复不完整：仅落盘用户明确确认的内容，不推断未提及题目的选择。
- 对包含多个关键子项的题目，若用户只确认其中一半，不强行补全为完整结论；已确认部分记入会议纪要，未确认子项继续留在 `parking-lot.md`。
- 若用户直接接受推荐选项或当前临时口径转正式，总控可直接使用第 4 节对应文案落盘；若用户选择备选或自定义方案，总控应先把用户原话转成明确结论文案，再写入 `decisions.md`。
- 若用户回复“按推荐”但未指明题号，默认仅对本轮发送的题目集合生效（当前是 4/5/6），不自动扩展到 1/2/3。

4. 可直接写入 `decisions.md` 的结论文案
- 若用户接受题 4 推荐选项：`review-admin.resolveTask` 继续沿用既有 `reviewResult` 正式枚举；`nextQueueType` 页面侧仅开放 `manual_review`；`governance-admin.resolveReport.resultAction` 仅表达举报成立后的处置动作；`accountAction.restrictionType` 复用 `watchlist / restricted_publish / restricted_apply / banned`。
- 若用户接受题 5 推荐选项：举报记录对举报人展示完整处理进展；举报状态正式英文 code 采用 `pending / processing / confirmed / rejected / closed`。
- 若用户接受题 6 推荐选项：处罚状态采用“账号状态 + 处罚记录状态”双轨模型；`accountStatus` 使用 `normal / watchlist / restricted_publish / restricted_apply / banned`，`restrictionRecord.status` 使用 `active / released`；处罚解除后按仍生效限制重新计算账号状态，无生效限制则回到 `normal`。
- 若用户接受题 2 采用 `null`：`preferredView` 正式默认值定为 `null`；前端与聚合返回继续兼容 `null`，不再自行固化其他默认展示。
- 若用户接受题 3 维持当前临时口径：非列表场景不保证直跳单条报名详情；若缺正式 `applicationId` 聚合字段，统一安全降级到“我的报名列表”。
- 题 1 当前不提供固定推荐落盘文案；原因是现有文档只确认了“前端不内置城市枚举”，尚未给出正式候选城市来源与默认城市来源。若用户拍板题 1，总控应将“候选城市来源 = <用户确认值>；默认城市来源 = <用户确认值>”转写为正式结论文案后再写入 `decisions.md`。

5. 建议总控动作
- 明日优先发送第 1 节“最小决策单”单条消息，不夹带环境/联调进展。
- 环境与联调问题继续单独发一条状态消息，保持分流，避免用户一次性输入负担过高。
- 第二批 3 题文案继续备稿，不自动发送；待 4/5/6 收口后再切换。
- 若用户只回复“2 选 `null`”或“3 保持不直跳”，总控可直接使用第 4 节对应文案落 `decisions.md` 与会议纪要。
- 若用户回复题 1 但只给了“候选城市来源”或只给了“默认城市来源”，总控只记录已确认半句，并继续追问缺失来源，不自行补全。
- 管理员联调最小真实值（`ADMIN_WEB_BASE_URL`、后台真实登录入口、可用管理员账号链路、初始密码交付方式、首次改密链路）优先由后台 / 环境 / 部署持有方直接回传；仅当存在多个可选方案时，再由用户做最小确认。
- 若后台访问地址、管理员账号本轮仍未解除，总控继续稳住当前 6 题边界，不新增新的规则题。
