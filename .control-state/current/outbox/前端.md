【回报】

线别：前端

1. 已完成
- 已读取 `__RUNTIME_DIR__/inbox/前端.md`，并按 R42 口径重新整理小程序最新真实 smoke readiness；本轮未改业务代码，只修正回报口径。
- 资料读写 / runtime 稳定性：`__FRONT_WORKTREE__/miniprogram/services/runtime-config.ts`、`__FRONT_WORKTREE__/miniprogram/services/cloud.ts`、`__FRONT_WORKTREE__/miniprogram/services/bootstrap.service.ts`、`__FRONT_WORKTREE__/miniprogram/utils/request.ts` 的真实承接层仍完整，当前已具备 `mock/cloud` 单点切换、`wx.cloud.init`、bootstrap 重跑、请求调试记录 4 个能力点。
- 资料读写 / runtime 稳定性：当前已满足的前置条件已重新收口为前端侧 5 项——`AppID=wxa6f615dcab1f984f` 已有控制面事实、`dev` 候选环境已收口为 `cloud1-4grxqg018586792d`、首批小程序所需云函数当前已 `10/10 Active`、`storageRule` 在当前 `dev` 环境按已知例外处理、前端真实 runtime 承接层已落地。
- 真实 smoke 执行情况或阻塞细项：`ADMIN_WEB_BASE_URL` 与后台 `/login` 不再属于本线阻塞；这两项已关闭，当前不再沿用旧说法。
- 真实 smoke 执行情况或阻塞细项：本轮未执行真实 smoke；当前真实缺口不是后台地址，而是“小程序真实运行态尚未切到 cloud 并产出首轮证据”。
- 待拍板兼容策略：城市筛选继续只保留字段位 / 缓存位；`preferredView` 继续兼容 `null`；非列表场景继续安全降级到“我的报名列表”。这些待拍板项当前不阻塞首批真实 smoke。

2. 当前文件
- `__RUNTIME_DIR__/outbox/前端.md`
- `__RUNTIME_DIR__/inbox/前端.md`
- `__FRONT_WORKTREE__/miniprogram/services/runtime-config.ts`
- `__FRONT_WORKTREE__/miniprogram/services/cloud.ts`
- `__FRONT_WORKTREE__/miniprogram/services/bootstrap.service.ts`
- `__FRONT_WORKTREE__/miniprogram/utils/request.ts`
- `__FRONT_WORKTREE__/miniprogram/app.ts`
- `__FRONT_WORKTREE__/miniprogram/project.config.json`
- `__BACKEND_WORKTREE__/scripts/coordination/environment-handoff.md`

3. 阻塞项
- 当前已满足前置：`AppID`、`dev` 环境、云函数部署状态、`storageRule` 例外口径、前端真实 runtime 承接层都已有事实基础；这些前置本轮不再重复报阻塞。
- 当前未满足前置 1：小程序代码默认仍是 `DEFAULT_API_MODE='mock'`、`DEFAULT_CLOUD_ENV_ID=''`；如果不在真实微信开发者工具运行态主动写入 `cloud` 与当前 env，`onLaunch` 仍会自动回到 mock。
- 当前未满足前置 2：`__FRONT_WORKTREE__/miniprogram/project.config.json` 仍是 `appid='touristappid'`；仓内尚无“已按真实 `AppID=wxa6f615dcab1f984f` 重新绑定项目并成功拉起 cloud runtime”的前端侧执行证据。
- 当前未满足前置 3：首批真实 smoke 仍未落证据；`bootstrap` → 资料读写 → `submit/withdraw` → 报名详情撤回入口 这 4 步尚未回传真实 `requestId/code/message` 与页面结果。
- 已关闭项：`ADMIN_WEB_BASE_URL` 与后台 `/login` 已不是当前本线阻塞，不再写入后续 readiness 口径。

4. 需要其他线配合
- 需要能操作微信开发者工具的执行侧，用真实 `AppID=wxa6f615dcab1f984f` 导入 `__FRONT_WORKTREE__/miniprogram`，确认当前不是 `touristappid` 工程态。
- 需要执行侧在小程序真实运行态把 runtime 切到 `cloud`，并写入当前 `dev` 环境 `cloud1-4grxqg018586792d`，然后再执行首轮真实 smoke。
- 需要总控 / 后端在 smoke 期间继续沿用既有环境 handoff 事实，不再回退成“缺后台地址 / 缺 `/login`”；若真实请求失败，再按具体函数 / action 回查。
- 需要体验设计审查在首轮真实证据回传后，再进入 must-close 复核；当前仍维持“前端承接 ready，真实证据待补”的边界。

5. 下一步
- 最接近可执行的下一步只有 1 条：在微信开发者工具里以真实 `AppID` 打开工程，切 runtime 到 `cloud`，并写入 `cloud1-4grxqg018586792d`。
- 完成切换后，立即按固定顺序执行首批真实 smoke：`bootstrap` → 资料读写 → `submit/withdraw` → 报名详情撤回入口。
- 每一步都记录真实 `requestId/code/message` 与页面结果；如果某一步失败，再把阻塞收口到具体函数 / action，不再回退成旧的后台地址口径。
- 在真实 smoke 未执行前，继续保持题 1 / 2 / 3 的兼容位与安全降级，不自造新规则。
