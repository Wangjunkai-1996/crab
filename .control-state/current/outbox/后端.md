【回报】

线别：后端

1. 已完成
- 已读取 `__RUNTIME_DIR__/inbox/后端.md`，并结合 `__BACKEND_WORKTREE__/cloudfunctions/**`、`__BACKEND_WORKTREE__/database/**`、`__BACKEND_WORKTREE__/scripts/coordination/environment-handoff.md` 完成后续真实读方案分析。
- 已输出专项方案文档：`__BACKEND_WORKTREE__/scripts/coordination/review-governance-real-read-plan.md`，包含两条路线对比：
  - 路线 A：继续匿名 CloudBase transport + 扩函数安全规则（短期最小可执行）
  - 路线 B：换 transport / token 化方案（中长期安全收敛）
- 已明确推荐结论：当前推荐先走路线 A 解锁 `review-admin` / `governance-admin` 真实读，再分阶段规划路线 B；两者可串行。
- 已明确影响评估：路线 A 不改现有 `admin-auth` 协议与函数内鉴权逻辑，对已验证 `admin-auth` 链路为低风险兼容；路线 B 若一次性全切会有中等回归风险，建议分阶段迁移。

2. 当前文件
- `__BACKEND_WORKTREE__/scripts/coordination/review-governance-real-read-plan.md`
- `__RUNTIME_DIR__/outbox/后端.md`

3. 阻塞项
- 当前技术阻塞不在后端函数实现，而在环境侧是否执行路线 A 的规则放开动作：是否在当前 `dev` 环境将 `review-admin`、`governance-admin` 加入 `invoke=true`。
- 若不执行路线 A、直接走路线 B，则会新增 transport 改造成本与回归窗口，短期无法“最小代价”开启真实读。

4. 需要其他线配合
- 总控/环境执行：按拍板结果更新当前 `dev` 环境函数安全规则（若走路线 A）。
- 后台：在规则到位后立即执行两组真实读 smoke（`review-admin.taskList/taskDetail`、`governance-admin.reportList/reportDetail`），并回传 `code/requestId` 与页面证据。
- 体验设计审查：对后台真实读证据做最小锚点复核，避免把“能调通”误写成“体验已收口”。

5. 下一步
- 若总控拍板路线 A：后端配合观察 `30002/30003/40003` 分布，确认函数内 `source/session/role` 三层鉴权继续有效。
- 若总控拍板路线 B：后端先产出 transport 迁移最小设计稿（灰度函数域 + 双通道并存 + 分函数切换），并单独评审迁移窗口，不影响当前已验证 `admin-auth`。
- 持续维持当前 `admin-auth` 已验证事实，不回退既有结论。
