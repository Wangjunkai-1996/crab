# KK_Crab 待拍板事项停车场

- 更新时间：2026-03-20 10:48
- 当前轮次：R45

## P0：影响当前联调或规则收口

- [ ] 后台动作请求最终枚举：`resolveReport.resultAction`、`accountAction.restrictionType`、`resolveTask.nextQueueType`
  - 提出方：后台 / 后端 / 产品经理 / 体验设计审查
  - 影响范围：后台动作表单、后端参数校验、正式 contract、真联调稳定性
  - 当前处理：产品经理已给第一批推荐方案与局部落盘规则；未拍板前继续使用后端临时事实源 + 后台单点 bridge / 常量层

- [ ] 举报记录对举报人的“处理中”可见性，以及举报状态正式英文 code
  - 提出方：产品经理 / 后端
  - 影响范围：举报记录页、消息通知、后端 DTO、后台返回前台的状态裁剪口径
  - 当前处理：产品经理已给第一批推荐方案与局部落盘规则；未拍板前后端继续按 `string` 保守兼容

- [ ] 处罚状态模型与英文 code：是否采用“账号状态 + 处罚记录状态”双轨，以及解除后的回退规则
  - 提出方：产品经理 / 后端
  - 影响范围：后台处罚列表、前台限制说明、后端 contract、处罚解除逻辑
  - 当前处理：产品经理已给第一批推荐方案与局部落盘规则；未拍板前后端继续按 `string` 保守兼容

## P1：可继续并行，但仍需正式拍板 / 验证

- [ ] `dm_configs` 重复清理策略
  - 提出方：后端 / 总控 / 产品 / 后台
  - 影响范围：配置 seed 幂等性、环境数据可信度、后续初始化 / 迁移策略
  - 当前处理：已确认 8 个正式 `groupKey` 各重复 1 次；后端推荐“保留最早 8 条、删除后插入的 8 条”，待总控正式确认后执行 `__BACKEND_WORKTREE__/scripts/dedupe-config-groups.mjs --strategy oldest --apply`

- [ ] 后台业务域详情页是否需要本轮最小 `dev` 样本数据，还是接受空态作为首轮页面级真实读基线
  - 提出方：总控 / 后台 / 后端 / 体验设计审查
  - 影响范围：`review-admin.taskDetail`、`governance-admin.reportDetail`、页面级真实读 smoke、详情页体验证据
  - 当前处理：服务端真实读 smoke 已证实当前 `review-admin.taskList` 与 `governance-admin.reportList` 返回空列表；环境 / 鉴权链路已绿，待决定是否追加样本数据覆盖 populated-data 路径

- [ ] 非列表场景是否要求直跳单条报名详情，以及是否必须补正式 `applicationId` 聚合字段
  - 提出方：前端 / 产品经理 / 体验设计审查 / 后端
  - 影响范围：`notice-bff.detail` 等聚合接口、前端 CTA 语义、报名详情直跳能力
  - 当前处理：前端统一安全降级到“我的报名列表”

- [ ] `preferredView` 默认值
  - 提出方：前端 / 产品经理 / 体验设计审查
  - 影响范围：`bootstrap` 返回、我的页头部摘要、入口排序、首次进入默认展示
  - 当前处理：技术口径允许返回 `null`，前端兼容 `null`

- [ ] 城市筛选数据源与默认城市来源
  - 提出方：前端 / 产品经理 / 体验设计审查
  - 影响范围：广场页、搜索页、筛选缓存、默认城市展示、前后端筛选口径
  - 当前处理：前端只保留字段位、缓存位、回显位，不内置城市枚举

- [ ] `notice-bff.republish.deadlineAt` 是否进入正式 contract
  - 提出方：后端 / 前端 / 产品经理
  - 影响范围：发布侧重发流程、前端 CTA、后端 contract、用户预期一致性
  - 当前处理：后端已给候选 contract / sample；未拍板前前端继续规避，后端不写入正式共享 contract

## P2：影响验收效率与体验复审

- [ ] 小程序真实 smoke 截图与录屏
  - 提出方：体验设计审查
  - 影响范围：前端体验第二轮复审
  - 当前处理：待执行侧在微信开发者工具中以真实 `AppID` 拉起 cloud 运行态并产出首轮证据；当前只接受替代证据

- [ ] 后台业务域页面级真实读证据
  - 提出方：体验设计审查 / 后台 / 总控
  - 影响范围：`dashboard`、日志页、审核 / 举报 / 处罚列表与详情的体验复核
  - 当前处理：`admin-auth` 体验复核已关闭；下一轮等待 `VITE_ENABLE_REAL_ADMIN_READS=true` 下的页面级真实读截图 / 录屏 / 状态清单

- [ ] Token 细节与关键状态页统一性待截图 / 录屏或状态清单补齐后复审
  - 提出方：体验设计审查
  - 影响范围：前后台视觉一致性、最终体验验收
  - 当前处理：前端 / 后台后续回报必须按证据模板与 must-close 清单补关键状态页证据或替代证据

## 使用规则

1. 未拍板事项只记录在这里，不散落在聊天和回报中。
2. 一旦拍板，应转移到 `decisions.md`，并从这里移除或标记已处理。
3. 若事项已阻塞当前轮执行，应同时在 `board.md` 的“跨线阻塞”中登记。
