# 前端收件箱

- 更新时间：`__GENERATED_AT__`
- 状态：执行中
- 工作目录：`__FRONT_WORKTREE__`
- 分支：`codex/mp-v1`

## 本轮任务

1. 继续保留 mock 联调模式，不要自行发明业务规则、权限规则、状态流转或联系方式释放逻辑。
2. 优先完善页面层细节和真实联调切换准备，不要扩散到共享文档或其他端目录。
3. 优先把 `bootstrap` 的真实联调适配层准备好，等后端提供 CloudBase 环境信息后完成从 mock 到真实接口的切换。
4. “城市筛选数据源”仍是待确认项，先保留字段位和缓存位，不要自行内置城市枚举。
5. 如本地环境允许，请补齐 TypeScript 静态编译检查。

## 本轮目标

- 细化广场、发布、消息、我的四个主 Tab 页面状态
- 细化搜索、详情、我的通告、我的报名、达人名片等首批内页
- 准备 `bootstrap` 真联调切换点，保持 mock 与真实请求可平滑切换

## 回报要求

完成后请把结果覆盖写入：
`__RUNTIME_DIR__/outbox/前端.md`

使用模板：

1. 已完成
2. 当前文件
3. 阻塞项
4. 需要其他线配合
5. 下一步
