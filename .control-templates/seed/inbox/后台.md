# 后台收件箱

- 更新时间：`__GENERATED_AT__`
- 状态：执行中
- 工作目录：`__ADMIN_WORKTREE__`
- 分支：`codex/admin-v1`

## 本轮任务

1. 优先接入真实 `admin-auth` 接口，包括 `login`、`me`、`logout`、`changePassword`。
2. `review-admin`、`governance-admin` 相关页面暂时继续保留 mock，但必须把字段统一收口到 service/model，不要在页面里散落定义最终 DTO。
3. 不要自行补完 `taskList`、`taskDetail`、`reportList`、`reportDetail`、`accountActionList`、`operationLogList` 的最终字段结构，等待后端统一提供。
4. 页面内动作显隐、工作台入口显隐、详情动作面板仍以后端返回的 `permissionSummary`、`availableActions` 为最终事实来源，不要前端自行固化。
5. 保持当前页面骨架稳定，优先做“可替换 mock”为真实接口的结构准备。

## 本轮目标

- 接入真实 `admin-auth`
- 收口 `review-admin` / `governance-admin` 的 service 类型与模型结构
- 为后端 DTO 到位后的一次性替换做好准备
- 继续完善审核详情、举报详情、处罚列表的动作表单与只读态切换

## 回报要求

完成后请把结果覆盖写入：
`__RUNTIME_DIR__/outbox/后台.md`

使用模板：

1. 已完成
2. 当前文件
3. 阻塞项
4. 需要其他线配合
5. 下一步
