# 后端收件箱

- 更新时间：`__GENERATED_AT__`
- 状态：执行中
- 工作目录：`__BACKEND_WORKTREE__`
- 分支：`codex/backend-v1`

## 本轮任务

1. 暂缓优先推进 `user-bff.mine`、`publisher-bff.getProfile`、`upsertProfile`，先补后台联调急需的管理端 DTO 与返回样例。
2. 优先补齐以下接口的字段级返回结构：
   - `review-admin.taskList`
   - `review-admin.taskDetail`
   - `governance-admin.dashboard`
   - `governance-admin.reportList`
   - `governance-admin.reportDetail`
   - `governance-admin.accountActionList`
   - `governance-admin.operationLogList`
3. 同时补齐：
   - `availableActions` 的最终 action key 枚举
   - `admin-auth.me.permissionSummary` 的结构定义
4. 尽量同时提供 TypeScript interface 与 JSON 返回样例，便于后台线直接对齐 service/model。
5. 同步准备小程序联调侧的 CloudBase 环境接入说明、可用云函数清单、`user-bff.bootstrap` 联调说明。
6. `preferredView` 默认值目前文档未锁定，请继续标记为待确认，不要固化为最终规则。
7. 继续推进 CloudBase 数据库规则/存储规则的正式语法与下发口径，争取将 dry-run 初始化脚本升级为可执行版本。

## 本轮目标

- 输出后台管理接口 DTO 明细与样例
- 输出 `availableActions` 与 `permissionSummary` 结构
- 输出小程序可用的环境接入说明
- 保持现有底座稳定，不扩散改动到其他线目录

## 回报要求

完成后请把结果覆盖写入：
`__RUNTIME_DIR__/outbox/后端.md`

使用模板：

1. 已完成
2. 当前文件
3. 阻塞项
4. 需要其他线配合
5. 下一步
