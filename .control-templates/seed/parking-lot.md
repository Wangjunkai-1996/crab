# KK_Crab 待拍板事项停车场

- 更新时间：`__GENERATED_AT__`
- 当前轮次：`__CURRENT_ROUND__`

## P0：影响当前联调

- [ ] 城市筛选数据源与默认城市来源
  - 提出方：前端
  - 影响范围：广场页、搜索页、筛选缓存、默认城市展示
  - 当前处理：只保留字段位和缓存位，不内置城市枚举

- [ ] `preferredView` 默认值
  - 提出方：后端
  - 影响范围：`bootstrap` 返回值、我的页摘要与入口排序
  - 当前处理：后端临时按占位值处理，但不固化为最终业务规则

## P1：影响部署与执行效率

- [ ] CloudBase 数据库规则/存储规则正式下发口径
  - 提出方：后端
  - 影响范围：初始化脚本从 dry-run 升级为可执行流程
  - 当前处理：继续确认 CLI 与规则文件语法

## 使用规则

1. 未拍板事项只记录在这里，不散落在聊天和回报中。
2. 一旦拍板，应转移到 `decisions.md`，并从这里移除或标记已处理。
3. 若事项已阻塞当前轮执行，应同时在 `board.md` 的“跨线阻塞”中登记。
